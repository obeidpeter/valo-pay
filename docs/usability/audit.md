# Usability audit and delivery backlog

## Evidence baseline

Audit date: **22 September 2026**. Branch: **codex/evidence-led-usability**. Source baseline: local commit **cc9cca4127d963d9f8a3a600091d502c08eb4239**, tree **17d3da2e958ed198a65b63c2bf0eda6e223029f3**; the tree matches GitHub main **210f30f2dd87b26b5cdf7f2d6205a06f5a2eaa5a**, merged PR47. No existing local edits were overwritten.

Browser baseline used the pre-existing compiled public-page release on an isolated loopback server, with deterministic synthetic records dated 19 September 2026. Its index SHA-256 is **ea217a3cb2993b1dd90b61abb3b2e3a356c39d9a4eb961d41eb2383b2ab3e37d**. The entry assets were index--lLTvsh-.js and index-BdHNgLDp.css. This records the actual compiled artefact rather than assuming that a source checkout is a build. The prior live build was 0ce4a88; this assignment does not publish production.

Before captures cover all 16 console routes (including customer history), landing, sign-in and sign-up at 1440, 768 and 390 CSS pixels, plus permission and proposal review states. Lists are deliberately seeded beyond one page. Local browser checks use Chrome on Windows; CI exercises the repository's additional browsers and PostgreSQL. Authentication in the loopback build is the documented anonymous fallback, not a real Clerk/MFA test.

During regression testing, the paginated fixture was corrected: 55 cloned proposals formerly shared one receipt; each now has its own linked receipt and instalment. This invalid baseline fixture correctly failed the new specific-proposal guard. Before screenshots are retained unaltered. The after fixture uses the same synthetic date, roles and viewport sizes but has additional linked records; differences in totals are fixture effects, not measured financial or usability improvement.

Verified architecture: React 19.1 / TypeScript / Vite, Wouter, TanStack Query v5, Radix primitives, Tailwind tokens; Node.js/Express with Drizzle/PostgreSQL and the existing generated /api/v1 contract. Existing light/dark themes, integer-kobo amounts, WAT display, persistent server jobs, tenant locks, idempotency responses, audit records and independent-review rules remain. No dependency, schema migration, external analytics service or new financial engine is introduced.

Planning sources inspected: the later Business Plan v3.0, Technical Requirements v2.0, Product Roadmap v2.0 and Marketing and Sales Strategy v2.0 in the supplied Connected Banking document set, their earlier v2.2/v1.2 baselines, relevant Open Banking scope and the repository's implementation record. Relevant requirement sections were read for UI-01/05/06, TEN-01/02/03/10, API-01/03, CON-02/04/08, OB-CNS-01–05, OB-DAT-01–04 and the separate activation gates. Documentary ambitions are not treated as implemented capabilities. The business plan and financial projections are unchanged; amendment proposals are in the release note.

## Method and source guidance

[NN/G Usability 101](https://www.nngroup.com/articles/usability-101-introduction-to-usability/) supplies five separate quality components. This audit treats first use, learned performance, return after absence, error handling and experienced ease as different questions. [The heuristics](https://www.nngroup.com/articles/ten-usability-heuristics/) are diagnostic prompts for individual issues, not a weighted score. [NN/G severity guidance](https://www.nngroup.com/articles/how-to-rate-the-severity-of-usability-problems/) supplies the 0–4 convention and frequency, impact and persistence considerations.

Severity here is expert judgement: 0 not a problem, 1 cosmetic, 2 minor, 3 major, 4 catastrophic. No live losses or observed human error rates are asserted. Conditional network/race findings are rated against their consequence and persistence, with frequency explicitly unknown. **Delivery priority is a separate Valo Pay judgement**: contain financial/privacy risks and shared defects first, then repeated work and accessibility, then secondary polish. Ordinal ratings are not multiplied into a precision score.

[NN/G usability testing](https://www.nngroup.com/articles/usability-testing-101/) informs representative tasks and participants. [NN/G questionnaire guidance](https://www.nngroup.com/articles/measuring-perceived-usability/) informs task-level SEQ and session-level SUS. Browser scripts are engineering evidence; human learnability, memorability and satisfaction remain pending. [WCAG 2.2](https://www.w3.org/TR/WCAG22/) is the accessibility target. Automated rule results, keyboard inspection and viewport emulation have narrower scope than full conformance.

Implementation references checked include [React refs](https://react.dev/reference/react/useRef), [controlled inputs](https://react.dev/reference/react-dom/components/input), [TanStack mutation behaviour](https://tanstack.com/query/latest/docs/framework/react/guides/mutations), [Radix dialogs](https://www.radix-ui.com/primitives/docs/components/dialog) and [focus not obscured](https://www.w3.org/WAI/WCAG22/Understanding/focus-not-obscured-minimum.html). The resulting designs and priorities are Valo Pay-specific recommendations.

## Findings register

Detailed reproductions, evidence, component mapping, effort, acceptance tests and residuals are in [core findings](core-findings.md), [import/form findings](import-findings.md) and [connected findings](connected-findings.md). Those records distinguish source defects from UI observations and hypotheses. The following shared-shell findings supplement them.

| ID / route / task | Observed reproduction and evidence | Components / diagnosis | Severity, frequency, consequence, persistence | Delivery / fix / acceptance |
| --- | --- | --- | --- | --- |
| UX-R01 · all console / J01 | Open anonymous Overview: baseline shows “Mode: sandbox” and no active role in the shell, though actual lender mode is observation. Source: Layout and workspace response; before Overview capture | Learnability, memorability, errors, satisfaction; H1/H2/H4, WCAG 1.3.1 | **2**. Every anonymous session; task frequency unknown. Environment can be mistaken for operating authority, recurring across pages | First slice, small. Show environment separately, actual lender mode, demo role and WAT on desktop/phone. Layout regression verifies source values |
| UX-R02 · queue return / J04, J07 | Baseline resets independent main scroll to zero on route change. Filter/page survives but return position does not. A first replacement sampled scroll after DOM clamping; independent Chrome review reproduced 1800→short page→0 | Efficiency, memorability, satisfaction; H3/H6/H7, WCAG 2.4.3 | **2**. Conditional on long queue; expected recurring. Repeated location reconstruction, no monetary write | Small shared fix. Bounded session-only offset map by route and lender/actor/role; observe scroll before DOM change, stop restoration on user interaction. Real browser tall/short/back test required |
| UX-R03 · shared mutations / J02–J07 | Lose a write response, then change input: baseline assigns a new key. Later authentication failure during recovery does not prove the original failed; authority checks precede replay lookup | Errors primary, all five components; H1/H5/H9, WCAG 3.3.1/3.3.3/4.1.3 | **3**. Network frequency unknown. Duplicate intention or false no-change message can persist in records | Highest priority, medium. Retain original key/payload, lock changed submissions, exact retry. Preserve an already unknown outcome through later 401/403/409. Focused tests inject committed response loss and recovery refusal |
| UX-R04 · reconciliation / J03 | After proposal creation, mark its instalment cancelled/closed/in dispute. Baseline confirmation overwrites state while engine candidate selection excludes those states | Errors, learnability; H5/H9, WCAG 3.3.4 | **3**. Race frequency unknown; inconsistent state with lasting record impact | Contained with UX-C01. Recheck eligibility before allocation creation/application; three status cases preserve the entire prior state. Golden suite includes these cases |

Shared changes support all five components without claiming measured improvement: visible context teaches correct scope; error links and retained queue position reduce reconstruction; stable locations and explicit unfinished outcomes support return; authoritative checks prevent costly mistakes; clearer comparison and feedback are hypotheses about comfort/control to test with users.

Final regression and independent code review added four bounded corrections:

- **UX-R05 — malformed confirmation (errors, learnability, satisfaction; H1/H5/H9; severity 3, network frequency unknown):** a valid HTTP success with an empty or malformed body previously cleared the shared retry key. Validate existing generated response contracts before clearing it, including record/lender identity and export readiness. Seven mutation families now retain the original body/key on an incomplete receipt; null, empty and invalid JSON responses are covered. No new dependency or public response contract was introduced.
- **UX-R06 — small-screen queue filters (efficiency, satisfaction; H4/H8; WCAG 1.4.10; severity 2):** at 320 CSS pixels the collections owner select could extend beyond its clipping card once its font loaded. The first multi-browser CI run also exposed the exception-type select in mobile WebKit in both themes. Stack the collection owner and exception owner/type labels on phones and explicitly constrain native select widths. The browser assertion waits for fonts and checks main-column containment, plus card containment for the collection owner. This is an observed layout defect, not an inferred participant error; assertions were retained.
- **UX-R07 — automatic batch resilience (errors, efficiency; H1/H5/H9; severity 3, conditional on stale obligations):** a strong receipt reference could reach a stopped instalment, and narration/amount rules could choose an instalment whose balance had already reduced. Keep ineligible receipts unallocated for Finance and apply current-balance limits before automatic selection. Do not redirect a strongly linked stopped receipt to another obligation. Golden tests show later valid receipts still reconcile and repeated runs do not duplicate allocations or ageing exceptions. Manual confirmation guards remain.
- **UX-R08 — reduced-motion theme contrast (errors, satisfaction; H1/H4; WCAG 1.4.3; severity 2, transient and timing-dependent):** a later CI run passed after retry but its first mobile WebKit dark-mode Settings audit found black inherited text on dark surfaces. An isolated reproduction using the compiled stylesheet traced this to the universal `transition-duration: 0.01ms`, which activates the default `transition-property: all` on otherwise unanimated text. WebKit propagated the old inherited colour over successive frames: 8/8 theme switches and 8/8 initial stylesheet applications still had wrong descendant colour after two frames. Changing only transition duration to `0s` made all 16 correct immediately, consistent with [MDN's zero-duration definition](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/transition-duration). Keep near-zero animation duration for animation-end lifecycle events; no application transition-end consumer was found. This fixes a transient rendered contrast problem rather than suppressing or delaying the axe assertion.

Traceability for these final corrections:

| ID / affected role and task | Changed implementation paths | Effort / dependencies | Executable acceptance / remaining boundary |
| --- | --- | --- | --- |
| UX-R05 · permitted writers, J02–J07; shared forms, settings and exports | `artifacts/valo-pay/src/lib/safe-mutations.ts` | Small–medium shared change; existing generated response schemas, server idempotency and consumer recovery controls. No API migration | `artifacts/valo-pay/tests/safe-mutations.test.tsx`: seven mutation families retain original key/body until a complete receipt; null, empty and invalid JSON stay uncertain. Implemented/tested. Reload recovery remains UX-B02 |
| UX-R06 · Operations/Finance/Admin and permitted readers, J04 and Collections; phone filters | `artifacts/valo-pay/src/pages/collections.tsx`, `artifacts/valo-pay/src/pages/exceptions.tsx` | Small layout correction; existing native controls, responsive tokens and loaded fonts. No backend dependency | `artifacts/valo-pay/e2e/usability.spec.ts`: both themes at 320 CSS pixels preserve visible controls inside the main column and collection-owner card after fonts load. Implemented; Chrome/WebKit follow-up passed. Physical-device/manual accessibility remains UX-B04 |
| UX-R07 · Admin/Operations/Finance, J03/J07; Reconciliation and daily close | `artifacts/api-server/src/domain/reconciliation.ts` | Small bounded domain correction; existing strong-reference ladder, current balances, Finance queue and ageing exceptions. Manual eligibility/ceiling guards remain authoritative | `artifacts/api-server/tests/reconciliation-golden.test.ts`: stopped instalments with both strong-key sources and reduced balances under R4/R5 do not abort valid later receipts; repeated runs preserve one allocation/ageing exception. `artifacts/api-server/tests/close-schedule-golden.test.ts`: adjacent close behaviour retained. Implemented/tested; independent engineer review remains required before merge |
| UX-R08 · all roles using reduced motion; theme changes and first rendering | `artifacts/valo-pay/src/index.css`, `artifacts/valo-pay/e2e/usability.spec.ts` | Small CSS correction; existing theme tokens and browser preference, no new dependency | New real Settings dark/light regression checks inherited colour and text fill against the active palette immediately, with zero transition duration. All existing axe assertions remain. Isolated WebKit comparison verified; integrated browser/CI confirmation pending at this entry. Manual assistive-technology acceptance remains UX-B04 |

## Explicit remaining backlog

| ID / classification | Risk and evidence | Priority / owner / next acceptance |
| --- | --- | --- |
| UX-B01 · backend compatibility | Old API callers may omit proposal identity/version. Current console always sends both; no mandatory schema change was made | High before any live acceptance. API owner: inventory callers, version/deprecate old contract, require exact reviewed proposal and prove concurrent requests against PostgreSQL |
| UX-B02 · recovery persistence | Exact uncertain-request identity is in component memory. Reload, sign-out or deliberately leaving discards it; sensitive bodies are not persisted in browser storage | High before real data/live actions. API/product owner: durable scoped operation lookup/recovery design. For this sandbox, inspect saved jobs/audit/records before a new intention; never infer rollback from a timeout |
| UX-B03 · import contract | A new deliberate reference-free import cannot be content-deduplicated by the current reference contract | Medium in synthetic scope, high before live import. Product/API owner: require references or approve durable import ledger, preserve atomicity and audit. UI discloses current limit |
| UX-B04 · human/accessibility validation | No representative participants, delayed session, physical devices or assistive technology were available in this run | Release gate for relevant live scope. Research/accessibility owner: execute kit; screen-reader, 200/400% actual zoom, physical keyboard/touch, accessible authentication and full contrast/target review |
| UX-B05 · real roles and authentication | Demo actors and persona switch are not real MFA, staff assignments or internal support capabilities | Existing G-DATA/TEN gates remain. Security owner: genuine staff identities, fresh MFA, revoked-session recovery and permitted exports verified in controlled staging |
| UX-B06 · preference privacy | Existing saved views keep user-entered name/search per lender in browser storage; queue queries are in URLs. Synthetic operation limits exposure, but typed identifiers/searches need a real-data design review | High before real-data activation. UX/security owner: user-scoped preference storage, sensitive search retention/redaction rules and safe return tokens. This release introduces only in-memory scroll offsets and no new persistent sensitive fields |
| UX-B07 · export availability | API supplies status/checksum and reauthorises download but no expiry date | Medium. Product/security owner: agreed retention/expiry contract before displaying dates; do not invent expiration or remote recall |
| UX-B08 · live external acceptance | Paystack has no verified test key/route; real bank, Xero, tax and payout capabilities remain disabled | Existing external gates, not a UI bug. Provider/security/finance owners: contracted access and scoped evidence; synthetic tests cannot pass these gates |
| UX-B09 · occasional-task onboarding | Optional sandbox guide is a general checklist, not a personalised work assignment; phase/role-specific first-task usefulness is an expert hypothesis | Medium after task study. Research/product owner: observe fresh users across roles before adding or reorganising guidance. Stable navigation retained |
| UX-B10 · import validation wording | The final row-error screenshot still includes raw validator terms such as consentProvenance and a minimum-character message. Error-only review and correction paths work, but the backend wording is not fully plain language | Medium. UI/API owner: map known validation paths to consistent import column labels and actionable messages without hiding raw evidence or changing accepted data; validate the revised copy with operators |

No unresolved catastrophic finding is asserted as fixed by visual polish. Known residuals are explicit and prevent treating this sandbox release as live financial acceptance. No bulk action, command palette, provider activation, new analytics or navigation migration was justified; established deep links and task locations remain.
