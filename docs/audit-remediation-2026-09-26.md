# Audit remediation — 26 September 2026

This release addresses the twelve findings from the comprehensive audit of PR #60 / main commit `9ae9f8afec9e2e3f9070809c5026f0ff182d74ce`. The fixes preserve recorded decisions and financial evidence. Where old records cannot establish intent or ownership, they require an explicit review rather than inventing it.

| Finding | Change | Regression coverage |
| --- | --- | --- |
| FIN-01 — editable exception decisions | Generic edits cannot write or remove workflow-owned resolution fields. Completed decisions, attribution and meaning are immutable at validation and final-state boundaries. Compliance reviewers cannot edit operational exceptions. The interface offers actions permitted by those rules. | `record-identity-guards.test.ts`, financial golden tests, exception interface tests. |
| FIN-02 — ambiguous unversioned reversal decisions | Reconciliation preserves the original decision and raises a renewed Finance/Admin review. Relevant payments and instalments are held; ordinary dispute release cannot bypass the review. A renewed decision records explicit current semantics. Previously applied effects remain until reviewed. | `financial-migrations.test.ts`, payment-evidence and dispute-lifecycle golden tests; repeat runs and both renewed outcomes. |
| FIN-03 — settlement identity collisions | Batch lookup and statement linking share provider-connection identity plus the exact batch reference. Existing mixed-provider batches and foreign links stay in variance with the prior evidence retained for correction. | Financial migration, settlement-currency and reconciliation-scale tests; both arrival orders and ambiguous legacy data. |
| FIN-04 — global observation event IDs | Import, validation, final-state checks and PostgreSQL use lender + provider connection + delivery channel + event ID. Distinct providers may legitimately reuse event IDs. | Record identity unit tests, migration 009 PostgreSQL rehearsal and exact readiness definitions. |
| FIN-05 — duplicate customer references | Nonempty references are unique per lender on create and edit, with a matching database constraint. Imports refuse ambiguous historical references. | Record identity tests, migration preflight atomic rollback and schema-push rehearsals. |
| REL-01 — silent sign-in module failure | Failed loading has an accessible error and retry. Retry fetches a fresh auth module URL and keeps provider/forms in the same module context. Drafts and anonymous pages remain mounted. | Auth session unit regressions and actual failed-chunk retry in built Chromium output. |
| REL-02 — exports behind busy lenders | The worker retains a keyset cursor across bounded twenty-job passes, traverses newer work and wraps to skipped jobs. Each sweep has a fixed upper bound so continuous new arrivals cannot postpone revisiting older jobs. Database timestamp precision is preserved. Concurrency, leases and ownership checks remain enforced. | Multi-page and continuous-arrival worker regressions; PostgreSQL test with two locked lender queues, a free third lender, microsecond timestamps and subsequent recovery. |
| UI-01 — faint field boundaries | Form controls use a dedicated border token with at least 3:1 contrast in both themes. Decorative borders retain their own token. | Computed browser contrast for actual rendered fields in light and dark themes. |
| UI-02 — oversized sidebar at enlarged text | Navigation switches according to available content space and text size; shared buttons wrap. Drawer keyboard and focus behaviour is retained across resize. | Desktop/mobile browser checks at 200% root text size, including keyboard navigation. |
| SEC-01 — legacy sandbox-cookie fixation | Retired cookie names never select an anonymous principal. HTTPS accepts only the current `__Host-` cookie; old names are ignored and cleared where possible. | Actual HTTP edge checks, conflicting/planted-cookie cases, current-cookie precedence and revised smoke checks. |
| SEC-02 — unbounded Clerk proxy work | The route has process/network concurrency and rate budgets, an absolute deadline, request/response byte limits, cancellation on disconnect and fixed safe failure replies. | Local HTTP upstream tests: streaming, unknown lengths, oversized requests/responses, aborts, timeouts, network quotas and slot recovery. |
| DEP-01 — transitive uuid advisory | A scoped override moves only gaxios 6.7.1's uuid to 11.1.1, retaining its CommonJS v4 API. | Full dependency audit and actual Storage/gaxios multipart generation without credentials or network calls. |

## Existing data and rollout

Apply migration 009 before publishing this application build, in every application and isolated runtime schema. See [database migrations](database-migrations.md) and the [identity preflight](record-identity-migration.md). Duplicate references or deliveries stop the transaction without deleting evidence. Review and correct genuine historical duplicates through an approved recorded process before retrying.

Unversioned reversal decisions receive a separate renewed-review exception on reconciliation. Finance or an administrator must inspect the retained evidence and choose the appropriate current outcome. Do not remove the hold by editing the original exception or releasing its instalment as an ordinary dispute.

Previously combined settlement batches stay in variance. Their earlier totals, provider identities and statement links are preserved. Run reconciliation for every existing lender after rollout before presenting refreshed settlement results; historical snapshots remain historical. A data owner must establish the real provider-scoped breakdown and commission a reviewed data repair. This release deliberately provides no generic edit or automatic release for that ambiguous historical state: ordinary reimport and reconciliation do not clear the quarantine. Fresh provider-scoped records no longer merge solely because their batch references coincide.

The retired-cookie change affects anonymous browsers that still hold only an old cookie: they start a new sample workspace. Existing current cookies and staff accounts retain their identity. No database records are deleted by this change. The old browser cookie is deliberately not accepted as proof of ownership for restoration.

## Host checks

The Clerk proxy permits 240 requests per minute per client network (IPv6 /64), 8 concurrent requests per network and 32 per process. It bounds each attempt to 30 seconds, requests to 16 MiB, unknown-length buffered replies to 4 MiB and known-length replies to 32 MiB. Quota/concurrency refusals are 429 with `Retry-After: 60`; oversized requests return 413; unsupported `Expect` handshakes return 417; upstream failures return 502/504 or terminate a reply whose headers were already sent. Disconnecting cancels upstream work. Limits reset on process restart and multiply across instances; the edge remains responsible for fleet-wide quotas and trusted forwarding headers.

Before commissioning a host, verify sign-in with its configured Clerk instance, readiness after migration, real provider test credentials when available, private export storage, monitoring delivery and backup recovery. Offline and disposable-database tests do not establish that those external services have been configured. No production database change, real payment, or live credential test is performed by this release's local verification.

## Verification

Local verification passed all 31 PostgreSQL-backed suites (including the final sandbox-identity and export-queue retests), 214 desktop/mobile Chromium browser tests with two expected viewport skips, workspace TypeScript, production API and console builds, generated-contract consistency, and documentation checks. The dependency audit reported no advisories. Financial migration, identity, cookie, proxy, export and Storage/uuid compatibility regressions exercise the fixes without external credentials. Platform-specific test-runner changes let the same checks run on Windows while preserving the Linux CI paths.
