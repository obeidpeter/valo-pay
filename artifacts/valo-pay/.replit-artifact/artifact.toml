kind = "web"
previewPath = "/"
title = "Valo Pay"
description = "Stage 1 collections operations for Nigerian lenders: mandate operations, controlled retries, reconciliation and exceptions, audit records and gate evidence. Observation-first; never holds money."
version = "1.0.0"
id = "artifacts/valo-pay"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21599

[services.development]
run = "pnpm --filter @workspace/valopay run dev"

[services.production]
build = [ "bash", "scripts/deployment-node.sh", "build", "pnpm", "--filter", "@workspace/valopay", "run", "build" ]
serve = "static"
publicDir = "artifacts/valo-pay/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21599"
BASE_PATH = "/"
