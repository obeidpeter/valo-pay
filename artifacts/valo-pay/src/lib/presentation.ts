/** The sample pack's customer and instalment references (see presentationSamples). */
export const PRESENTATION_CUSTOMER = 'PRES-C001';
export const PRESENTATION_INSTALMENT = 'PRES-D001';

/** Presenter guidance describes existing workflows; it is never operational evidence. */
export const presentationSteps = [
  { title: 'Start with the work', time: '45 sec', href: '/overview', action: 'Open overview', value: 'Know what needs attention before the day begins.', show: 'Name the selected sample lender. Point out outstanding obligations, matches to review and open exceptions.', say: 'Valo Pay brings payment evidence and the work around it into one place for a lender’s Operations and Finance teams.', fallback: 'Use the figures currently on screen. These are synthetic records, not customer traction or measured savings.' },
  { title: 'Bring in payment evidence', time: '60 sec', href: '/imports', action: 'Open import batches', value: 'Check records before they affect the books.', show: 'Open the prepared payment batch. Show the naira amount and row checks before committing it once.', say: 'A file is checked and saved with its source. An error can be corrected before the batch is committed.', fallback: 'If the file was already committed, show its saved result and provenance. Do not change source identities just to import it again.' },
  // The sample payment names its instalment and equals it, so rule R1 matches it automatically and with certainty: it is never
  // in Matches to review. Its match and explanation are in the sample customer's history, which the guide opens once imported.
  { title: 'Explain the match', time: '90 sec', href: `/customers?q=${PRESENTATION_CUSTOMER}`, customer: PRESENTATION_CUSTOMER, action: 'Open the sample customer', value: 'Connect a receipt to the obligation it covers.', show: `After committing the payment file, run reconciliation on the Reconciliation page if your role permits it. Then open the sample customer ${PRESENTATION_CUSTOMER} and its history. The payment names instalment ${PRESENTATION_INSTALMENT} and the amounts are equal, so rule R1 matched them automatically and with certainty. The history shows the match with its rule and explanation. It never appears in Matches to review, which holds only matches Finance must confirm.`, say: 'When a payment names the instalment it pays and the amounts agree, the match is made automatically and recorded with its reason. A less certain match waits for Finance to confirm it instead of being accepted silently.', fallback: `If the customer is not found, the sample files have not been imported for this lender: import them first. If the payment is still unallocated, reconciliation has not run since the import; run it. To show a match that waits for Finance, open Matches to review in Reconciliation and say it is a seeded payment, not the sample one. If access is refused, explain the role requirement; do not change permissions for the presentation.` },
  { title: 'Give an exception an owner', time: '60 sec', href: '/exceptions', action: 'Open exceptions', value: 'Turn an unresolved item into accountable work.', show: 'Choose a separate open sample exception. Show the customer, owner and deadline, then open its case history.', say: 'When something needs investigation, the next person can see the context, the next action and the handover history.', fallback: 'If no exceptions are open, show a resolved case. Recording a resolution does not execute a payment or establish missing evidence.' },
  { title: 'Review the day’s close', time: '60 sec', href: '/reports#daily-closes', action: 'Open daily closes', value: 'Separate a recorded close from an independently reviewed close.', show: 'Open a rehearsed daily close and its source coverage. Use Close review to show what Finance accepted or returned.', say: 'A close freezes the evidence available at that time. Missing sources remain visible, and review is a separate decision.', fallback: 'A missing source or pending review is a useful control to explain. Never describe it as complete. A second demo role alone does not satisfy independent approval.' },
  { title: 'Leave with the evidence', time: '45 sec', href: '/exports', action: 'Open saved exports', value: 'Make the work inspectable after the meeting.', show: 'Open a prepared export for this lender. Show its saved status and download the ready file. Use a customer timeline if you want to start a new dispute pack.', say: 'Evidence can be retrieved after the session. Its saved status shows whether generation finished or needs recovery.', fallback: 'If an export is still running, show its actual status and use the previously downloaded sample file. Do not describe a queued file as ready.' },
] as const;

export const presentationChecks = [
  { id: 'build', label: 'I opened the intended build and checked the pages I will show.' },
  { id: 'lender', label: 'I chose one sample lender and checked my actual permissions.' },
  { id: 'records', label: 'I rehearsed the import, match and case I will use.' },
  { id: 'close', label: 'I checked the close’s actual source coverage and review status.' },
  { id: 'backup', label: 'I downloaded a sample export and prepared a backup recording or screenshots.' },
  { id: 'claims', label: 'I will identify sample figures and explain the outstanding live dependencies.' },
] as const;

export function presentationSamples(date: string) {
  return [
    { kind: 'Customers', filename: '01-presentation-customers.csv', csv: 'source_row_id,name,reference,consentProvenance,bankName,accountMasked\npres-c001,Presentation customer,PRES-C001,Synthetic presentation consent,Sandbox Bank,•••• 0001' },
    { kind: 'Instalments', filename: '02-presentation-instalments.csv', csv: `source_row_id,name,reference,customerId,amount,dueDate,owner\npres-d001,Presentation instalment,PRES-D001,PRES-C001,18000.50,${date},lms` },
    { kind: 'Payment evidence', filename: '03-presentation-payment.csv', csv: 'source_row_id,name,reference,customerId,amount,source,dueItemId,narration\npres-o001,Presentation payment,PRES-O001,PRES-C001,18000.50,statement,PRES-D001,PRES-D001 synthetic transfer' },
  ];
}

export function presenterBrief(date: string): string {
  return `# Valo Pay presenter brief\n\nPrepared for ${date} (WAT). Suggested demonstration: six minutes.\n\n## Opening\n\nValo Pay helps lender teams connect payment evidence, resolve exceptions and review the daily close. This demonstration uses synthetic records; no money moves.\n\n## Before the meeting\n\n${presentationChecks.map(c => `- [ ] ${c.label}`).join('\n')}\n\n## Prepare the sample import\n\nUse one sample lender in an isolated rehearsal workspace. Import Customers, then Instalments before the meeting; import Payment evidence during the demonstration. In Import batches choose the matching record type, choose Naira, use source name “Presentation sample”, source row column “source_row_id” and a distinct file reference for each CSV. Use the pack’s date (${date}) as its business date. Save and check each batch, inspect the amount, then commit once. Expected instalment and receipt: exactly ₦18,000.50 each. Reconciliation is a separate action. Once it runs, rule R1 matches the payment to ${PRESENTATION_INSTALMENT} automatically and with certainty, because the payment names that instalment and the amounts are equal. The match never appears in Matches to review; its rule and explanation are in the sample customer’s history (Customers, search ${PRESENTATION_CUSTOMER}, View history).\n\nStable source row IDs protect repeat imports; repeating the same file is not a reset. For a fresh rehearsal use a separate anonymous browser profile/private session or a new empty synthetic lender, with appropriate access. A new empty lender has no seeded exceptions: prepare a case or demonstrate the absence honestly. Never clear an existing workspace.\n\n## Demonstration\n\n${presentationSteps.map((s,i) => `### ${i + 1}. ${s.title} · ${s.time}\n\nOpen: ${s.href}\n\nShow: ${s.show}\n\nSay: “${s.say}”\n\nIf needed: ${s.fallback}`).join('\n\n')}\n\n## Questions to prepare for\n\n- Who is it for? Start with lender Operations and Finance teams handling payment reconciliation and exceptions.\n- What is working? Demonstrate the saved records, checks, decisions and exports actually available in this build.\n- Is Paystack connected? The test adapter and local scenarios are implemented. An external connection has not been verified; do not claim payment acceptance or Direct Debit availability.\n- Is it production-ready? Real staff identity/MFA, managed keys, restricted database access, recovery and external services still require host verification and the agreed acceptance process.\n- Does this prove traction or recovery uplift? No. Bring separately verified customer conversations, agreements and measured pilot results, if available.\n- What will investment enable? Explain your actual hiring, commissioning and pilot plan. Do not invent an amount, timeline, customer count or return.\n\n## If something fails\n\nExplain the actual state. For an uncertain write, inspect Operations before resubmitting; use the existing recovery action. For a pending export, inspect Saved exports. Continue with a prepared screenshot or recording and label it as recorded. Do not disable controls to finish the story.\n\n## Closing\n\nThe next milestone is a controlled lender pilot that measures operator completion, errors and time to close. Ask for feedback on the workflow and introductions to relevant lender teams.\n`;
}

export function downloadPresentationFile(filename: string, content: string, csv = false) {
  const url = URL.createObjectURL(new Blob([csv ? '\uFEFF' : '', content], { type: csv ? 'text/csv;charset=utf-8' : 'text/markdown;charset=utf-8' }));
  const anchor = document.createElement('a');
  anchor.href = url;
  anchor.download = filename;
  document.body.append(anchor);
  anchor.click();
  anchor.remove();
  window.setTimeout(() => URL.revokeObjectURL(url), 1000);
}
