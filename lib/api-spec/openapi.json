{
  "openapi": "3.1.0",
  "info": {
    "title": "Valo Pay sandbox API",
    "version": "1.1.0",
    "description": "Valo Pay collections and connected banking sandbox API. All monetary fields are integer minor units (NGN kobo). Real data and all outbound provider instructions are disabled in connected modules."
  },
  "servers": [
    {
      "url": "/api"
    }
  ],
  "paths": {
    "/healthz": {
      "get": {
        "operationId": "healthCheck",
        "tags": [
          "valopay"
        ],
        "parameters": [],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HealthStatus"
                }
              }
            }
          },
          "429": {
            "description": "More than 120 health checks a minute from this client network; retry after the Retry-After seconds.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60, the health limit's window.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service could not prepare this answer; a read changes nothing, so it can be tried again.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          }
        },
        "summary": "Liveness: the process answers, with its build, uptime and scheduler state",
        "description": "Never touches the database, so a database outage does not read as a dead process. Needs no sandbox or sign-in, and answers whether or not Clerk is configured. At most 120 health checks a minute per client network, both health addresses together (an IPv6 client's network is its /64)."
      }
    },
    "/readyz": {
      "get": {
        "operationId": "readinessCheck",
        "tags": [
          "valopay"
        ],
        "parameters": [],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ReadinessStatus"
                }
              }
            }
          },
          "429": {
            "description": "More than 120 health checks a minute from this client network; retry after the Retry-After seconds.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60, the health limit's window.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service could not prepare this answer; a read changes nothing, so it can be tried again.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "503": {
            "description": "Not ready: the database cannot be reached within the check's time limit, or lacks a table, column, unique index or check constraint this build needs",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ReadinessStatus"
                }
              }
            }
          }
        },
        "summary": "Readiness: one bounded round trip to the database, which also checks its schema",
        "description": "Answers 503 with status degraded while the database does not answer within the check's time limit, or lacks a table, column, unique index or check constraint this build needs. A missing read index leaves the answer ready, with checks.schema.status indexes_missing, since every request still works, only slower. The log names what is missing and where it comes from, and any connection error; the answer does not. Probes share one check: while it runs every probe waits for it, and its answer is reused for a second after it finishes, so a burst makes one database round trip. Needs no sandbox or sign-in, and answers whether or not Clerk is configured. At most 120 health checks a minute per client network, both health addresses together."
      }
    },
    "/v1/workspace": {
      "get": {
        "operationId": "getWorkspace",
        "tags": [
          "valopay"
        ],
        "parameters": [],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Workspace"
                }
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service could not prepare this answer; a read changes nothing, so it can be tried again.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          }
        },
        "summary": "The caller's workspace: its lenders, roles and actor",
        "description": "On a first visit an anonymous caller gets a new synthetic sandbox with two lenders, and the sandbox cookie that names it on every later request (components.securitySchemes.sandboxCookie); a signed-in person gets their own workspace. New sandboxes are limited per client network (20 an hour; an IPv6 client's network is its /64, and a /48 starts at most 60) and per server (300 an hour, which hold back only a network that has already started one in its hour: a network's first is never refused because of others). A browser that sends two different sandbox cookies is refused (400) rather than guessed between."
      }
    },
    "/v1/overview": {
      "get": {
        "operationId": "getOverview",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Overview"
                }
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service could not prepare this answer; a read changes nothing, so it can be tried again.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          }
        },
        "summary": "The operations overview for one lender",
        "description": "Metrics, queues, recent activity (the eight latest audit entries), upcoming due items, the last and next daily close, and the alerts feed (NFR-OBS-02), whose audit check covers the entries since the last one verified."
      }
    },
    "/v1/records/{kind}": {
      "get": {
        "operationId": "listRecords",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "kind",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string"
            },
            "description": "Record kind: one of the shared schema's recordKinds (customers, mandates, due-items, attempts, observations, payments, ...)."
          },
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          },
          {
            "name": "search",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "Text matched, ignoring case and accents, against the record's name, its reference and the text and number values in its data, nested ones included; never a field's name, true, false or null."
          },
          {
            "name": "status",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "Only records in this status; omitted or \"all\" for every status."
          },
          {
            "name": "limit",
            "in": "query",
            "schema": {
              "type": "integer",
              "minimum": 1,
              "maximum": 500
            },
            "description": "Page size, from 1 to 500; a value outside that range is refused (400). Omitted, a kind that grows with history (audit, closes, exports, notifications, retry-decisions) returns its newest 500 with nextOffset to page on, and any other kind its whole filtered set, for existing relationship and balance views."
          },
          {
            "name": "offset",
            "in": "query",
            "schema": {
              "type": "integer",
              "minimum": 0
            },
            "description": "Rows to skip in the newest-first order."
          },
          {
            "name": "updatedSince",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "An RFC 3339 date and time with Z or an offset, such as 2026-09-18T08:00:00+01:00; only records updated at or after that instant (incremental sync). A number, a date without a time, a time without Z or an offset, or a year outside 0001 to 9999 is refused (400, naming updatedSince)."
          },
          {
            "name": "customerId",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "Only records directly linked to this customer, in the selected lender."
          },
          {
            "name": "id",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "Only this exact record ID, in the selected kind and lender."
          },
          {
            "name": "allocatable",
            "in": "query",
            "schema": {
              "type": "string",
              "enum": [
                "true",
                "false"
              ]
            },
            "description": "Instalments (due-items) only. true lists just the instalments that can take an allocation now: those that still owe an amount and are not cancelled, closed or in dispute, the ones a manual allocation accepts, so total counts the choices. Omitted or false lists every instalment. Refused (400) for any other kind."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RecordList"
                }
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service could not prepare this answer; a read changes nothing, so it can be tried again.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          }
        },
        "summary": "Records of one kind for one lender, newest first",
        "description": "Filtered by status and by a search that ignores case and accents; paged with limit and offset; updatedSince for incremental sync; allocatable for the instalments a manual allocation accepts."
      },
      "post": {
        "operationId": "createRecord",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "kind",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string"
            },
            "description": "Record kind: one of the shared schema's recordKinds (customers, mandates, due-items, attempts, observations, payments, ...)."
          },
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": false,
            "schema": {
              "type": "string",
              "minLength": 8,
              "maxLength": 200
            },
            "description": "Optional: without one the write still runs, but a lost answer cannot be recovered and a repeat may apply twice. With one, the request is journaled in Operations and repeatable. 8 to 200 characters, one per unchanged intention. The same key with different input is refused (409). A key whose request was refused cannot run again: its journal entry is closed. A repeat after a lost answer returns the original result, checked before the version; once the lender's retention policy has removed that stored result, the repeat is refused (410). A repeat while the request is still running is answered 503 with Retry-After and operation running, and leaves it to finish. The result is kept with the request's journal entry, so a key names one request of the person who sent it, in its lender."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ValopayRecord"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "410": {
            "description": "Gone: a request with this Idempotency-Key already completed, and the lender's retention policy has since removed its stored result, so it cannot run again. Its entry in Operations remains.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "413": {
            "description": "The body is larger than 2 MB, or holds more than 100,000 values (every object, list, text, number, true, false and null in it).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "415": {
            "description": "The body is not JSON (a form or text body is refused), or its character set or encoding is not supported; send UTF-8 JSON as application/json.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "500": {
            "description": "The service failed. With committed false nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); with operation completed a request with the key was saved; otherwise the outcome is unconfirmed: check Operations, or repeat the same request with its Idempotency-Key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          }
        },
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RecordInput"
              }
            }
          }
        },
        "summary": "Create a record of an editable kind",
        "description": "Validated against the kind's data schema; a status only a domain action may set is refused."
      }
    },
    "/v1/records/{kind}/{id}": {
      "patch": {
        "operationId": "updateRecord",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "kind",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string"
            },
            "description": "Record kind: one of the shared schema's recordKinds (customers, mandates, due-items, attempts, observations, payments, ...)."
          },
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The record's id."
          },
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": false,
            "schema": {
              "type": "string",
              "minLength": 8,
              "maxLength": 200
            },
            "description": "Optional: without one the write still runs, but a lost answer cannot be recovered and a repeat may apply twice. With one, the request is journaled in Operations and repeatable. 8 to 200 characters, one per unchanged intention. The same key with different input is refused (409). A key whose request was refused cannot run again: its journal entry is closed. A repeat after a lost answer returns the original result, checked before the version; once the lender's retention policy has removed that stored result, the repeat is refused (410). A repeat while the request is still running is answered 503 with Retry-After and operation running, and leaves it to finish. The result is kept with the request's journal entry, so a key names one request of the person who sent it, in its lender."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ValopayRecord"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "410": {
            "description": "Gone: a request with this Idempotency-Key already completed, and the lender's retention policy has since removed its stored result, so it cannot run again. Its entry in Operations remains.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "413": {
            "description": "The body is larger than 2 MB, or holds more than 100,000 values (every object, list, text, number, true, false and null in it).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "415": {
            "description": "The body is not JSON (a form or text body is refused), or its character set or encoding is not supported; send UTF-8 JSON as application/json.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "500": {
            "description": "The service failed. With committed false nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); with operation completed a request with the key was saved; otherwise the outcome is unconfirmed: check Operations, or repeat the same request with its Idempotency-Key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          }
        },
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RecordUpdate"
              }
            }
          }
        },
        "summary": "Update a record",
        "description": "Editable kinds only; an approved, preregistered or closed version is immutable. data is merged over the stored data as a merge patch: a field left out keeps its value and a field sent as null is removed, which is how an edit clears an optional field. Send expectedUpdatedAt from the edit's original record to reject stale changes with 409. An identical successful Idempotency-Key replay returns its original result before checking the version."
      }
    },
    "/v1/actions": {
      "post": {
        "operationId": "performAction",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": false,
            "schema": {
              "type": "string",
              "minLength": 8,
              "maxLength": 200
            },
            "description": "Optional: without one the write still runs, but a lost answer cannot be recovered and a repeat may apply twice. With one, the request is journaled in Operations and repeatable. 8 to 200 characters, one per unchanged intention. The same key with different input is refused (409). A key whose request was refused cannot run again: its journal entry is closed. A repeat after a lost answer returns the original result, checked before the version; once the lender's retention policy has removed that stored result, the repeat is refused (410). A repeat while the request is still running is answered 503 with Retry-After and operation running, and leaves it to finish. The result is kept with the request's journal entry, so a key names one request of the person who sent it, in its lender. A set_role change is repeatable with its key but is not recorded in Operations, so a refused one may be sent again and retention never removes its result. Its result is kept apart from the journal's, so a journaled write sent with the same key is a separate request."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ActionResult"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "410": {
            "description": "Gone: a request with this Idempotency-Key already completed, and the lender's retention policy has since removed its stored result, so it cannot run again. Its entry in Operations remains.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "413": {
            "description": "The body is larger than 2 MB, or holds more than 100,000 values (every object, list, text, number, true, false and null in it).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "415": {
            "description": "The body is not JSON (a form or text body is refused), or its character set or encoding is not supported; send UTF-8 JSON as application/json.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "500": {
            "description": "The service failed. With committed false nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); with operation completed a request with the key was saved; otherwise the outcome is unconfirmed: check Operations, or repeat the same request with its Idempotency-Key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          }
        },
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ActionInput"
              }
            }
          }
        },
        "summary": "Run a domain action on the lender's state",
        "description": "Every action is audited, most require a reason, and the persona's role applies; the catalogue of actions is in docs/frontend-contract.md."
      }
    },
    "/v1/imports": {
      "post": {
        "operationId": "importRecords",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": false,
            "schema": {
              "type": "string",
              "minLength": 8,
              "maxLength": 200
            },
            "description": "Optional: without one the write still runs, but a lost answer cannot be recovered and a repeat may apply twice. With one, the request is journaled in Operations and repeatable. 8 to 200 characters, one per unchanged intention. The same key with different input is refused (409). A key whose request was refused cannot run again: its journal entry is closed. A repeat after a lost answer returns the original result, checked before the version; once the lender's retention policy has removed that stored result, the repeat is refused (410). A repeat while the request is still running is answered 503 with Retry-After and operation running, and leaves it to finish. The result is kept with the request's journal entry, so a key names one request of the person who sent it, in its lender. Only a commit (commit true) uses it: a preview writes nothing."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ImportResult"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "410": {
            "description": "Gone: a request with this Idempotency-Key already completed, and the lender's retention policy has since removed its stored result, so it cannot run again. Its entry in Operations remains.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "413": {
            "description": "The body is larger than 2 MB, or holds more than 100,000 values (every object, list, text, number, true, false and null in it).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "415": {
            "description": "The body is not JSON (a form or text body is refused), or its character set or encoding is not supported; send UTF-8 JSON as application/json.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "500": {
            "description": "The service failed. With committed false nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); with operation completed a request with the key was saved; otherwise the outcome is unconfirmed: check Operations, or repeat the same request with its Idempotency-Key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          }
        },
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ImportInput"
              }
            }
          }
        },
        "summary": "Preview or commit a synthetic CSV import",
        "description": "commit=false validates every row and reports each; commit=true persists all rows or none. syntheticOnly must be true: no real lender data."
      }
    },
    "/v1/customers/{id}/timeline": {
      "get": {
        "operationId": "getCustomerTimeline",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The record's id."
          },
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Timeline"
                }
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service could not prepare this answer; a read changes nothing, so it can be tried again.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          }
        },
        "summary": "A customer's position and complete timeline",
        "description": "Every event, mandate, due item and payment, with each retry decision as it was recorded."
      }
    },
    "/v1/reports": {
      "get": {
        "operationId": "getReports",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          },
          {
            "name": "includeCloses",
            "in": "query",
            "schema": {
              "type": "string",
              "enum": [
                "true",
                "false"
              ]
            },
            "description": "Default true for compatibility: the close array, where closes more than a week before the latest carry their summary (GET /v1/close-history/{id} returns any close whole). The console passes false and loads paged close summaries separately."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Report"
                }
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service could not prepare this answer; a read changes nothing, so it can be tried again.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          }
        },
        "summary": "Reports for one lender",
        "description": "Metrics, the billing statement and invoices, the recovery experiment, operational measurement (Test 5) and the daily closes with their REC-07 reports."
      }
    },
    "/v1/gates": {
      "get": {
        "operationId": "getGates",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Gates"
                }
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service could not prepare this answer; a read changes nothing, so it can be tried again.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          }
        },
        "summary": "Production readiness gates",
        "description": "Prerequisites and decisions, always unproven on synthetic data, and the limitations the sandbox cannot remove."
      }
    },
    "/v1/settings": {
      "get": {
        "operationId": "getSettings",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Settings"
                }
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service could not prepare this answer; a read changes nothing, so it can be tried again.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          }
        },
        "summary": "A lender's settings, permissions, integrations, members and calendar",
        "description": "Permissions are those of the caller's current persona."
      },
      "patch": {
        "operationId": "updateSettings",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": false,
            "schema": {
              "type": "string",
              "minLength": 8,
              "maxLength": 200
            },
            "description": "Optional: without one the write still runs, but a lost answer cannot be recovered and a repeat may apply twice. With one, the request is journaled in Operations and repeatable. 8 to 200 characters, one per unchanged intention. The same key with different input is refused (409). A key whose request was refused cannot run again: its journal entry is closed. A repeat after a lost answer returns the original result, checked before the version; once the lender's retention policy has removed that stored result, the repeat is refused (410). A repeat while the request is still running is answered 503 with Retry-After and operation running, and leaves it to finish. The result is kept with the request's journal entry, so a key names one request of the person who sent it, in its lender."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Settings"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "410": {
            "description": "Gone: a request with this Idempotency-Key already completed, and the lender's retention policy has since removed its stored result, so it cannot run again. Its entry in Operations remains.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "413": {
            "description": "The body is larger than 2 MB, or holds more than 100,000 values (every object, list, text, number, true, false and null in it).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "415": {
            "description": "The body is not JSON (a form or text body is refused), or its character set or encoding is not supported; send UTF-8 JSON as application/json.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "500": {
            "description": "The service failed. With committed false nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); with operation completed a request with the key was saved; otherwise the outcome is unconfirmed: check Operations, or repeat the same request with its Idempotency-Key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          }
        },
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SettingsInput"
              }
            }
          }
        },
        "summary": "Change a lender's execution settings",
        "description": "Admin only. Send expectedRevision from the settings originally opened; 409 leaves outdated edits unapplied. The revision covers editable preferences and is unaffected by scheduler cursor changes. An identical successful Idempotency-Key replay returns its original result before checking the version."
      }
    },
    "/v1/exports": {
      "post": {
        "operationId": "createExport",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": false,
            "schema": {
              "type": "string",
              "minLength": 8,
              "maxLength": 200
            },
            "description": "Optional: without one the write still runs, but a lost answer cannot be recovered and a repeat may apply twice. With one, the request is journaled in Operations and repeatable. 8 to 200 characters, one per unchanged intention. The same key with different input is refused (409). A key whose request was refused cannot run again: its journal entry is closed. A repeat after a lost answer returns the original result, checked before the version; once the lender's retention policy has removed that stored result, the repeat is refused (410). A repeat while the request is still running is answered 503 with Retry-After and operation running, and leaves it to finish. The result is kept with the request's journal entry, so a key names one request of the person who sent it, in its lender."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ExportResult"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "410": {
            "description": "Gone: a request with this Idempotency-Key already completed, and the lender's retention policy has since removed its stored result, so it cannot run again. Its entry in Operations remains.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "413": {
            "description": "The body is larger than 2 MB, or holds more than 100,000 values (every object, list, text, number, true, false and null in it).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "415": {
            "description": "The body is not JSON (a form or text body is refused), or its character set or encoding is not supported; send UTF-8 JSON as application/json.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "429": {
            "description": "Too many requests: the lender already has ten exports waiting or running (Retry-After 30, about the time one takes to finish), more than 300 requests came from this client or 1,200 from its network in a minute, or too many new sandboxes came from its network or were started on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit, and 30 when the lender's export queue is full.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "500": {
            "description": "The service failed. With committed false nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); with operation completed a request with the key was saved; otherwise the outcome is unconfirmed: check Operations, or repeat the same request with its Idempotency-Key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          }
        },
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ExportInput"
              }
            }
          }
        },
        "summary": "Queue a private export",
        "description": "Durably saves a queued export job and returns immediately. Poll its status before downloading. Rendering and private storage run outside the database transaction; retries use the same immutable object key. A record kind, gate pack, billing statement or customer dispute pack supports JSON, CSV or PDF. A dispute pack (either name), the customer register or the audit trail is queued only by an Admin, Finance or Compliance reviewer (403 otherwise)."
      }
    },
    "/v1/exports/{id}": {
      "get": {
        "operationId": "getExportJob",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The record's id."
          },
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ExportResult"
                }
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service could not prepare this answer; a read changes nothing, so it can be tried again.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          }
        },
        "summary": "Check a saved export",
        "description": "Tenant-authorised status, safe failure reason and checksum/download details once ready. Older immediate export records remain downloadable."
      }
    },
    "/v1/exports/{id}/retry": {
      "post": {
        "operationId": "retryExportJob",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The record's id."
          },
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": false,
            "schema": {
              "type": "string",
              "minLength": 8,
              "maxLength": 200
            },
            "description": "Optional: without one the write still runs, but a lost answer cannot be recovered and a repeat may apply twice. With one, the request is journaled in Operations and repeatable. 8 to 200 characters, one per unchanged intention. The same key with different input is refused (409). A key whose request was refused cannot run again: its journal entry is closed. A repeat after a lost answer returns the original result, checked before the version; once the lender's retention policy has removed that stored result, the repeat is refused (410). A repeat while the request is still running is answered 503 with Retry-After and operation running, and leaves it to finish. The result is kept with the request's journal entry, so a key names one request of the person who sent it, in its lender."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ExportResult"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "410": {
            "description": "Gone: the lender's retention policy removed this export's file, so it cannot be generated again under the same identity (start a new export); or a request with this Idempotency-Key already completed and retention has since removed its stored result.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "413": {
            "description": "The body is larger than 2 MB, or holds more than 100,000 values (every object, list, text, number, true, false and null in it).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "415": {
            "description": "The body is not JSON (a form or text body is refused), or its character set or encoding is not supported; send UTF-8 JSON as application/json.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "500": {
            "description": "The service failed. With committed false nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); with operation completed a request with the key was saved; otherwise the outcome is unconfirmed: check Operations, or repeat the same request with its Idempotency-Key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          }
        },
        "summary": "Retry a saved export",
        "description": "Requeues a failed or expired job while preserving its identity and private object key. Running and ready jobs are returned unchanged; retries cannot overwrite a completed file. A dispute pack, the customer register or the audit trail is retried only by an Admin, Finance or Compliance reviewer (403 otherwise)."
      }
    },
    "/v1/exports/{id}/download": {
      "get": {
        "operationId": "downloadExport",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The record's id."
          },
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          }
        ],
        "responses": {
          "200": {
            "description": "Private verified export bytes",
            "content": {
              "application/octet-stream": {
                "schema": {
                  "type": "string",
                  "format": "binary"
                }
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "404": {
            "description": "Export not found in tenant",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "410": {
            "description": "Gone: the lender's retention policy removed this export's file. Its checksum and deletion receipt are kept; start a new export if current evidence is needed.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service could not prepare this answer; a read changes nothing, so it can be tried again.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "502": {
            "description": "Private storage answered with an error; nothing was sent.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "504": {
            "description": "Private storage did not answer in time; nothing was sent.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          }
        },
        "summary": "Download an export",
        "description": "The bytes are read from private storage and checked against the recorded SHA-256 before any are sent. A dispute pack (either name), the customer register or the audit trail is downloaded only by an Admin, Finance or Compliance reviewer (403 otherwise); every role may read a job's status."
      }
    },
    "/v1/openapi.json": {
      "get": {
        "operationId": "getOpenApiDocument",
        "tags": [
          "valopay"
        ],
        "responses": {
          "200": {
            "description": "Versioned public API specification",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "additionalProperties": true
                }
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service could not prepare this answer; a read changes nothing, so it can be tried again.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          }
        },
        "summary": "This specification",
        "description": "The versioned public contract the console and the generated clients are built from."
      }
    },
    "/v1/webhooks/{provider}": {
      "post": {
        "operationId": "disabledProviderWebhook",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "provider",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string"
            },
            "description": "Provider name; this generic address refuses every provider (the Paystack test ingress has its own address)."
          }
        ],
        "responses": {
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "403": {
            "description": "Disabled until a provider-specific signed adapter is configured. No events are processed.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "413": {
            "description": "The body is larger than 2 MB, or holds more than 100,000 values (every object, list, text, number, true, false and null in it).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "415": {
            "description": "The body is not JSON (a form or text body is refused), or its character set or encoding is not supported; send UTF-8 JSON as application/json.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service failed. With committed false nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); with operation completed a request with the key was saved; otherwise the outcome is unconfirmed: check Operations, or repeat the same request with its Idempotency-Key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          }
        },
        "summary": "Generic provider webhook address, always refused",
        "description": "Always 403: no event is processed here. Paystack test events go to POST /v1/providers/paystack/{connectionId}/events."
      }
    },
    "/v1/queues/{queue}": {
      "get": {
        "operationId": "listQueue",
        "tags": [
          "valopay"
        ],
        "summary": "A priority-sorted, lender-scoped queue with complete filter counts and page-specific linked records",
        "parameters": [
          {
            "name": "queue",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "enum": [
                "exceptions",
                "mandates",
                "collections"
              ]
            },
            "description": "Priority queue to read: exceptions, mandates or collections."
          },
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The active lender, belonging to the caller’s workspace. Missing or empty, the request is refused with 400 naming merchantId."
          },
          {
            "name": "view",
            "in": "query",
            "required": false,
            "schema": {
              "type": "string",
              "maxLength": 200
            },
            "description": "A supported view for the queue. Defaults to open for exceptions and all for mandates and collections."
          },
          {
            "name": "owner",
            "in": "query",
            "required": false,
            "schema": {
              "type": "string",
              "maxLength": 200
            },
            "description": "Exact owner filter. Omit for every owner."
          },
          {
            "name": "type",
            "in": "query",
            "required": false,
            "schema": {
              "type": "string",
              "maxLength": 200
            },
            "description": "Exact exception type filter. Omit for every type."
          },
          {
            "name": "record",
            "in": "query",
            "required": false,
            "schema": {
              "type": "string",
              "maxLength": 200
            },
            "description": "Select this exact record in the queue instead of applying its view, within the active lender."
          },
          {
            "name": "target",
            "in": "query",
            "required": false,
            "schema": {
              "type": "string",
              "maxLength": 200
            },
            "description": "Locate the page containing this record among the filtered results. Does not bypass filters."
          },
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "minimum": 1,
              "maximum": 100
            },
            "description": "Page size, default 25 and maximum 100."
          },
          {
            "name": "offset",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "minimum": 0,
              "maximum": 2147483647
            },
            "description": "Rows to skip after filtering and priority ordering. Clamped to the last available page if results shrink."
          },
          {
            "name": "q",
            "in": "query",
            "schema": {
              "type": "string",
              "maxLength": 200
            },
            "description": "Literal accent-insensitive customer name, customer reference or queue record name/reference search, applied before counting and paging."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/QueuePage"
                }
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service could not prepare this answer; a read changes nothing, so it can be tried again.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          }
        },
        "description": "Filters and priority order are applied before pagination. Counts cover the full filtered queue. Related records only support the current page and remain in the same lender. A target locates the page containing a linked record; an unavailable or filtered-out target leaves the requested page unchanged. Dates use West Africa Time and the returned asOf timestamp."
      }
    },
    "/v1/reconciliation/{queue}": {
      "get": {
        "operationId": "listReconciliation",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          },
          {
            "name": "queue",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "enum": [
                "proposals",
                "duplicates",
                "payments",
                "observations",
                "audit",
                "batches"
              ]
            },
            "description": "Reconciliation work queue."
          },
          {
            "name": "dueItem",
            "in": "query",
            "schema": {
              "type": "string",
              "maxLength": 200
            },
            "description": "Optional instalment focus; payment queues are restricted to its customer, proposals to its exact instalment."
          },
          {
            "name": "limit",
            "in": "query",
            "schema": {
              "type": "integer",
              "minimum": 1,
              "maximum": 100
            },
            "description": "Page size; defaults to 25."
          },
          {
            "name": "offset",
            "in": "query",
            "schema": {
              "type": "integer",
              "minimum": 0,
              "maximum": 2147483647
            },
            "description": "Rows to skip; clamped when the result shrinks."
          },
          {
            "name": "q",
            "in": "query",
            "schema": {
              "type": "string",
              "maxLength": 200
            },
            "description": "Literal case- and accent-insensitive customer, payment or instalment name/reference search before counting and paging. Audit sample metadata remains unfiltered."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ReconciliationPage"
                }
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service could not prepare this answer; a read changes nothing, so it can be tried again.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          }
        },
        "summary": "Page a reconciliation work queue",
        "description": "Filters and counts in PostgreSQL before paging. Linked payment, instalment and customer records belong to the same lender. Audit uses the reproducible previous-month sample including superseded reviewed matches."
      }
    },
    "/v1/close-history": {
      "get": {
        "operationId": "listCloseHistory",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          },
          {
            "name": "from",
            "in": "query",
            "schema": {
              "type": "string",
              "maxLength": 10
            },
            "description": "Inclusive date in YYYY-MM-DD format, in West Africa Time."
          },
          {
            "name": "to",
            "in": "query",
            "schema": {
              "type": "string",
              "maxLength": 10
            },
            "description": "Inclusive date in YYYY-MM-DD format, in West Africa Time."
          },
          {
            "name": "limit",
            "in": "query",
            "schema": {
              "type": "integer",
              "minimum": 1,
              "maximum": 100
            },
            "description": "Page size; defaults to 25."
          },
          {
            "name": "offset",
            "in": "query",
            "schema": {
              "type": "integer",
              "minimum": 0,
              "maximum": 2147483647
            },
            "description": "Rows to skip; clamped when the result shrinks."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CloseHistoryPage"
                }
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service could not prepare this answer; a read changes nothing, so it can be tried again.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          }
        },
        "summary": "Page recorded daily closes",
        "description": "Newest first, with complete range counts and whole-range comparison endpoints. Missing historical measures remain absent. Invalid dates or reversed ranges are rejected."
      }
    },
    "/v1/close-history/{id}": {
      "get": {
        "operationId": "getCloseDetail",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          },
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "Close record in the active lender."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ValopayRecord"
                }
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service could not prepare this answer; a read changes nothing, so it can be tried again.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          }
        },
        "summary": "Read the evidence for one recorded close",
        "description": "Returns the full immutable close report on demand within the current lender."
      }
    },
    "/v1/customers/{id}/history": {
      "get": {
        "operationId": "getCustomerHistory",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          },
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "Customer in the active lender."
          },
          {
            "name": "record",
            "in": "query",
            "schema": {
              "type": "string",
              "maxLength": 200
            },
            "description": "Optional selected history record; must belong to this customer and lender."
          },
          {
            "name": "eventsLimit",
            "in": "query",
            "schema": {
              "type": "integer",
              "minimum": 1,
              "maximum": 100
            },
            "description": "Page size; defaults to 25."
          },
          {
            "name": "eventsOffset",
            "in": "query",
            "schema": {
              "type": "integer",
              "minimum": 0,
              "maximum": 2147483647
            },
            "description": "Rows to skip; clamped when the result shrinks."
          },
          {
            "name": "mandatesLimit",
            "in": "query",
            "schema": {
              "type": "integer",
              "minimum": 1,
              "maximum": 100
            },
            "description": "Page size; defaults to 25."
          },
          {
            "name": "mandatesOffset",
            "in": "query",
            "schema": {
              "type": "integer",
              "minimum": 0,
              "maximum": 2147483647
            },
            "description": "Rows to skip; clamped when the result shrinks."
          },
          {
            "name": "dueItemsLimit",
            "in": "query",
            "schema": {
              "type": "integer",
              "minimum": 1,
              "maximum": 100
            },
            "description": "Page size; defaults to 25."
          },
          {
            "name": "dueItemsOffset",
            "in": "query",
            "schema": {
              "type": "integer",
              "minimum": 0,
              "maximum": 2147483647
            },
            "description": "Rows to skip; clamped when the result shrinks."
          },
          {
            "name": "paymentsLimit",
            "in": "query",
            "schema": {
              "type": "integer",
              "minimum": 1,
              "maximum": 100
            },
            "description": "Page size; defaults to 25."
          },
          {
            "name": "paymentsOffset",
            "in": "query",
            "schema": {
              "type": "integer",
              "minimum": 0,
              "maximum": 2147483647
            },
            "description": "Rows to skip; clamped when the result shrinks."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CustomerHistory"
                }
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service could not prepare this answer; a read changes nothing, so it can be tried again.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          }
        },
        "summary": "Page a customer history with complete balances",
        "description": "Each section is independently paged in SQL, newest first with stable ID ordering. Counts and monetary aggregates are calculated before paging; no partial state may be written. Unknown customers return 404. The existing timeline endpoint retains its full-history contract."
      }
    },
    "/v1/connected": {
      "get": {
        "operationId": "getConnectedWorkspace",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ConnectedWorkspace"
                }
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service could not prepare this answer; a read changes nothing, so it can be tried again.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          }
        },
        "summary": "Read the connected workspace",
        "description": "Same-origin, private and tenant scoped. Returns granular consent state, bound sample payment intents, explained credit assessments, independent SME cash planning and closed live gates."
      }
    },
    "/v1/connected/actions": {
      "post": {
        "operationId": "performConnectedAction",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 8,
              "maxLength": 200
            },
            "description": "Required: a request without one is refused (400, naming the header). 8 to 200 characters, one per unchanged intention. The same key with different input is refused (409). A key whose request was refused cannot run again: its journal entry is closed. A repeat after a lost answer returns the original result, checked before the version; once the lender's retention policy has removed that stored result, the repeat is refused (410). A repeat while the request is still running is answered 503 with Retry-After and operation running, and leaves it to finish. The result is kept with the request's journal entry, so a key names one request of the person who sent it, in its lender."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ConnectedActionResult"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "410": {
            "description": "Gone: a request with this Idempotency-Key already completed, and the lender's retention policy has since removed its stored result, so it cannot run again. Its entry in Operations remains.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "413": {
            "description": "The body is larger than 2 MB, or holds more than 100,000 values (every object, list, text, number, true, false and null in it).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "415": {
            "description": "The body is not JSON (a form or text body is refused), or its character set or encoding is not supported; send UTF-8 JSON as application/json.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "500": {
            "description": "The service failed. With committed false nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); with operation completed a request with the key was saved; otherwise the outcome is unconfirmed: check Operations, or repeat the same request with its Idempotency-Key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          }
        },
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ConnectedActionInput"
              }
            }
          }
        },
        "summary": "Perform a synthetic connected-workspace action",
        "description": "Runs inside the existing merchant transaction and audit boundary. Role, purpose, subject, expiry, ownership and version checks apply. Unknown payment outcomes hold retries. A repeat with the same key is answered before the revision is checked. No bank, credit bureau, accounting or tax endpoint is called."
      }
    },
    "/v1/operations": {
      "get": {
        "operationId": "listOperations",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          },
          {
            "name": "offset",
            "in": "query",
            "schema": {
              "type": "integer",
              "minimum": 0,
              "maximum": 100000
            },
            "description": "Rows to skip in the newest-first order; pages hold 25 rows."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/OperationList"
                }
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service could not prepare this answer; a read changes nothing, so it can be tried again.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          }
        },
        "summary": "List the caller's recovery journal",
        "description": "Every keyed, recoverable request the caller made in this lender, with its confirmation state. Read-only; private to the person who made the requests."
      }
    },
    "/v1/operations/{id}/retry": {
      "post": {
        "operationId": "retryOperation",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The record's id."
          },
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/OperationReplayResult"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "410": {
            "description": "Gone: the stored request expired under the lender's retention policy and cannot run again; or the request it repeats is gone itself, as an export whose file retention deleted is.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "413": {
            "description": "The body is larger than 2 MB, or holds more than 100,000 values (every object, list, text, number, true, false and null in it).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "415": {
            "description": "The body is not JSON (a form or text body is refused), or its character set or encoding is not supported; send UTF-8 JSON as application/json.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour), or, repeating an export, the lender already has ten exports waiting or running (Retry-After 30, about the time one takes to finish).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit, and 30 when the lender's export queue is full.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "500": {
            "description": "The service failed. With committed false nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); with operation completed a request with the key was saved; otherwise the outcome is unconfirmed: check Operations, or repeat the same request with its Idempotency-Key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          }
        },
        "summary": "Recover or repeat a journaled request",
        "description": "Re-enters the original route with the stored request and key under the current rules, so it can answer anything that route answers. A completed entry returns its saved result; a cancelled or refused one is refused (409); a different role cannot repeat it (403); a request whose stored payload expired under retention is gone (410)."
      }
    },
    "/v1/operations/{id}/cancel": {
      "post": {
        "operationId": "cancelOperation",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The record's id."
          },
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Message"
                }
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "410": {
            "description": "Gone: the stored request expired under the lender's retention policy and cannot run again.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "413": {
            "description": "The body is larger than 2 MB, or holds more than 100,000 values (every object, list, text, number, true, false and null in it).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "415": {
            "description": "The body is not JSON (a form or text body is refused), or its character set or encoding is not supported; send UTF-8 JSON as application/json.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service failed. With committed false nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); with operation completed a request with the key was saved; otherwise the outcome is unconfirmed: check Operations, or repeat the same request with its Idempotency-Key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          }
        },
        "summary": "Cancel an unconfirmed request",
        "description": "Confirms with the server that the request never completed and closes it, so its key cannot run again. A completed entry, or one whose receipt already exists, is refused (409); one whose stored payload expired under retention is gone (410)."
      }
    },
    "/v1/pilot/journey": {
      "get": {
        "operationId": "getPilotJourney",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PilotJourney"
                }
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service could not prepare this answer; a read changes nothing, so it can be tried again.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          }
        },
        "summary": "Read the pilot journey counts",
        "description": "Counts of the records each pilot step needs, for the journey page. No record is created by reading."
      }
    },
    "/v1/pilot/lenders": {
      "post": {
        "operationId": "createPilotLender",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 8,
              "maxLength": 200
            },
            "description": "Required: a request without one is refused (400, naming the header). 8 to 200 characters, one per new lender. The key names the lender it creates: a repeat returns that lender, and the same key with different details is refused (409). The request is not recorded in Operations, so a request refused before the lender was created (by the caller's role, or at the sandbox's five-lender limit) may be sent again with the same key once the refusal no longer applies."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Merchant"
                }
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "413": {
            "description": "The body is larger than 2 MB, or holds more than 100,000 values (every object, list, text, number, true, false and null in it).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "415": {
            "description": "The body is not JSON (a form or text body is refused), or its character set or encoding is not supported; send UTF-8 JSON as application/json.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service failed. With committed false nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); with operation completed a request with the key was saved; otherwise the outcome is unconfirmed: check Operations, or repeat the same request with its Idempotency-Key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          }
        },
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PilotLenderInput"
              }
            }
          }
        },
        "summary": "Create a synthetic lender",
        "description": "An administrator: on a staff host with recent MFA; in a sandbox, the demo Administrator. A sandbox workspace holds at most five lenders, the two samples included, and a sixth is refused (409). The key makes creation repeatable; the same key with different details is refused."
      }
    },
    "/v1/pilot/batches": {
      "get": {
        "operationId": "listImportBatches",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          },
          {
            "name": "offset",
            "in": "query",
            "schema": {
              "type": "integer",
              "minimum": 0,
              "maximum": 100000
            },
            "description": "Rows to skip in the newest-first order; pages hold 25 rows."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ImportBatchList"
                }
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service could not prepare this answer; a read changes nothing, so it can be tried again.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          }
        },
        "summary": "List import batches",
        "description": "Newest first, 25 a page, without source rows."
      },
      "post": {
        "operationId": "saveImportBatch",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": false,
            "schema": {
              "type": "string",
              "minLength": 8,
              "maxLength": 200
            },
            "description": "Optional: without one the write still runs, but a lost answer cannot be recovered and a repeat may apply twice. With one, the request is journaled in Operations and repeatable. 8 to 200 characters, one per unchanged intention. The same key with different input is refused (409). A key whose request was refused cannot run again: its journal entry is closed. A repeat after a lost answer returns the original result, checked before the version; once the lender's retention policy has removed that stored result, the repeat is refused (410). A repeat while the request is still running is answered 503 with Retry-After and operation running, and leaves it to finish. The result is kept with the request's journal entry, so a key names one request of the person who sent it, in its lender."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ValopayRecord"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "410": {
            "description": "Gone: a request with this Idempotency-Key already completed, and the lender's retention policy has since removed its stored result, so it cannot run again. Its entry in Operations remains.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "413": {
            "description": "The body is larger than 2 MB, or holds more than 100,000 values (every object, list, text, number, true, false and null in it).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "415": {
            "description": "The body is not JSON (a form or text body is refused), or its character set or encoding is not supported; send UTF-8 JSON as application/json.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "500": {
            "description": "The service failed. With committed false nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); with operation completed a request with the key was saved; otherwise the outcome is unconfirmed: check Operations, or repeat the same request with its Idempotency-Key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          }
        },
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ImportBatchInput"
              }
            }
          }
        },
        "summary": "Save a source batch",
        "description": "Parses and checks the rows, screens them for raw bank details, and records the batch as ready or needing correction. A batch with the same source identity is refused (409)."
      }
    },
    "/v1/pilot/batches/{id}": {
      "get": {
        "operationId": "getImportBatch",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The record's id."
          },
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ImportBatchDetail"
                }
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service could not prepare this answer; a read changes nothing, so it can be tried again.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          }
        },
        "summary": "Open an import batch",
        "description": "The batch with its source rows and revisions. Import operator roles only (403); unknown batches are 404."
      }
    },
    "/v1/pilot/batches/{id}/save": {
      "post": {
        "operationId": "saveImportBatchRevision",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The record's id."
          },
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": false,
            "schema": {
              "type": "string",
              "minLength": 8,
              "maxLength": 200
            },
            "description": "Optional: without one the write still runs, but a lost answer cannot be recovered and a repeat may apply twice. With one, the request is journaled in Operations and repeatable. 8 to 200 characters, one per unchanged intention. The same key with different input is refused (409). A key whose request was refused cannot run again: its journal entry is closed. A repeat after a lost answer returns the original result, checked before the version; once the lender's retention policy has removed that stored result, the repeat is refused (410). A repeat while the request is still running is answered 503 with Retry-After and operation running, and leaves it to finish. The result is kept with the request's journal entry, so a key names one request of the person who sent it, in its lender."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ValopayRecord"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "410": {
            "description": "Gone: a request with this Idempotency-Key already completed, and the lender's retention policy has since removed its stored result, so it cannot run again. Its entry in Operations remains.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "413": {
            "description": "The body is larger than 2 MB, or holds more than 100,000 values (every object, list, text, number, true, false and null in it).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "415": {
            "description": "The body is not JSON (a form or text body is refused), or its character set or encoding is not supported; send UTF-8 JSON as application/json.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "500": {
            "description": "The service failed. With committed false nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); with operation completed a request with the key was saved; otherwise the outcome is unconfirmed: check Operations, or repeat the same request with its Idempotency-Key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          }
        },
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ImportBatchInput"
              }
            }
          }
        },
        "summary": "Correct an uncommitted batch",
        "description": "Saves a revision of the batch, keeping its source identity. A committed batch or a stale version is refused (409)."
      }
    },
    "/v1/pilot/batches/{id}/commit": {
      "post": {
        "operationId": "commitImportBatch",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The record's id."
          },
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": false,
            "schema": {
              "type": "string",
              "minLength": 8,
              "maxLength": 200
            },
            "description": "Optional: without one the write still runs, but a lost answer cannot be recovered and a repeat may apply twice. With one, the request is journaled in Operations and repeatable. 8 to 200 characters, one per unchanged intention. The same key with different input is refused (409). A key whose request was refused cannot run again: its journal entry is closed. A repeat after a lost answer returns the original result, checked before the version; once the lender's retention policy has removed that stored result, the repeat is refused (410). A repeat while the request is still running is answered 503 with Retry-After and operation running, and leaves it to finish. The result is kept with the request's journal entry, so a key names one request of the person who sent it, in its lender."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ValopayRecord"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "410": {
            "description": "Gone: a request with this Idempotency-Key already completed, and the lender's retention policy has since removed its stored result, so it cannot run again. Its entry in Operations remains.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "413": {
            "description": "The body is larger than 2 MB, or holds more than 100,000 values (every object, list, text, number, true, false and null in it).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "415": {
            "description": "The body is not JSON (a form or text body is refused), or its character set or encoding is not supported; send UTF-8 JSON as application/json.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "500": {
            "description": "The service failed. With committed false nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); with operation completed a request with the key was saved; otherwise the outcome is unconfirmed: check Operations, or repeat the same request with its Idempotency-Key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          }
        },
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/BatchVersion"
              }
            }
          }
        },
        "summary": "Commit a checked batch",
        "description": "Imports the checked rows in one transaction and records the original source totals. A batch that is not ready, or a stale version, is refused (409)."
      }
    },
    "/v1/pilot/cases/{id}": {
      "get": {
        "operationId": "getCase",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The record's id."
          },
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CaseDetail"
                }
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service could not prepare this answer; a read changes nothing, so it can be tried again.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          }
        },
        "summary": "Open a case",
        "description": "The exception with its possible assignees, handover history and citable evidence."
      },
      "post": {
        "operationId": "coordinateCase",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The record's id."
          },
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": false,
            "schema": {
              "type": "string",
              "minLength": 8,
              "maxLength": 200
            },
            "description": "Optional: without one the write still runs, but a lost answer cannot be recovered and a repeat may apply twice. With one, the request is journaled in Operations and repeatable. 8 to 200 characters, one per unchanged intention. The same key with different input is refused (409). A key whose request was refused cannot run again: its journal entry is closed. A repeat after a lost answer returns the original result, checked before the version; once the lender's retention policy has removed that stored result, the repeat is refused (410). A repeat while the request is still running is answered 503 with Retry-After and operation running, and leaves it to finish. The result is kept with the request's journal entry, so a key names one request of the person who sent it, in its lender."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ValopayRecord"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "410": {
            "description": "Gone: a request with this Idempotency-Key already completed, and the lender's retention policy has since removed its stored result, so it cannot run again. Its entry in Operations remains.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "413": {
            "description": "The body is larger than 2 MB, or holds more than 100,000 values (every object, list, text, number, true, false and null in it).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "415": {
            "description": "The body is not JSON (a form or text body is refused), or its character set or encoding is not supported; send UTF-8 JSON as application/json.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "500": {
            "description": "The service failed. With committed false nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); with operation completed a request with the key was saved; otherwise the outcome is unconfirmed: check Operations, or repeat the same request with its Idempotency-Key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          }
        },
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CaseInput"
              }
            }
          }
        },
        "summary": "Hand over or update a case",
        "description": "Records the assignee, next action and evidence, with the version being changed. The next action must be in the future."
      }
    },
    "/v1/team/verify": {
      "post": {
        "operationId": "verifyStaffIdentity",
        "tags": [
          "valopay"
        ],
        "parameters": [],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Message"
                }
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "anyOf": [
                    {
                      "$ref": "#/components/schemas/ErrorBody"
                    },
                    {
                      "$ref": "#/components/schemas/ReverificationRequired"
                    }
                  ]
                }
              }
            }
          },
          "413": {
            "description": "The body is larger than 2 MB, or holds more than 100,000 values (every object, list, text, number, true, false and null in it).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "415": {
            "description": "The body is not JSON (a form or text body is refused), or its character set or encoding is not supported; send UTF-8 JSON as application/json.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service failed. With committed false nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); with operation completed a request with the key was saved; otherwise the outcome is unconfirmed: check Operations, or repeat the same request with its Idempotency-Key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          }
        },
        "summary": "Verify staff identity with a fresh second factor",
        "description": "Staff hosts only (403 elsewhere). Requires a signed-in session with an enrolled second factor used recently; otherwise answers 403 with the identity provider's re-verification instruction."
      }
    },
    "/v1/team": {
      "get": {
        "operationId": "getTeam",
        "tags": [
          "valopay"
        ],
        "parameters": [],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/StaffDirectory"
                }
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service could not prepare this answer; a read changes nothing, so it can be tried again.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          }
        },
        "summary": "Read the team directory",
        "description": "Members, lenders, invitations and access history as the caller's role allows. In the sandbox every list is empty."
      }
    },
    "/v1/team/invitations": {
      "post": {
        "operationId": "inviteStaff",
        "tags": [
          "valopay"
        ],
        "parameters": [],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/InvitationCreated"
                }
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "413": {
            "description": "The body is larger than 2 MB, or holds more than 100,000 values (every object, list, text, number, true, false and null in it).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "415": {
            "description": "The body is not JSON (a form or text body is refused), or its character set or encoding is not supported; send UTF-8 JSON as application/json.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service failed. With committed false nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); with operation completed a request with the key was saved; otherwise the outcome is unconfirmed: check Operations, or repeat the same request with its Idempotency-Key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          }
        },
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/InvitationInput"
              }
            }
          }
        },
        "summary": "Invite a staff member",
        "description": "Administrator with recent MFA. Replaces any pending invitation for the same address; the token expires in seven days and is never emailed by the service."
      }
    },
    "/v1/team/invitations/{id}/revoke": {
      "post": {
        "operationId": "revokeInvitation",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The record's id."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Message"
                }
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "413": {
            "description": "The body is larger than 2 MB, or holds more than 100,000 values (every object, list, text, number, true, false and null in it).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "415": {
            "description": "The body is not JSON (a form or text body is refused), or its character set or encoding is not supported; send UTF-8 JSON as application/json.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service failed. With committed false nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); with operation completed a request with the key was saved; otherwise the outcome is unconfirmed: check Operations, or repeat the same request with its Idempotency-Key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          }
        },
        "summary": "Revoke an invitation",
        "description": "Administrator with recent MFA. A revoked token cannot be accepted; an invitation that is no longer pending is refused (409)."
      }
    },
    "/v1/team/invitations/{id}/approve": {
      "post": {
        "operationId": "approveInvitation",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The record's id."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Message"
                }
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "413": {
            "description": "The body is larger than 2 MB, or holds more than 100,000 values (every object, list, text, number, true, false and null in it).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "415": {
            "description": "The body is not JSON (a form or text body is refused), or its character set or encoding is not supported; send UTF-8 JSON as application/json.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service failed. With committed false nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); with operation completed a request with the key was saved; otherwise the outcome is unconfirmed: check Operations, or repeat the same request with its Idempotency-Key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          }
        },
        "summary": "Approve an invitation as the second administrator",
        "description": "Administrator with recent MFA, other than the one who sent it (403); the approval is recorded in the access history. Only a pending Admin, Finance or Compliance reviewer invitation waits for one: any other, or one already approved, is refused (409). The invited person can accept it afterwards."
      }
    },
    "/v1/team/members/{id}": {
      "patch": {
        "operationId": "updateStaffMember",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The record's id."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/StaffMemberChange"
                }
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "413": {
            "description": "The body is larger than 2 MB, or holds more than 100,000 values (every object, list, text, number, true, false and null in it).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "415": {
            "description": "The body is not JSON (a form or text body is refused), or its character set or encoding is not supported; send UTF-8 JSON as application/json.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service failed. With committed false nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); with operation completed a request with the key was saved; otherwise the outcome is unconfirmed: check Operations, or repeat the same request with its Idempotency-Key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          }
        },
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/MembershipInput"
              }
            }
          }
        },
        "summary": "Change a membership",
        "description": "Administrator with recent MFA; nobody changes their own membership. Records the reason in the access history. A change that leaves the membership active as Admin, Finance or Compliance reviewer when it was not (a new role, or a reactivation) is saved as a request for a second administrator: the answer names it (pendingChange), the membership stays as it is until another administrator approves it, and the same request again answers the same waiting request. Every other change takes effect at once."
      }
    },
    "/v1/team/changes/{id}/approve": {
      "post": {
        "operationId": "approveStaffChange",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The record's id."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/StaffMemberChange"
                }
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "413": {
            "description": "The body is larger than 2 MB, or holds more than 100,000 values (every object, list, text, number, true, false and null in it).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "415": {
            "description": "The body is not JSON (a form or text body is refused), or its character set or encoding is not supported; send UTF-8 JSON as application/json.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service failed. With committed false nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); with operation completed a request with the key was saved; otherwise the outcome is unconfirmed: check Operations, or repeat the same request with its Idempotency-Key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          }
        },
        "summary": "Approve a membership change as the second administrator",
        "description": "Administrator with recent MFA, other than the one who asked and other than the person changed (403). Applies exactly the requested change and records who asked and who approved. A request already approved or declined, or whose membership changed since it was made, is refused (409)."
      }
    },
    "/v1/team/changes/{id}/decline": {
      "post": {
        "operationId": "declineStaffChange",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The record's id."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Message"
                }
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "413": {
            "description": "The body is larger than 2 MB, or holds more than 100,000 values (every object, list, text, number, true, false and null in it).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "415": {
            "description": "The body is not JSON (a form or text body is refused), or its character set or encoding is not supported; send UTF-8 JSON as application/json.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service failed. With committed false nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); with operation completed a request with the key was saved; otherwise the outcome is unconfirmed: check Operations, or repeat the same request with its Idempotency-Key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          }
        },
        "summary": "Decline or withdraw a membership change",
        "description": "Administrator with recent MFA, other than the person changed (403); the administrator who asked withdraws it the same way. Recorded in the access history; the membership is unchanged. A request already approved or declined is refused (409)."
      }
    },
    "/v1/team/members/{id}/lenders": {
      "patch": {
        "operationId": "updateStaffLenders",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The record's id."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/StaffLenderAccess"
                }
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "413": {
            "description": "The body is larger than 2 MB, or holds more than 100,000 values (every object, list, text, number, true, false and null in it).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "415": {
            "description": "The body is not JSON (a form or text body is refused), or its character set or encoding is not supported; send UTF-8 JSON as application/json.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service failed. With committed false nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); with operation completed a request with the key was saved; otherwise the outcome is unconfirmed: check Operations, or repeat the same request with its Idempotency-Key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          }
        },
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/StaffLenderAccessInput"
              }
            }
          }
        },
        "summary": "Set a member's lender access",
        "description": "Administrator with recent MFA. Non-administrators open only the lenders named here; sessions pick the change up on their next request."
      }
    },
    "/v1/team/accept": {
      "post": {
        "operationId": "acceptInvitation",
        "tags": [
          "valopay"
        ],
        "parameters": [],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/InvitationAccepted"
                }
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "413": {
            "description": "The body is larger than 2 MB, or holds more than 100,000 values (every object, list, text, number, true, false and null in it).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "415": {
            "description": "The body is not JSON (a form or text body is refused), or its character set or encoding is not supported; send UTF-8 JSON as application/json.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service failed. With committed false nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); with operation completed a request with the key was saved; otherwise the outcome is unconfirmed: check Operations, or repeat the same request with its Idempotency-Key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          }
        },
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AcceptInvitationInput"
              }
            }
          }
        },
        "summary": "Accept an invitation",
        "description": "The signed-in person's verified email must match the invitation. Creates a 90-day membership. An Admin, Finance or Compliance reviewer invitation is refused (403) until a second administrator has approved it."
      }
    },
    "/v1/team/readiness": {
      "get": {
        "operationId": "getAccessReadiness",
        "tags": [
          "valopay"
        ],
        "parameters": [],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AccessReadiness"
                }
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service could not prepare this answer; a read changes nothing, so it can be tried again.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          }
        },
        "summary": "Read the access readiness checks",
        "description": "What this host has configured and verified for real staff access, from the request's own checks."
      }
    },
    "/v1/team/readiness/encryption": {
      "post": {
        "operationId": "verifyEncryption",
        "tags": [
          "valopay"
        ],
        "parameters": [],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/EncryptionVerification"
                }
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "413": {
            "description": "The body is larger than 2 MB, or holds more than 100,000 values (every object, list, text, number, true, false and null in it).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "415": {
            "description": "The body is not JSON (a form or text body is refused), or its character set or encoding is not supported; send UTF-8 JSON as application/json.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service failed. With committed false nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); with operation completed a request with the key was saved; otherwise the outcome is unconfirmed: check Operations, or repeat the same request with its Idempotency-Key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          }
        },
        "summary": "Verify managed payload encryption",
        "description": "Administrator with recent MFA. Seals and opens a synthetic payload with the configured key (503 when none is configured)."
      }
    },
    "/v1/team/readiness/protect": {
      "post": {
        "operationId": "protectPayloads",
        "tags": [
          "valopay"
        ],
        "parameters": [],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PayloadProtection"
                }
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "413": {
            "description": "The body is larger than 2 MB, or holds more than 100,000 values (every object, list, text, number, true, false and null in it).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "415": {
            "description": "The body is not JSON (a form or text body is refused), or its character set or encoding is not supported; send UTF-8 JSON as application/json.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service failed. With committed false nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); with operation completed a request with the key was saved; otherwise the outcome is unconfirmed: check Operations, or repeat the same request with its Idempotency-Key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          }
        },
        "summary": "Protect stored payloads",
        "description": "Administrator with recent MFA. Seals one bounded batch of unprotected import rows and recovery payloads; run until none remain (503 when no key is configured)."
      }
    },
    "/v1/pilot/progress": {
      "get": {
        "operationId": "getPilotProgress",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PilotProgress"
                }
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service could not prepare this answer; a read changes nothing, so it can be tried again.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          }
        },
        "summary": "Read the pilot progress steps",
        "description": "Derived from the lender's records on every read; nothing is written."
      }
    },
    "/v1/pilot/close-reviews": {
      "get": {
        "operationId": "listCloseReviews",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CloseReviewList"
                }
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service could not prepare this answer; a read changes nothing, so it can be tried again.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          }
        },
        "summary": "List closes and their reviews",
        "description": "Newest 25 closes with their discrepancies, review state and the available Finance reviewers."
      }
    },
    "/v1/pilot/close-reviews/prepare": {
      "post": {
        "operationId": "prepareCloseReview",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 8,
              "maxLength": 200
            },
            "description": "Required: a request without one is refused (400, naming the header). 8 to 200 characters, one per unchanged intention. The same key with different input is refused (409). A key whose request was refused cannot run again: its journal entry is closed. A repeat after a lost answer returns the original result, checked before the version; once the lender's retention policy has removed that stored result, the repeat is refused (410). A repeat while the request is still running is answered 503 with Retry-After and operation running, and leaves it to finish. The result is kept with the request's journal entry, so a key names one request of the person who sent it, in its lender."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ValopayRecord"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "410": {
            "description": "Gone: a request with this Idempotency-Key already completed, and the lender's retention policy has since removed its stored result, so it cannot run again. Its entry in Operations remains.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "413": {
            "description": "The body is larger than 2 MB, or holds more than 100,000 values (every object, list, text, number, true, false and null in it).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "415": {
            "description": "The body is not JSON (a form or text body is refused), or its character set or encoding is not supported; send UTF-8 JSON as application/json.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "500": {
            "description": "The service failed. With committed false nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); with operation completed a request with the key was saved; otherwise the outcome is unconfirmed: check Operations, or repeat the same request with its Idempotency-Key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          }
        },
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PrepareCloseReviewInput"
              }
            }
          }
        },
        "summary": "Prepare a close for review",
        "description": "Snapshots the close with its source-completeness basis and assigns an independent Finance reviewer. A close with an open review, a stale version or an unanswered discrepancy is refused."
      }
    },
    "/v1/pilot/close-reviews/{id}/decision": {
      "post": {
        "operationId": "decideCloseReview",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The record's id."
          },
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 8,
              "maxLength": 200
            },
            "description": "Required: a request without one is refused (400, naming the header). 8 to 200 characters, one per unchanged intention. The same key with different input is refused (409). A key whose request was refused cannot run again: its journal entry is closed. A repeat after a lost answer returns the original result, checked before the version; once the lender's retention policy has removed that stored result, the repeat is refused (410). A repeat while the request is still running is answered 503 with Retry-After and operation running, and leaves it to finish. The result is kept with the request's journal entry, so a key names one request of the person who sent it, in its lender."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ValopayRecord"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "410": {
            "description": "Gone: a request with this Idempotency-Key already completed, and the lender's retention policy has since removed its stored result, so it cannot run again. Its entry in Operations remains.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "413": {
            "description": "The body is larger than 2 MB, or holds more than 100,000 values (every object, list, text, number, true, false and null in it).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "415": {
            "description": "The body is not JSON (a form or text body is refused), or its character set or encoding is not supported; send UTF-8 JSON as application/json.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "500": {
            "description": "The service failed. With committed false nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); with operation completed a request with the key was saved; otherwise the outcome is unconfirmed: check Operations, or repeat the same request with its Idempotency-Key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          }
        },
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DecideCloseReviewInput"
              }
            }
          }
        },
        "summary": "Approve or reject a close review",
        "description": "Only the named reviewer decides, and only while the snapshot is current; a changed close or source declaration must be prepared again. One browser switching demo roles is one person, so a sandbox cannot decide its own preparation (403)."
      }
    },
    "/v1/pilot/import-corrections": {
      "get": {
        "operationId": "listImportCorrections",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          },
          {
            "name": "batchId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The committed import batch whose records may be corrected."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ImportCorrectionList"
                }
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service could not prepare this answer; a read changes nothing, so it can be tried again.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          }
        },
        "summary": "List import corrections for a batch",
        "description": "The batch's imported records and every proposal with its current state."
      },
      "post": {
        "operationId": "proposeImportCorrection",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 8,
              "maxLength": 200
            },
            "description": "Required: a request without one is refused (400, naming the header). 8 to 200 characters, one per unchanged intention. The same key with different input is refused (409). A key whose request was refused cannot run again: its journal entry is closed. A repeat after a lost answer returns the original result, checked before the version; once the lender's retention policy has removed that stored result, the repeat is refused (410). A repeat while the request is still running is answered 503 with Retry-After and operation running, and leaves it to finish. The result is kept with the request's journal entry, so a key names one request of the person who sent it, in its lender."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ImportCorrectionView"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "410": {
            "description": "Gone: a request with this Idempotency-Key already completed, and the lender's retention policy has since removed its stored result, so it cannot run again. Its entry in Operations remains.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "413": {
            "description": "The body is larger than 2 MB, or holds more than 100,000 values (every object, list, text, number, true, false and null in it).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "415": {
            "description": "The body is not JSON (a form or text body is refused), or its character set or encoding is not supported; send UTF-8 JSON as application/json.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "500": {
            "description": "The service failed. With committed false nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); with operation completed a request with the key was saved; otherwise the outcome is unconfirmed: check Operations, or repeat the same request with its Idempotency-Key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          }
        },
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ImportCorrectionProposalInput"
              }
            }
          }
        },
        "summary": "Propose an import correction",
        "description": "Records the comparison as evidence for an independent Finance reviewer. A changed comparison, a missing reviewer or an open proposal on the same record is refused."
      }
    },
    "/v1/pilot/import-corrections/preview": {
      "post": {
        "operationId": "previewImportCorrection",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ImportCorrectionPreview"
                }
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "413": {
            "description": "The body is larger than 2 MB, or holds more than 100,000 values (every object, list, text, number, true, false and null in it).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "415": {
            "description": "The body is not JSON (a form or text body is refused), or its character set or encoding is not supported; send UTF-8 JSON as application/json.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service failed. With committed false nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); with operation completed a request with the key was saved; otherwise the outcome is unconfirmed: check Operations, or repeat the same request with its Idempotency-Key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          }
        },
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ImportCorrectionPreviewInput"
              }
            }
          }
        },
        "summary": "Compare a proposed correction",
        "description": "Shows what would change, which closes and financial records it touches, and what blocks it. Nothing is written."
      }
    },
    "/v1/pilot/import-corrections/{id}/decision": {
      "post": {
        "operationId": "decideImportCorrection",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The record's id."
          },
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 8,
              "maxLength": 200
            },
            "description": "Required: a request without one is refused (400, naming the header). 8 to 200 characters, one per unchanged intention. The same key with different input is refused (409). A key whose request was refused cannot run again: its journal entry is closed. A repeat after a lost answer returns the original result, checked before the version; once the lender's retention policy has removed that stored result, the repeat is refused (410). A repeat while the request is still running is answered 503 with Retry-After and operation running, and leaves it to finish. The result is kept with the request's journal entry, so a key names one request of the person who sent it, in its lender."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ImportCorrectionView"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "410": {
            "description": "Gone: a request with this Idempotency-Key already completed, and the lender's retention policy has since removed its stored result, so it cannot run again. Its entry in Operations remains.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "413": {
            "description": "The body is larger than 2 MB, or holds more than 100,000 values (every object, list, text, number, true, false and null in it).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "415": {
            "description": "The body is not JSON (a form or text body is refused), or its character set or encoding is not supported; send UTF-8 JSON as application/json.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "500": {
            "description": "The service failed. With committed false nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); with operation completed a request with the key was saved; otherwise the outcome is unconfirmed: check Operations, or repeat the same request with its Idempotency-Key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          }
        },
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ImportCorrectionDecisionInput"
              }
            }
          }
        },
        "summary": "Decide an import correction",
        "description": "Only the named reviewer approves or rejects; the proposer may withdraw. Approval applies the change only while the comparison is current."
      }
    },
    "/v1/sources": {
      "get": {
        "operationId": "getSources",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          },
          {
            "name": "businessDate",
            "in": "query",
            "schema": {
              "type": "string",
              "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
            },
            "description": "The WAT business date to report completeness for; defaults to the current one."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SourcesView"
                }
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service could not prepare this answer; a read changes nothing, so it can be tried again.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          }
        },
        "summary": "Read the sources page",
        "description": "Completeness for the business date, profiles with delivery state, batches with quality totals and the Paystack test inbox."
      }
    },
    "/v1/sources/profiles": {
      "post": {
        "operationId": "createSourceProfile",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": false,
            "schema": {
              "type": "string",
              "minLength": 8,
              "maxLength": 200
            },
            "description": "Optional: without one the write still runs, but a lost answer cannot be recovered and a repeat may apply twice. With one, the request is journaled in Operations and repeatable. 8 to 200 characters, one per unchanged intention. The same key with different input is refused (409). A key whose request was refused cannot run again: its journal entry is closed. A repeat after a lost answer returns the original result, checked before the version; once the lender's retention policy has removed that stored result, the repeat is refused (410). A repeat while the request is still running is answered 503 with Retry-After and operation running, and leaves it to finish. The result is kept with the request's journal entry, so a key names one request of the person who sent it, in its lender."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ValopayRecord"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "410": {
            "description": "Gone: a request with this Idempotency-Key already completed, and the lender's retention policy has since removed its stored result, so it cannot run again. Its entry in Operations remains.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "413": {
            "description": "The body is larger than 2 MB, or holds more than 100,000 values (every object, list, text, number, true, false and null in it).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "415": {
            "description": "The body is not JSON (a form or text body is refused), or its character set or encoding is not supported; send UTF-8 JSON as application/json.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "500": {
            "description": "The service failed. With committed false nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); with operation completed a request with the key was saved; otherwise the outcome is unconfirmed: check Operations, or repeat the same request with its Idempotency-Key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          }
        },
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SourceProfileInput"
              }
            }
          }
        },
        "summary": "Create a source profile",
        "description": "One profile per source and record kind (409 otherwise). Import operator roles only."
      }
    },
    "/v1/sources/profiles/{id}/save": {
      "post": {
        "operationId": "saveSourceProfile",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The record's id."
          },
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": false,
            "schema": {
              "type": "string",
              "minLength": 8,
              "maxLength": 200
            },
            "description": "Optional: without one the write still runs, but a lost answer cannot be recovered and a repeat may apply twice. With one, the request is journaled in Operations and repeatable. 8 to 200 characters, one per unchanged intention. The same key with different input is refused (409). A key whose request was refused cannot run again: its journal entry is closed. A repeat after a lost answer returns the original result, checked before the version; once the lender's retention policy has removed that stored result, the repeat is refused (410). A repeat while the request is still running is answered 503 with Retry-After and operation running, and leaves it to finish. The result is kept with the request's journal entry, so a key names one request of the person who sent it, in its lender."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ValopayRecord"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "410": {
            "description": "Gone: a request with this Idempotency-Key already completed, and the lender's retention policy has since removed its stored result, so it cannot run again. Its entry in Operations remains.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "413": {
            "description": "The body is larger than 2 MB, or holds more than 100,000 values (every object, list, text, number, true, false and null in it).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "415": {
            "description": "The body is not JSON (a form or text body is refused), or its character set or encoding is not supported; send UTF-8 JSON as application/json.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "500": {
            "description": "The service failed. With committed false nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); with operation completed a request with the key was saved; otherwise the outcome is unconfirmed: check Operations, or repeat the same request with its Idempotency-Key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          }
        },
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SourceProfileInput"
              }
            }
          }
        },
        "summary": "Change a source profile",
        "description": "Saves a new version of the profile; batches keep the version they were checked against."
      }
    },
    "/v1/sources/manifests": {
      "post": {
        "operationId": "saveSourceManifest",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 8,
              "maxLength": 200
            },
            "description": "Required: a request without one is refused (400, naming the header). 8 to 200 characters, one per unchanged intention. The same key with different input is refused (409). A key whose request was refused cannot run again: its journal entry is closed. A repeat after a lost answer returns the original result, checked before the version; once the lender's retention policy has removed that stored result, the repeat is refused (410). A repeat while the request is still running is answered 503 with Retry-After and operation running, and leaves it to finish. The result is kept with the request's journal entry, so a key names one request of the person who sent it, in its lender."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ValopayRecord"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "410": {
            "description": "Gone: a request with this Idempotency-Key already completed, and the lender's retention policy has since removed its stored result, so it cannot run again. Its entry in Operations remains.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "413": {
            "description": "The body is larger than 2 MB, or holds more than 100,000 values (every object, list, text, number, true, false and null in it).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "415": {
            "description": "The body is not JSON (a form or text body is refused), or its character set or encoding is not supported; send UTF-8 JSON as application/json.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "500": {
            "description": "The service failed. With committed false nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); with operation completed a request with the key was saved; otherwise the outcome is unconfirmed: check Operations, or repeat the same request with its Idempotency-Key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          }
        },
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SourceManifestInput"
              }
            }
          }
        },
        "summary": "Declare the expected source files",
        "description": "Records what must arrive for a business date. A revision must name the current declaration; a source batch declared for another date is refused."
      }
    },
    "/v1/sources/paystack/fixtures": {
      "post": {
        "operationId": "runPaystackFixture",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": false,
            "schema": {
              "type": "string",
              "minLength": 8,
              "maxLength": 200
            },
            "description": "Optional: without one the write still runs, but a lost answer cannot be recovered and a repeat may apply twice. With one, the request is journaled in Operations and repeatable. 8 to 200 characters, one per unchanged intention. The same key with different input is refused (409). A key whose request was refused cannot run again: its journal entry is closed. A repeat after a lost answer returns the original result, checked before the version; once the lender's retention policy has removed that stored result, the repeat is refused (410). A repeat while the request is still running is answered 503 with Retry-After and operation running, and leaves it to finish. The result is kept with the request's journal entry, so a key names one request of the person who sent it, in its lender."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PaystackFixtureResult"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "410": {
            "description": "Gone: a request with this Idempotency-Key already completed, and the lender's retention policy has since removed its stored result, so it cannot run again. Its entry in Operations remains.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "413": {
            "description": "The body is larger than 2 MB, or holds more than 100,000 values (every object, list, text, number, true, false and null in it).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "415": {
            "description": "The body is not JSON (a form or text body is refused), or its character set or encoding is not supported; send UTF-8 JSON as application/json.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "500": {
            "description": "The service failed. With committed false nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); with operation completed a request with the key was saved; otherwise the outcome is unconfirmed: check Operations, or repeat the same request with its Idempotency-Key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          }
        },
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PaystackFixtureInput"
              }
            }
          }
        },
        "summary": "Deliver a recorded Paystack scenario",
        "description": "Runs a test-only fixture through the inbox. No external call is made and no financial record is created."
      }
    },
    "/v1/sources/events/{id}/replay": {
      "post": {
        "operationId": "replayProviderEvent",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The record's id."
          },
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": false,
            "schema": {
              "type": "string",
              "minLength": 8,
              "maxLength": 200
            },
            "description": "Optional: without one the write still runs, but a lost answer cannot be recovered and a repeat may apply twice. With one, the request is journaled in Operations and repeatable. 8 to 200 characters, one per unchanged intention. The same key with different input is refused (409). A key whose request was refused cannot run again: its journal entry is closed. A repeat after a lost answer returns the original result, checked before the version; once the lender's retention policy has removed that stored result, the repeat is refused (410). A repeat while the request is still running is answered 503 with Retry-After and operation running, and leaves it to finish. The result is kept with the request's journal entry, so a key names one request of the person who sent it, in its lender."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProviderEvent"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "410": {
            "description": "Gone: a request with this Idempotency-Key already completed, and the lender's retention policy has since removed its stored result, so it cannot run again. Its entry in Operations remains.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "413": {
            "description": "The body is larger than 2 MB, or holds more than 100,000 values (every object, list, text, number, true, false and null in it).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "415": {
            "description": "The body is not JSON (a form or text body is refused), or its character set or encoding is not supported; send UTF-8 JSON as application/json.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "500": {
            "description": "The service failed. With committed false nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); with operation completed a request with the key was saved; otherwise the outcome is unconfirmed: check Operations, or repeat the same request with its Idempotency-Key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          }
        },
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ProviderReplayInput"
              }
            }
          }
        },
        "summary": "Replay a stored provider event",
        "description": "Re-processes the event with the reason recorded; duplicates are recognised and counted."
      }
    },
    "/v1/providers/paystack/{connectionId}/events": {
      "post": {
        "operationId": "receivePaystackTestEvent",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "connectionId",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "pattern": "^[a-f0-9]{64}$"
            },
            "description": "The opaque ID an operator mapped to one synthetic lender in VALOPAY_PAYSTACK_CONNECTIONS; it alone selects the lender, and it is not a credential."
          },
          {
            "name": "x-paystack-signature",
            "in": "header",
            "required": true,
            "schema": {
              "type": "string",
              "pattern": "^[a-fA-F0-9]{128}$"
            },
            "description": "HMAC-SHA512, under the configured test secret key and in hexadecimal, of the body's exact bytes: the JSON as sent, or, for a body sent with Content-Encoding gzip, deflate or br, the JSON bytes after decompression (not the compressed bytes); any other encoding is refused (415)."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PaystackDeliveryReceipt"
                }
              }
            }
          },
          "400": {
            "description": "The body is not JSON bytes, the connection ID is malformed, or the signed event is inconsistent or from live mode",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "401": {
            "description": "The signature does not match the body's bytes (decompressed first, when the body is compressed) under the configured test key; nothing was locked, read or saved",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "403": {
            "description": "With the lender locked, the mapping names another workspace, or the lender is not a synthetic lender in sandbox or observation mode with its kill switch on",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "404": {
            "description": "No lender is mapped to this connection ID, or, when the lender cannot be locked, it is not in the mapped workspace (removed, or the mapping names the wrong lender or workspace); correct the connection mapping, since delivering again will not help",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "413": {
            "description": "The body is larger than 256 KiB",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "415": {
            "description": "The body is not JSON (a form or text body is refused), or its character set or encoding is not supported; send UTF-8 JSON as application/json.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "429": {
            "description": "More than 120 deliveries a minute from this client network, or more than 60 signed deliveries a minute to this connection; retry after the Retry-After seconds",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60, the delivery limit's window.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service failed. With committed false nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); with operation completed a request with the key was saved; otherwise the outcome is unconfirmed: check Operations, or repeat the same request with its Idempotency-Key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "503": {
            "description": "The ingress is off or misconfigured, or the mapped lender is in its workspace but busy; Paystack delivers again",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          }
        },
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PaystackTestEvent"
              }
            }
          }
        },
        "summary": "Receive a signed Paystack test event",
        "description": "The address to register as the webhook URL of a Paystack test account. Off unless the host sets VALOPAY_PAYSTACK_INGRESS to test. The signature is checked on the body's bytes, decompressed first when its Content-Encoding is gzip, deflate or br, before any lender is locked or read, so a forged or tampered delivery gets 401 and nothing else. A verified event is saved as test-mode evidence only: it creates no payment, allocation, debit or mandate authority, and still needs independent verification. Not a console call: no sandbox, sign-in or Idempotency-Key. At most 120 deliveries a minute per client network and, once signed, 60 a minute per connection. A repeat of a saved event is acknowledged without an audit entry, and its delivery count is written at most once a minute."
      }
    },
    "/v1/work": {
      "get": {
        "operationId": "getPersonalWork",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          },
          {
            "name": "scope",
            "in": "query",
            "schema": {
              "type": "string",
              "enum": [
                "mine",
                "team"
              ],
              "default": "mine"
            },
            "description": "The caller's own work, or (administrators) the whole team's."
          },
          {
            "name": "filter",
            "in": "query",
            "schema": {
              "type": "string",
              "enum": [
                "all",
                "overdue",
                "handover",
                "review",
                "unread"
              ],
              "default": "all"
            },
            "description": "Only overdue items, handovers, reviews or unread notifications."
          },
          {
            "name": "offset",
            "in": "query",
            "schema": {
              "type": "integer",
              "minimum": 0,
              "maximum": 100000
            },
            "description": "Rows to skip."
          },
          {
            "name": "limit",
            "in": "query",
            "schema": {
              "type": "integer",
              "minimum": 1,
              "maximum": 50
            },
            "description": "Page size; defaults to 25."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PersonalWorkView"
                }
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service could not prepare this answer; a read changes nothing, so it can be tried again.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          }
        },
        "summary": "Read personal work",
        "description": "Derived from cases, reviews and notifications on every read; bounded and lender scoped."
      }
    },
    "/v1/work/notifications/read": {
      "post": {
        "operationId": "readNotification",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 8,
              "maxLength": 200
            },
            "description": "Required: a request without one is refused (400, naming the header). 8 to 200 characters, one per unchanged intention. The same key with different input is refused (409). A key whose request was refused cannot run again: its journal entry is closed. A repeat after a lost answer returns the original result, checked before the version; once the lender's retention policy has removed that stored result, the repeat is refused (410). A repeat while the request is still running is answered 503 with Retry-After and operation running, and leaves it to finish. The result is kept with the request's journal entry, so a key names one request of the person who sent it, in its lender."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/WorkReceipt"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "410": {
            "description": "Gone: a request with this Idempotency-Key already completed, and the lender's retention policy has since removed its stored result, so it cannot run again. Its entry in Operations remains.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "413": {
            "description": "The body is larger than 2 MB, or holds more than 100,000 values (every object, list, text, number, true, false and null in it).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "415": {
            "description": "The body is not JSON (a form or text body is refused), or its character set or encoding is not supported; send UTF-8 JSON as application/json.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "500": {
            "description": "The service failed. With committed false nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); with operation completed a request with the key was saved; otherwise the outcome is unconfirmed: check Operations, or repeat the same request with its Idempotency-Key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          }
        },
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/WorkReceiptInput"
              }
            }
          }
        },
        "summary": "Mark a notification read",
        "description": "Records who read it and when; the server supplies the recipient."
      }
    },
    "/v1/work/handovers/acknowledge": {
      "post": {
        "operationId": "acknowledgeHandover",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 8,
              "maxLength": 200
            },
            "description": "Required: a request without one is refused (400, naming the header). 8 to 200 characters, one per unchanged intention. The same key with different input is refused (409). A key whose request was refused cannot run again: its journal entry is closed. A repeat after a lost answer returns the original result, checked before the version; once the lender's retention policy has removed that stored result, the repeat is refused (410). A repeat while the request is still running is answered 503 with Retry-After and operation running, and leaves it to finish. The result is kept with the request's journal entry, so a key names one request of the person who sent it, in its lender."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/WorkReceipt"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "410": {
            "description": "Gone: a request with this Idempotency-Key already completed, and the lender's retention policy has since removed its stored result, so it cannot run again. Its entry in Operations remains.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "413": {
            "description": "The body is larger than 2 MB, or holds more than 100,000 values (every object, list, text, number, true, false and null in it).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "415": {
            "description": "The body is not JSON (a form or text body is refused), or its character set or encoding is not supported; send UTF-8 JSON as application/json.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "500": {
            "description": "The service failed. With committed false nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); with operation completed a request with the key was saved; otherwise the outcome is unconfirmed: check Operations, or repeat the same request with its Idempotency-Key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          }
        },
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/WorkReceiptInput"
              }
            }
          }
        },
        "summary": "Acknowledge a handover",
        "description": "Records that the assignee took the case over; a stale version is refused."
      }
    },
    "/v1/lifecycle": {
      "get": {
        "operationId": "getLifecycle",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          },
          {
            "name": "offset",
            "in": "query",
            "schema": {
              "type": "integer",
              "minimum": 0,
              "maximum": 100000
            },
            "description": "Rows to skip in the newest-first order; pages hold 25 rows."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LifecycleView"
                }
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service could not prepare this answer; a read changes nothing, so it can be tried again.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          }
        },
        "summary": "Read retention controls",
        "description": "Administrators only (403). Policy, holds, inventory and runs for the lender."
      }
    },
    "/v1/lifecycle/runs/{id}": {
      "get": {
        "operationId": "getLifecycleRun",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The record's id."
          },
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LifecycleRunView"
                }
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          },
          "500": {
            "description": "The service could not prepare this answer; a read changes nothing, so it can be tried again.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            }
          }
        },
        "summary": "Read a retention run",
        "description": "Administrators only. The run's manifest and receipts."
      }
    },
    "/v1/lifecycle/policy": {
      "post": {
        "operationId": "saveRetentionPolicy",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 8,
              "maxLength": 200
            },
            "description": "Required: a request without one is refused (400, naming the header). 8 to 200 characters, one per unchanged intention. The same key with different input is refused (409). A key whose request was refused cannot run again: its journal entry is closed. A repeat after a lost answer returns the original result, checked before the version; once the lender's retention policy has removed that stored result, the repeat is refused (410). A repeat while the request is still running is answered 503 with Retry-After and operation running, and leaves it to finish. The result is kept with the request's journal entry, so a key names one request of the person who sent it, in its lender."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LifecycleView"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "410": {
            "description": "Gone: a request with this Idempotency-Key already completed, and the lender's retention policy has since removed its stored result, so it cannot run again. Its entry in Operations remains.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "413": {
            "description": "The body is larger than 2 MB, or holds more than 100,000 values (every object, list, text, number, true, false and null in it).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "415": {
            "description": "The body is not JSON (a form or text body is refused), or its character set or encoding is not supported; send UTF-8 JSON as application/json.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "500": {
            "description": "The service failed. With committed false nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); with operation completed a request with the key was saved; otherwise the outcome is unconfirmed: check Operations, or repeat the same request with its Idempotency-Key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          }
        },
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RetentionPolicyInput"
              }
            }
          }
        },
        "summary": "Change the retention policy",
        "description": "Administrators only. Keeps every previous policy version with its reason. A period shorter than the workspace's minimum (minimumDays: 30 days in the sandbox; in a staff pilot, six years for original source files and export files and a year for recovery payloads) is refused (400)."
      }
    },
    "/v1/lifecycle/holds": {
      "post": {
        "operationId": "setRetentionHold",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 8,
              "maxLength": 200
            },
            "description": "Required: a request without one is refused (400, naming the header). 8 to 200 characters, one per unchanged intention. The same key with different input is refused (409). A key whose request was refused cannot run again: its journal entry is closed. A repeat after a lost answer returns the original result, checked before the version; once the lender's retention policy has removed that stored result, the repeat is refused (410). A repeat while the request is still running is answered 503 with Retry-After and operation running, and leaves it to finish. The result is kept with the request's journal entry, so a key names one request of the person who sent it, in its lender."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LifecycleView"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "410": {
            "description": "Gone: a request with this Idempotency-Key already completed, and the lender's retention policy has since removed its stored result, so it cannot run again. Its entry in Operations remains.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "413": {
            "description": "The body is larger than 2 MB, or holds more than 100,000 values (every object, list, text, number, true, false and null in it).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "415": {
            "description": "The body is not JSON (a form or text body is refused), or its character set or encoding is not supported; send UTF-8 JSON as application/json.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "500": {
            "description": "The service failed. With committed false nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); with operation completed a request with the key was saved; otherwise the outcome is unconfirmed: check Operations, or repeat the same request with its Idempotency-Key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          }
        },
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RetentionHoldInput"
              }
            }
          }
        },
        "summary": "Place or release a hold",
        "description": "Administrators only. A held item is never deleted by a run."
      }
    },
    "/v1/lifecycle/runs": {
      "post": {
        "operationId": "previewLifecycleRun",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 8,
              "maxLength": 200
            },
            "description": "Required: a request without one is refused (400, naming the header). 8 to 200 characters, one per unchanged intention. The same key with different input is refused (409). A key whose request was refused cannot run again: its journal entry is closed. A repeat after a lost answer returns the original result, checked before the version; once the lender's retention policy has removed that stored result, the repeat is refused (410). A repeat while the request is still running is answered 503 with Retry-After and operation running, and leaves it to finish. The result is kept with the request's journal entry, so a key names one request of the person who sent it, in its lender."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LifecycleRunView"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "410": {
            "description": "Gone: a request with this Idempotency-Key already completed, and the lender's retention policy has since removed its stored result, so it cannot run again. Its entry in Operations remains.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "413": {
            "description": "The body is larger than 2 MB, or holds more than 100,000 values (every object, list, text, number, true, false and null in it).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "415": {
            "description": "The body is not JSON (a form or text body is refused), or its character set or encoding is not supported; send UTF-8 JSON as application/json.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "500": {
            "description": "The service failed. With committed false nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); with operation completed a request with the key was saved; otherwise the outcome is unconfirmed: check Operations, or repeat the same request with its Idempotency-Key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          }
        },
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/LifecyclePreviewInput"
              }
            }
          }
        },
        "summary": "Preview a retention run",
        "description": "Administrators only. Records the exact manifest of what would be deleted; nothing is deleted. With nothing old enough to delete, the preview is refused (400)."
      }
    },
    "/v1/lifecycle/runs/{id}/approve": {
      "post": {
        "operationId": "approveLifecycleRun",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The record's id."
          },
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 8,
              "maxLength": 200
            },
            "description": "Required: a request without one is refused (400, naming the header). 8 to 200 characters, one per unchanged intention. The same key with different input is refused (409). A key whose request was refused cannot run again: its journal entry is closed. A repeat after a lost answer returns the original result, checked before the version; once the lender's retention policy has removed that stored result, the repeat is refused (410). A repeat while the request is still running is answered 503 with Retry-After and operation running, and leaves it to finish. The result is kept with the request's journal entry, so a key names one request of the person who sent it, in its lender."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LifecycleRunView"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "410": {
            "description": "Gone: a request with this Idempotency-Key already completed, and the lender's retention policy has since removed its stored result, so it cannot run again. Its entry in Operations remains.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "413": {
            "description": "The body is larger than 2 MB, or holds more than 100,000 values (every object, list, text, number, true, false and null in it).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "415": {
            "description": "The body is not JSON (a form or text body is refused), or its character set or encoding is not supported; send UTF-8 JSON as application/json.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "500": {
            "description": "The service failed. With committed false nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); with operation completed a request with the key was saved; otherwise the outcome is unconfirmed: check Operations, or repeat the same request with its Idempotency-Key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          }
        },
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/LifecycleApproveInput"
              }
            }
          }
        },
        "summary": "Approve a retention run",
        "description": "Administrators only; in a staff pilot, an administrator other than the one who prepared the preview (403). The manifest digest must match the preview; a changed inventory must be previewed again."
      }
    },
    "/v1/lifecycle/runs/{id}/execute": {
      "post": {
        "operationId": "executeLifecycleRun",
        "tags": [
          "valopay"
        ],
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The record's id."
          },
          {
            "name": "merchantId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "description": "The lender (a merchant in the API) the request is scoped to; one of the caller's workspace merchants. Missing or empty, the request is refused with 400 naming merchantId, on every operation."
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": true,
            "schema": {
              "type": "string",
              "minLength": 8,
              "maxLength": 200
            },
            "description": "Required: a request without one is refused (400, naming the header). 8 to 200 characters, one per unchanged intention. The same key with different input is refused (409). A key whose request was refused cannot run again: its journal entry is closed. A repeat after a lost answer returns the original result, checked before the version; once the lender's retention policy has removed that stored result, the repeat is refused (410). A repeat while the request is still running is answered 503 with Retry-After and operation running, and leaves it to finish. The result is kept with the request's journal entry, so a key names one request of the person who sent it, in its lender."
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LifecycleRunView"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "400": {
            "description": "Refused: a parameter, header or body field failed validation (details names at most 20 of them, detailCount how many there were), the path's percent-encoding cannot be decoded, the body is not valid JSON, cannot be decompressed, is nested more than 32 levels deep or holds a NUL character or an unpaired surrogate, or a rule refused the request in its own words. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "401": {
            "description": "Sign-in required: a staff host answers only a signed-in pilot staff session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "403": {
            "description": "Refused: another origin, or the caller's role, membership, lender access, recent MFA or a readiness gate does not allow it.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "404": {
            "description": "Not found in the caller's workspace: the lender, or a record the path or the body names (an action's recordId, a customerId, a linked record, an export's close review, a retention run).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "409": {
            "description": "Conflict: what the request names changed since it was read, its Idempotency-Key belongs to another request or its journal entry is cancelled, or the change conflicts with saved records. Nothing was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "410": {
            "description": "Gone: a request with this Idempotency-Key already completed, and the lender's retention policy has since removed its stored result, so it cannot run again. Its entry in Operations remains.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "413": {
            "description": "The body is larger than 2 MB, or holds more than 100,000 values (every object, list, text, number, true, false and null in it).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "415": {
            "description": "The body is not JSON (a form or text body is refused), or its character set or encoding is not supported; send UTF-8 JSON as application/json.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "429": {
            "description": "Too many requests: more than 300 a minute for this client (a signed-in person, a sandbox this server has served, or otherwise the client's network: an IPv4 address or an IPv6 /64), more than 1,200 a minute from its network, or too many new sandboxes from its network or on this server (try again in an hour).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait: 60 after the request limit, 3600 after the new-sandbox limit.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "500": {
            "description": "The service failed. With committed false nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); with operation completed a request with the key was saved; otherwise the outcome is unconfirmed: check Operations, or repeat the same request with its Idempotency-Key.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          },
          "503": {
            "description": "Busy or unavailable: a database limit turned the request away (a busy lender or workspace, a lock or statement past its limit, a lost or unavailable connection), the same request is still running (operation running), or a service it needs is unavailable or not configured: the key service, or private storage or the identity provider out of reach. committed false says nothing was saved (for a request with an Idempotency-Key, nothing sent with the key); operation completed says a request with the key was saved.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorBody"
                }
              }
            },
            "headers": {
              "Retry-After": {
                "description": "Seconds to wait before trying again: sent when a database limit turned the request away, the same request is still running, or private storage or the identity provider could not be reached or said it is unavailable; absent when a service it needs is not configured, or the key service cannot open protected data.",
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              },
              "X-Valopay-Operation": {
                "$ref": "#/components/headers/X-Valopay-Operation"
              }
            }
          }
        },
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/LifecycleExecuteInput"
              }
            }
          }
        },
        "summary": "Execute an approved run",
        "description": "Administrators only. Removes as many of the run's sources as fit in a two-second budget under the lender lock, each checked again just before it is deleted and given a receipt. A blocked source, or a deletion that cannot be confirmed, stops the run with its reason (status attention) and the sources after it wait; nothing is skipped silently. Send it again to continue until the status is completed: sources not yet attempted go first."
      }
    }
  },
  "components": {
    "schemas": {
      "HealthStatus": {
        "type": "object",
        "properties": {
          "status": {
            "type": "string"
          },
          "build": {
            "type": "string"
          },
          "startedAt": {
            "type": "string"
          },
          "uptimeSeconds": {
            "type": "integer"
          },
          "scheduler": {
            "$ref": "#/components/schemas/SchedulerStatus"
          }
        },
        "required": [
          "status",
          "build",
          "startedAt",
          "uptimeSeconds",
          "scheduler"
        ],
        "description": "The liveness answer: the build, when the process started, its uptime and what the close scheduler is doing."
      },
      "SchedulerRun": {
        "type": "object",
        "properties": {
          "runId": {
            "type": "string"
          },
          "at": {
            "type": "string"
          },
          "durationMs": {
            "type": "integer"
          },
          "initialised": {
            "type": "integer"
          },
          "examined": {
            "type": "integer"
          },
          "closed": {
            "type": "integer"
          },
          "skipped": {
            "type": "integer"
          },
          "failed": {
            "type": "integer"
          },
          "paused": {
            "type": "integer"
          },
          "batches": {
            "type": "integer"
          }
        },
        "required": [
          "runId",
          "at",
          "durationMs",
          "initialised",
          "examined",
          "closed",
          "skipped",
          "failed"
        ],
        "description": "The last scheduler pass that found work: its id, when it ran, how long it took, how many batches it read and what it did, including idle sandboxes whose automatic close it paused."
      },
      "SchedulerStatus": {
        "type": "object",
        "properties": {
          "state": {
            "type": "string",
            "enum": [
              "not_started",
              "running",
              "off",
              "external",
              "stopped"
            ],
            "description": "running: this process schedules the daily closes. off: it schedules none (VALOPAY_CLOSE_SCHEDULER=off). external: it schedules none because a separate scheduled job runs them with the one-shot close pass (VALOPAY_CLOSE_SCHEDULER=external), which this process cannot observe. not_started and stopped: the scheduler has not started yet, or has stopped."
          },
          "intervalMs": {
            "type": [
              "integer",
              "null"
            ]
          },
          "ticks": {
            "type": "integer"
          },
          "lastTickAt": {
            "type": [
              "string",
              "null"
            ]
          },
          "lastRun": {
            "oneOf": [
              {
                "$ref": "#/components/schemas/SchedulerRun"
              },
              {
                "type": "null"
              }
            ]
          },
          "lastSuccessAt": {
            "type": [
              "string",
              "null"
            ]
          },
          "lastErrorAt": {
            "type": [
              "string",
              "null"
            ]
          }
        },
        "required": [
          "state",
          "intervalMs",
          "ticks",
          "lastTickAt",
          "lastRun"
        ],
        "description": "Whether closes are scheduled in this process, how often it looks, when it last looked and its last pass with work."
      },
      "DatabaseCheck": {
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "enum": [
              "ok",
              "failed"
            ]
          },
          "latencyMs": {
            "type": "integer"
          }
        },
        "required": [
          "status",
          "latencyMs"
        ],
        "description": "One round trip to the database and how long it took."
      },
      "SchemaCheck": {
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "enum": [
              "ok",
              "indexes_missing",
              "incomplete",
              "unchecked"
            ],
            "description": "ok: every table, column, unique index, check constraint and read index this build needs is present. indexes_missing: ready, but a read index a migration adds is missing, so some reads are slower until it is applied. incomplete: a table, column, unique index or check constraint is missing, so the instance is not ready. unchecked: the database did not answer. The server log names what is missing and where it comes from."
          }
        },
        "required": [
          "status"
        ],
        "description": "Whether the database holds every table, column, unique index, check constraint and read index this build needs: ok, indexes_missing (ready, some reads slower), incomplete (not ready) or unchecked while the database does not answer. The server log, not the answer, names what is missing."
      },
      "ReadinessStatus": {
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "enum": [
              "ok",
              "degraded"
            ]
          },
          "build": {
            "type": "string"
          },
          "checks": {
            "type": "object",
            "properties": {
              "database": {
                "$ref": "#/components/schemas/DatabaseCheck"
              },
              "schema": {
                "$ref": "#/components/schemas/SchemaCheck"
              }
            },
            "required": [
              "database",
              "schema"
            ]
          }
        },
        "required": [
          "status",
          "build",
          "checks"
        ],
        "description": "The readiness answer: ok, or degraded while the database does not answer or lacks a table, column, unique index or check constraint this build needs."
      },
      "RecordData": {
        "type": "object",
        "additionalProperties": {},
        "description": "A record's data: the fields the kind's schema declares, and anything else a caller stored."
      },
      "ValopayRecord": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "merchantId": {
            "type": "string"
          },
          "kind": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "status": {
            "type": "string"
          },
          "reference": {
            "type": "string"
          },
          "amountKobo": {
            "type": "integer"
          },
          "customerId": {
            "type": "string"
          },
          "createdAt": {
            "type": "string"
          },
          "updatedAt": {
            "type": "string"
          },
          "data": {
            "$ref": "#/components/schemas/RecordData"
          }
        },
        "required": [
          "id",
          "merchantId",
          "kind",
          "name",
          "status",
          "reference",
          "amountKobo",
          "customerId",
          "createdAt",
          "updatedAt",
          "data"
        ],
        "description": "A stored record of any kind, with its lender, status, reference, amount in kobo and data."
      },
      "RecordInput": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "minLength": 1
          },
          "status": {
            "type": "string",
            "maxLength": 100
          },
          "reference": {
            "type": "string",
            "maxLength": 200
          },
          "amountKobo": {
            "type": "integer",
            "minimum": 0
          },
          "customerId": {
            "type": "string",
            "maxLength": 100
          },
          "data": {
            "$ref": "#/components/schemas/RecordData"
          }
        },
        "required": [
          "name"
        ],
        "description": "A new record: only the name is required, and it cannot be empty; the kind's default status applies when none is given. A status is at most 100 characters, a reference 200 and a customerId 100."
      },
      "RecordUpdate": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "minLength": 1
          },
          "status": {
            "type": "string",
            "maxLength": 100
          },
          "reference": {
            "type": "string",
            "maxLength": 200
          },
          "amountKobo": {
            "type": "integer",
            "minimum": 0
          },
          "customerId": {
            "type": "string",
            "maxLength": 100
          },
          "data": {
            "$ref": "#/components/schemas/RecordData"
          },
          "expectedUpdatedAt": {
            "type": "string"
          }
        },
        "required": [],
        "description": "The fields to change on a record; omitted fields keep their values. In data, a field sent as null is removed. A name cannot be empty, and a status, reference or customerId is bounded as a new record's is."
      },
      "Merchant": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "shortName": {
            "type": "string"
          },
          "segment": {
            "type": "string"
          },
          "mode": {
            "type": "string"
          },
          "status": {
            "type": "string"
          },
          "provider": {
            "type": "string"
          },
          "monthlyVolume": {
            "type": "integer"
          },
          "killSwitch": {
            "type": "boolean"
          },
          "preDataReady": {
            "type": "boolean"
          },
          "preLiveReady": {
            "type": "boolean"
          }
        },
        "required": [
          "id",
          "name",
          "shortName",
          "segment",
          "mode",
          "status",
          "provider",
          "monthlyVolume",
          "killSwitch",
          "preDataReady",
          "preLiveReady"
        ],
        "description": "A lender: its mode (observation or instruction), provider, volume, kill switch and readiness flags."
      },
      "Workspace": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string"
          },
          "environment": {
            "type": "string"
          },
          "actor": {
            "type": "string"
          },
          "role": {
            "type": "string"
          },
          "authenticated": {
            "type": "boolean"
          },
          "merchants": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Merchant"
            }
          },
          "roles": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "productionEnabled": {
            "type": "boolean"
          },
          "accessMode": {
            "type": "string",
            "enum": [
              "sandbox",
              "staff"
            ],
            "description": "Whether the server authorises a demo persona or a provisioned staff membership."
          },
          "viewerScope": {
            "type": "string",
            "description": "Opaque workspace/user scope for browser preferences; never an authorisation credential."
          }
        },
        "required": [
          "name",
          "environment",
          "actor",
          "role",
          "authenticated",
          "merchants",
          "roles",
          "productionEnabled"
        ],
        "description": "The caller's workspace: who is acting, in which role, whether they signed in, and the lenders and roles available."
      },
      "Metric": {
        "type": "object",
        "properties": {
          "key": {
            "type": "string"
          },
          "label": {
            "type": "string"
          },
          "value": {
            "type": "number"
          },
          "unit": {
            "type": "string"
          },
          "detail": {
            "type": "string"
          }
        },
        "required": [
          "key",
          "label",
          "value",
          "unit",
          "detail"
        ],
        "description": "A named measurement with its unit and the basis it was derived from."
      },
      "Alert": {
        "type": "object",
        "properties": {
          "key": {
            "type": "string"
          },
          "severity": {
            "type": "string"
          },
          "title": {
            "type": "string"
          },
          "detail": {
            "type": "string"
          },
          "count": {
            "type": "integer"
          },
          "since": {
            "type": "string"
          },
          "linkedRecordId": {
            "type": "string"
          }
        },
        "required": [
          "key",
          "severity",
          "title",
          "detail"
        ],
        "description": "An NFR-OBS-02 alert: what condition holds, how severe it is, since when and the record it points at."
      },
      "Overview": {
        "type": "object",
        "properties": {
          "metrics": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Metric"
            }
          },
          "queues": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Metric"
            }
          },
          "activity": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ValopayRecord"
            }
          },
          "upcoming": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ValopayRecord"
            }
          },
          "mode": {
            "type": "string"
          },
          "environment": {
            "type": "string"
          },
          "lastClose": {
            "type": "string"
          },
          "nextClose": {
            "type": "string"
          },
          "closeTime": {
            "type": "string"
          },
          "alerts": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Alert"
            }
          },
          "closeSchedule": {
            "$ref": "#/components/schemas/EffectiveCloseSchedule"
          }
        },
        "required": [
          "metrics",
          "queues",
          "activity",
          "upcoming",
          "mode",
          "environment",
          "lastClose",
          "nextClose",
          "closeTime",
          "alerts"
        ],
        "description": "The overview: metrics, queues, recent activity, upcoming due items, the close schedule and the alerts."
      },
      "RecordList": {
        "type": "object",
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ValopayRecord"
            }
          },
          "total": {
            "type": "integer"
          },
          "nextOffset": {
            "type": "integer"
          }
        },
        "required": [
          "items",
          "total"
        ],
        "description": "One page of records with the filtered total; nextOffset is present while more rows remain."
      },
      "ActionInput": {
        "type": "object",
        "properties": {
          "action": {
            "type": "string"
          },
          "recordId": {
            "type": "string"
          },
          "reason": {
            "type": "string"
          },
          "data": {
            "$ref": "#/components/schemas/RecordData"
          },
          "expectedUpdatedAt": {
            "type": "string"
          }
        },
        "required": [
          "action"
        ],
        "description": "An action to run: its name, the record it applies to, the reason for it and any data it needs."
      },
      "ActionResult": {
        "type": "object",
        "properties": {
          "message": {
            "type": "string"
          },
          "record": {
            "$ref": "#/components/schemas/ValopayRecord"
          },
          "data": {
            "$ref": "#/components/schemas/RecordData"
          }
        },
        "required": [
          "message",
          "data"
        ],
        "description": "What an action did, in words, with the record it produced or changed and any data it returns."
      },
      "ImportInput": {
        "type": "object",
        "properties": {
          "kind": {
            "type": "string"
          },
          "csv": {
            "type": "string"
          },
          "syntheticOnly": {
            "type": "boolean"
          },
          "commit": {
            "type": "boolean"
          },
          "mapping": {
            "$ref": "#/components/schemas/RecordData"
          },
          "amountUnit": {
            "type": "string",
            "enum": [
              "naira",
              "kobo"
            ],
            "description": "Unit used by source amount values; defaults to kobo for existing API clients. The console requires an explicit choice."
          }
        },
        "required": [
          "kind",
          "csv",
          "syntheticOnly",
          "commit"
        ],
        "description": "A synthetic CSV to preview or commit for one kind, with an optional column mapping."
      },
      "ImportRow": {
        "type": "object",
        "properties": {
          "row": {
            "type": "integer"
          },
          "status": {
            "type": "string"
          },
          "message": {
            "type": "string"
          }
        },
        "required": [
          "row",
          "status",
          "message"
        ],
        "description": "The outcome of one imported row."
      },
      "ImportResult": {
        "type": "object",
        "properties": {
          "valid": {
            "type": "integer"
          },
          "invalid": {
            "type": "integer"
          },
          "imported": {
            "type": "integer"
          },
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ImportRow"
            }
          },
          "columns": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "preview": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "row": {
                  "type": "integer"
                },
                "values": {
                  "$ref": "#/components/schemas/RecordData"
                },
                "amountKobo": {
                  "type": "integer"
                }
              },
              "required": [
                "row",
                "values"
              ]
            }
          },
          "skipped": {
            "type": "integer"
          },
          "warnings": {
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        },
        "required": [
          "valid",
          "invalid",
          "imported",
          "rows"
        ],
        "description": "How many rows were valid, invalid and imported, and each row's outcome. warnings, when present, says which name or reference came from a fallback (the reference, a row number or a generated reference) while a column was left unused, and the check and the commit are not refused for it."
      },
      "Report": {
        "type": "object",
        "properties": {
          "metrics": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Metric"
            }
          },
          "billing": {
            "$ref": "#/components/schemas/RecordData"
          },
          "experiment": {
            "$ref": "#/components/schemas/RecordData"
          },
          "operational": {
            "$ref": "#/components/schemas/RecordData"
          },
          "closes": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ValopayRecord"
            }
          }
        },
        "required": [
          "metrics",
          "billing",
          "experiment",
          "operational",
          "closes"
        ],
        "description": "The reports: metrics, billing, the experiment, operational measurement and the daily closes."
      },
      "Gate": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "title": {
            "type": "string"
          },
          "description": {
            "type": "string"
          },
          "status": {
            "type": "string"
          },
          "evidence": {
            "type": "string"
          },
          "due": {
            "type": "string"
          }
        },
        "required": [
          "id",
          "title",
          "description",
          "status",
          "evidence",
          "due"
        ],
        "description": "One readiness gate: what it needs, its status and the evidence recorded."
      },
      "Gates": {
        "type": "object",
        "properties": {
          "prerequisites": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Gate"
            }
          },
          "decisions": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Gate"
            }
          },
          "limitations": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "cashKobo": {
            "type": "integer"
          },
          "burnKobo": {
            "type": "integer"
          }
        },
        "required": [
          "prerequisites",
          "decisions",
          "limitations",
          "cashKobo",
          "burnKobo"
        ],
        "description": "The prerequisites and decisions, the sandbox's limitations, and the cash and burn figures used for the funding decision."
      },
      "Timeline": {
        "type": "object",
        "properties": {
          "customer": {
            "$ref": "#/components/schemas/ValopayRecord"
          },
          "position": {
            "$ref": "#/components/schemas/RecordData"
          },
          "events": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ValopayRecord"
            }
          },
          "mandates": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ValopayRecord"
            }
          },
          "dueItems": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ValopayRecord"
            }
          },
          "payments": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ValopayRecord"
            }
          }
        },
        "required": [
          "customer",
          "position",
          "events",
          "mandates",
          "dueItems",
          "payments"
        ],
        "description": "A customer, their derived position, and every related event, mandate, due item and payment."
      },
      "Settings": {
        "type": "object",
        "properties": {
          "merchant": {
            "$ref": "#/components/schemas/Merchant"
          },
          "settings": {
            "$ref": "#/components/schemas/RecordData"
          },
          "permissions": {
            "$ref": "#/components/schemas/RecordData"
          },
          "integrations": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ValopayRecord"
            }
          },
          "members": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ValopayRecord"
            }
          },
          "calendar": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ValopayRecord"
            }
          },
          "closeSchedule": {
            "$ref": "#/components/schemas/EffectiveCloseSchedule"
          },
          "revision": {
            "type": "string"
          }
        },
        "required": [
          "merchant",
          "settings",
          "permissions",
          "integrations",
          "members",
          "calendar"
        ],
        "description": "A lender's settings and the caller's permissions, with integrations, members and the business calendar."
      },
      "SettingsInput": {
        "type": "object",
        "properties": {
          "executionStart": {
            "type": "integer"
          },
          "executionEnd": {
            "type": "integer"
          },
          "authorisationMode": {
            "type": "string"
          },
          "contactRoute": {
            "type": "string"
          },
          "minimumTicketKobo": {
            "type": "integer"
          },
          "defaultOwner": {
            "type": "string"
          },
          "policyChangeRequiresConsent": {
            "type": "boolean"
          },
          "unallocatedAlertThreshold": {
            "type": "integer"
          },
          "notificationCostAlertKobo": {
            "type": "integer"
          },
          "closeTime": {
            "type": "string"
          },
          "scheduledCloseEnabled": {
            "type": "boolean"
          },
          "expectedRevision": {
            "type": "string"
          }
        },
        "required": [],
        "description": "The execution settings to change; every field is optional."
      },
      "ExportInput": {
        "type": "object",
        "properties": {
          "kind": {
            "type": "string"
          },
          "customerId": {
            "type": "string"
          },
          "closeReviewId": {
            "type": "string"
          },
          "format": {
            "type": "string",
            "enum": [
              "json",
              "csv",
              "pdf"
            ]
          }
        },
        "required": [
          "kind",
          "format"
        ],
        "description": "What to export (a record kind, gate-pack, billing, dispute-pack or customer-pack with a customerId) and in which format."
      },
      "ExportResult": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "downloadUrl": {
            "type": "string"
          },
          "status": {
            "type": "string",
            "enum": [
              "queued",
              "running",
              "ready",
              "failed"
            ]
          },
          "stage": {
            "type": "string",
            "enum": [
              "queued",
              "checking",
              "rendering",
              "uploading",
              "confirming",
              "ready",
              "failed"
            ]
          },
          "lastProgressAt": {
            "type": "string"
          },
          "stalled": {
            "type": "boolean"
          },
          "retryAllowed": {
            "type": "boolean"
          },
          "recoveryAt": {
            "type": "string"
          },
          "expiredAt": {
            "type": "string"
          },
          "kind": {
            "type": "string"
          },
          "format": {
            "type": "string"
          },
          "customerId": {
            "type": "string"
          },
          "requestedAt": {
            "type": "string"
          },
          "attempts": {
            "type": "integer"
          },
          "checksum": {
            "type": "string"
          },
          "generatedAt": {
            "type": "string"
          },
          "byteLength": {
            "type": "integer"
          },
          "generationMs": {
            "type": "integer"
          },
          "error": {
            "type": "string"
          }
        },
        "required": [
          "id",
          "downloadUrl"
        ],
        "description": "Saved export job identity, status and retry details. Checksum, generatedAt and file size appear only when ready; the download route rejects unfinished jobs. Optional status retains compatibility with older immediate-export responses."
      },
      "EffectiveCloseSchedule": {
        "type": "object",
        "properties": {
          "time": {
            "type": "string"
          },
          "enabled": {
            "type": "boolean"
          },
          "automatic": {
            "type": "boolean"
          },
          "nextAt": {
            "type": [
              "string",
              "null"
            ]
          },
          "runtimeState": {
            "type": "string",
            "enum": [
              "not_started",
              "running",
              "off",
              "external",
              "stopped"
            ],
            "description": "The close service as this process sees it (the health answer's scheduler state). With external a separate scheduled job runs the closes: nothing is advertised as automatic, but a close that job has not run is still missed."
          },
          "serviceIssue": {
            "type": [
              "string",
              "null"
            ],
            "enum": [
              "starting",
              "delayed",
              "failed",
              null
            ]
          },
          "missed": {
            "type": "boolean"
          },
          "overdueMinutes": {
            "type": "integer"
          },
          "lateAfterMinutes": {
            "type": "integer"
          },
          "lastAt": {
            "type": [
              "string",
              "null"
            ]
          },
          "lastTrigger": {
            "type": [
              "string",
              "null"
            ]
          },
          "lastCheckedAt": {
            "type": [
              "string",
              "null"
            ]
          },
          "lastErrorAt": {
            "type": [
              "string",
              "null"
            ]
          },
          "failedAttempts": {
            "type": "integer"
          },
          "retryAt": {
            "type": [
              "string",
              "null"
            ]
          },
          "pausedForInactivityAt": {
            "type": [
              "string",
              "null"
            ]
          }
        },
        "required": [
          "time",
          "enabled",
          "automatic",
          "nextAt",
          "runtimeState",
          "serviceIssue",
          "missed",
          "overdueMinutes",
          "lateAfterMinutes",
          "lastAt",
          "lastTrigger",
          "lastCheckedAt",
          "lastErrorAt"
        ],
        "description": "Lender schedule combined with the actual scheduler service status. nextAt is present only when automatic closes are available; run history belongs only to this lender. failedAttempts and retryAt describe failed automatic attempts at the pending time (retryAt only while automatic closes are available); pausedForInactivityAt says when the scheduler switched off the automatic close of a sandbox nobody changed. Answers from earlier builds may lack these three fields."
      },
      "QueuePage": {
        "type": "object",
        "required": [
          "items",
          "related",
          "total",
          "offset",
          "counts",
          "owners",
          "types",
          "asOf"
        ],
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ValopayRecord"
            }
          },
          "related": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ValopayRecord"
            }
          },
          "total": {
            "type": "integer",
            "minimum": 0
          },
          "offset": {
            "type": "integer",
            "minimum": 0
          },
          "counts": {
            "type": "object",
            "additionalProperties": {
              "type": "integer",
              "minimum": 0
            }
          },
          "owners": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "types": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "asOf": {
            "type": "string"
          }
        },
        "description": "A bounded priority queue page with complete filter counts, available owners and types, the applied offset and lender-scoped linked records. Counts are calculated before pagination. asOf is the timestamp used to determine overdue and due-today states: a deadline written as a day alone (YYYY-MM-DD) is due all that West Africa Time day and overdue once it ends, one with a time passes at that instant, and one that is not a real date is no deadline."
      },
      "ReconciliationPage": {
        "type": "object",
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ValopayRecord"
            }
          },
          "related": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ValopayRecord"
            }
          },
          "total": {
            "type": "integer"
          },
          "offset": {
            "type": "integer"
          },
          "asOf": {
            "type": "string"
          },
          "precision": {
            "$ref": "#/components/schemas/RecordData"
          }
        },
        "required": [
          "items",
          "related",
          "total",
          "offset",
          "asOf"
        ],
        "description": "A database-filtered reconciliation queue page, with complete count and lender-scoped linked evidence. Precision metadata describes the complete seeded monthly sample."
      },
      "CloseHistoryPage": {
        "type": "object",
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ValopayRecord"
            }
          },
          "total": {
            "type": "integer"
          },
          "allTotal": {
            "type": "integer"
          },
          "offset": {
            "type": "integer"
          },
          "first": {
            "$ref": "#/components/schemas/ValopayRecord"
          },
          "latest": {
            "$ref": "#/components/schemas/ValopayRecord"
          }
        },
        "required": [
          "items",
          "total",
          "allTotal",
          "offset"
        ],
        "description": "Paged close summaries and first/latest closing positions for the entire WAT date range; full REC-07 evidence is fetched separately."
      },
      "CustomerHistoryCounts": {
        "type": "object",
        "properties": {
          "events": {
            "type": "integer"
          },
          "mandates": {
            "type": "integer"
          },
          "dueItems": {
            "type": "integer"
          },
          "payments": {
            "type": "integer"
          }
        },
        "required": [
          "events",
          "mandates",
          "dueItems",
          "payments"
        ],
        "description": "Complete counts or actual offsets for the four customer-history sections."
      },
      "CustomerHistory": {
        "type": "object",
        "properties": {
          "customer": {
            "$ref": "#/components/schemas/ValopayRecord"
          },
          "position": {
            "$ref": "#/components/schemas/RecordData"
          },
          "events": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ValopayRecord"
            }
          },
          "mandates": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ValopayRecord"
            }
          },
          "dueItems": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ValopayRecord"
            }
          },
          "payments": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ValopayRecord"
            }
          },
          "totals": {
            "$ref": "#/components/schemas/CustomerHistoryCounts"
          },
          "offsets": {
            "$ref": "#/components/schemas/CustomerHistoryCounts"
          },
          "focusedRecord": {
            "$ref": "#/components/schemas/ValopayRecord"
          }
        },
        "required": [
          "customer",
          "position",
          "events",
          "mandates",
          "dueItems",
          "payments",
          "totals",
          "offsets"
        ],
        "description": "Bounded pages of customer records with balances derived from every related record, full section counts and an optional lender-scoped selected record."
      },
      "ErrorDetail": {
        "type": "object",
        "properties": {
          "field": {
            "type": "string",
            "description": "The field, query value or header, as a dotted path; empty for the body as a whole."
          },
          "message": {
            "type": "string"
          }
        },
        "required": [
          "field",
          "message"
        ],
        "additionalProperties": false,
        "description": "One field a request got wrong: its dotted path (a query value or header by its name) and what is wrong with it."
      },
      "ErrorBody": {
        "type": "object",
        "properties": {
          "error": {
            "type": "string",
            "description": "What happened, in plain words: a refusal in its rule's own wording, a failure in general words."
          },
          "requestId": {
            "type": "string",
            "description": "The request's reference, also sent as X-Request-Id; quoting it finds the request in the log."
          },
          "details": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ErrorDetail"
            },
            "maxItems": 20,
            "description": "Present when validation failed: the first 20 fields at most, each with what is wrong with it."
          },
          "detailCount": {
            "type": "integer",
            "minimum": 0,
            "description": "Present with details: how many problems validation found, which may be more than details lists."
          },
          "code": {
            "type": "string",
            "enum": [
              "pilot_disabled",
              "configuration_invalid",
              "authentication_required",
              "session_invalid",
              "membership_required",
              "membership_inactive",
              "role_not_permitted",
              "mfa_required",
              "reverification_required"
            ],
            "description": "Present when staff access was refused: why."
          },
          "committed": {
            "type": "boolean",
            "const": false,
            "description": "Present on a failure that saved nothing: the transaction was rolled back, so the request may be sent again as new. For a request with an Idempotency-Key it is decided for the key: present only when nothing sent with the key was or can be saved (its journal entry is cancelled, or nothing was saved under it before this request failed), never while a request with the key was saved or is still running. A read's 500 never carries it."
          },
          "operation": {
            "type": "string",
            "enum": [
              "pending",
              "running",
              "completed",
              "cancelled"
            ],
            "description": "Present when the request's Idempotency-Key has an operations-journal entry whose state is known: completed (a request with the key was saved), running (another attempt with the key is still running it), pending (its outcome is not confirmed yet) or cancelled (nothing sent with the key was or can be saved)."
          }
        },
        "required": [
          "error",
          "requestId"
        ],
        "additionalProperties": false,
        "description": "The body of every refusal and failure: what happened in plain words and the request's reference, with the fields validation refused (at most 20, and how many there were), the staff-access refusal code, whether nothing was saved (committed false) and the state of the request's journal entry (operation)."
      },
      "CreditAssessmentResult": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "tenantId": {
            "type": "string"
          },
          "applicantId": {
            "type": "string"
          },
          "applicationRef": {
            "type": "string"
          },
          "version": {
            "type": "integer"
          },
          "previousResultId": {
            "type": [
              "string",
              "null"
            ]
          },
          "mode": {
            "type": "string",
            "const": "synthetic"
          },
          "createdAt": {
            "type": "string"
          },
          "assessedAsOf": {
            "type": "string"
          },
          "createdBy": {
            "type": "string"
          },
          "state": {
            "type": "string",
            "enum": [
              "review_pending",
              "insufficient_evidence",
              "blocked"
            ]
          },
          "evidence": {
            "type": "object",
            "properties": {
              "status": {
                "type": "string",
                "enum": [
                  "adequate",
                  "insufficient",
                  "blocked"
                ]
              },
              "issues": {
                "type": "array",
                "items": {
                  "type": "object",
                  "properties": {
                    "code": {
                      "type": "string"
                    },
                    "message": {
                      "type": "string"
                    },
                    "severity": {
                      "type": "string",
                      "enum": [
                        "blocking",
                        "warning"
                      ]
                    },
                    "sourceId": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "code",
                    "message",
                    "severity"
                  ],
                  "additionalProperties": false
                }
              },
              "coverageDays": {
                "type": "integer"
              },
              "sourceCount": {
                "type": "integer",
                "minimum": 0
              },
              "latestSourceAsOf": {
                "type": [
                  "string",
                  "null"
                ]
              },
              "earliestSourceAsOf": {
                "type": [
                  "string",
                  "null"
                ]
              },
              "reviewValidUntil": {
                "type": [
                  "string",
                  "null"
                ]
              },
              "requiredAccountIds": {
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "grantVersions": {
                "type": "array",
                "items": {
                  "type": "object",
                  "properties": {
                    "id": {
                      "type": "string"
                    },
                    "version": {
                      "type": "integer"
                    }
                  },
                  "required": [
                    "id",
                    "version"
                  ],
                  "additionalProperties": false
                }
              }
            },
            "required": [
              "status",
              "issues",
              "coverageDays",
              "sourceCount",
              "latestSourceAsOf",
              "earliestSourceAsOf",
              "reviewValidUntil",
              "requiredAccountIds",
              "grantVersions"
            ],
            "additionalProperties": false
          },
          "features": {
            "type": [
              "object",
              "null"
            ],
            "properties": {
              "codeVersion": {
                "type": "string",
                "const": "credit-features-synthetic-v1"
              },
              "currency": {
                "type": "string",
                "const": "NGN"
              },
              "periodDays": {
                "type": "integer",
                "const": 30
              },
              "periodCount": {
                "type": "integer"
              },
              "monthlyIncomeKobo": {
                "type": "array",
                "items": {
                  "type": "integer"
                }
              },
              "sustainableMonthlyIncomeKobo": {
                "type": "integer"
              },
              "observedEssentialMonthlyKobo": {
                "type": "integer"
              },
              "essentialMonthlyKobo": {
                "type": "integer"
              },
              "verifiedCommitmentsMonthlyKobo": {
                "type": "integer"
              },
              "declaredCommitmentsMonthlyKobo": {
                "type": "integer"
              },
              "totalCommitmentsMonthlyKobo": {
                "type": "integer"
              },
              "activeIncomePeriods": {
                "type": "integer"
              },
              "incomeVolatilityBps": {
                "type": [
                  "integer",
                  "null"
                ]
              },
              "largestPayerShareBps": {
                "type": [
                  "integer",
                  "null"
                ]
              },
              "liquidityBufferKobo": {
                "type": [
                  "integer",
                  "null"
                ]
              },
              "unknownInflowKobo": {
                "type": "integer"
              },
              "unknownInflowBps": {
                "type": "integer"
              },
              "includedTransactionRefs": {
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "excludedTransactions": {
                "type": "array",
                "items": {
                  "type": "object",
                  "properties": {
                    "reference": {
                      "type": "string"
                    },
                    "reason": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "reference",
                    "reason"
                  ],
                  "additionalProperties": false
                }
              },
              "duplicatesIgnored": {
                "type": "integer"
              }
            },
            "required": [
              "codeVersion",
              "currency",
              "periodDays",
              "periodCount",
              "monthlyIncomeKobo",
              "sustainableMonthlyIncomeKobo",
              "observedEssentialMonthlyKobo",
              "essentialMonthlyKobo",
              "verifiedCommitmentsMonthlyKobo",
              "declaredCommitmentsMonthlyKobo",
              "totalCommitmentsMonthlyKobo",
              "activeIncomePeriods",
              "incomeVolatilityBps",
              "largestPayerShareBps",
              "liquidityBufferKobo",
              "unknownInflowKobo",
              "unknownInflowBps",
              "includedTransactionRefs",
              "excludedTransactions",
              "duplicatesIgnored"
            ],
            "additionalProperties": false
          },
          "score": {
            "type": [
              "object",
              "null"
            ],
            "properties": {
              "type": {
                "type": "string",
                "const": "rulecard"
              },
              "value": {
                "type": "integer"
              },
              "maximum": {
                "type": "integer",
                "const": 100
              },
              "band": {
                "type": "string",
                "enum": [
                  "stronger",
                  "review",
                  "weaker"
                ]
              },
              "rulecardVersion": {
                "type": "string"
              },
              "factors": {
                "type": "array",
                "items": {
                  "type": "object",
                  "properties": {
                    "code": {
                      "type": "string"
                    },
                    "label": {
                      "type": "string"
                    },
                    "points": {
                      "type": "integer"
                    },
                    "maximum": {
                      "type": "integer"
                    },
                    "reason": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "code",
                    "label",
                    "points",
                    "maximum",
                    "reason"
                  ],
                  "additionalProperties": false
                }
              },
              "disclaimer": {
                "type": "string"
              }
            },
            "required": [
              "type",
              "value",
              "maximum",
              "band",
              "rulecardVersion",
              "factors",
              "disclaimer"
            ],
            "additionalProperties": false
          },
          "affordability": {
            "type": [
              "object",
              "null"
            ],
            "properties": {
              "incomeStressBps": {
                "type": "integer"
              },
              "stressedMonthlyIncomeKobo": {
                "type": "integer"
              },
              "baselineResidualKobo": {
                "type": "integer"
              },
              "stressedResidualKobo": {
                "type": "integer"
              },
              "monthlyCapacityKobo": {
                "type": "integer"
              },
              "peakScheduledMonthlyKobo": {
                "type": "integer"
              },
              "scheduledTotalKobo": {
                "type": "integer"
              },
              "requestedPrincipalKobo": {
                "type": "integer"
              },
              "indicativePrincipalCapacityKobo": {
                "type": "integer"
              },
              "termDays": {
                "type": "integer"
              },
              "debtServiceBps": {
                "type": [
                  "integer",
                  "null"
                ]
              },
              "repaymentMonths": {
                "type": "array",
                "items": {
                  "type": "object",
                  "properties": {
                    "month": {
                      "type": "string"
                    },
                    "amountKobo": {
                      "type": "integer"
                    },
                    "stressedAfterPaymentKobo": {
                      "type": "integer"
                    }
                  },
                  "required": [
                    "month",
                    "amountKobo",
                    "stressedAfterPaymentKobo"
                  ],
                  "additionalProperties": false
                }
              },
              "scheduleAffordable": {
                "type": "boolean"
              }
            },
            "required": [
              "incomeStressBps",
              "stressedMonthlyIncomeKobo",
              "baselineResidualKobo",
              "stressedResidualKobo",
              "monthlyCapacityKobo",
              "peakScheduledMonthlyKobo",
              "scheduledTotalKobo",
              "requestedPrincipalKobo",
              "indicativePrincipalCapacityKobo",
              "termDays",
              "debtServiceBps",
              "repaymentMonths",
              "scheduleAffordable"
            ],
            "additionalProperties": false
          },
          "policy": {
            "type": "object",
            "properties": {
              "id": {
                "type": "string"
              },
              "version": {
                "type": "integer"
              },
              "recommendation": {
                "type": "string",
                "enum": [
                  "review_recommended",
                  "policy_not_met",
                  "insufficient_evidence"
                ]
              },
              "reasons": {
                "type": "array",
                "items": {
                  "type": "string"
                }
              }
            },
            "required": [
              "id",
              "version",
              "recommendation",
              "reasons"
            ],
            "additionalProperties": false
          },
          "snapshotHash": {
            "type": "string"
          },
          "requiredReview": {
            "type": "boolean",
            "const": true
          },
          "billable": {
            "type": "boolean",
            "const": false
          },
          "restrictions": {
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        },
        "required": [
          "id",
          "tenantId",
          "applicantId",
          "applicationRef",
          "version",
          "previousResultId",
          "mode",
          "createdAt",
          "assessedAsOf",
          "createdBy",
          "state",
          "evidence",
          "features",
          "score",
          "affordability",
          "policy",
          "snapshotHash",
          "requiredReview",
          "billable",
          "restrictions"
        ],
        "additionalProperties": false,
        "description": "An illustrative synthetic credit assessment: evidence, features, rule score, affordability and policy recommendation. Never a probability of default or a lending decision; features, score and affordability are null when the evidence or authority does not allow them."
      },
      "CreditReview": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "assessmentId": {
            "type": "string"
          },
          "assessmentVersion": {
            "type": "integer"
          },
          "tenantId": {
            "type": "string"
          },
          "applicantId": {
            "type": "string"
          },
          "reviewer": {
            "type": "string"
          },
          "reviewedAt": {
            "type": "string"
          },
          "outcome": {
            "type": "string",
            "enum": [
              "approve",
              "amend_terms",
              "decline",
              "request_information"
            ]
          },
          "rationale": {
            "type": "string"
          },
          "applicantExplanation": {
            "type": "string"
          },
          "reasonCodes": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "override": {
            "type": "boolean"
          },
          "overrideRationale": {
            "type": [
              "string",
              "null"
            ]
          },
          "mode": {
            "type": "string",
            "const": "synthetic"
          },
          "actualLendingDecision": {
            "type": "boolean",
            "const": false
          },
          "fundsMoved": {
            "type": "boolean",
            "const": false
          },
          "authentication": {
            "type": "string",
            "const": "simulated_sandbox_review"
          }
        },
        "required": [
          "id",
          "assessmentId",
          "assessmentVersion",
          "tenantId",
          "applicantId",
          "reviewer",
          "reviewedAt",
          "outcome",
          "rationale",
          "applicantExplanation",
          "reasonCodes",
          "override",
          "overrideRationale",
          "mode",
          "actualLendingDecision",
          "fundsMoved",
          "authentication"
        ],
        "additionalProperties": false,
        "description": "A recorded sandbox review of one assessment version; never an actual lending decision and never moves funds."
      },
      "CreditDesk": {
        "type": "object",
        "properties": {
          "mode": {
            "type": "string",
            "const": "synthetic"
          },
          "liveEnabled": {
            "type": "boolean",
            "const": false
          },
          "canAssess": {
            "type": "boolean"
          },
          "canReview": {
            "type": "boolean"
          },
          "actor": {
            "type": "string"
          },
          "permissions": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "customerId": {
                  "type": "string"
                },
                "accountRead": {
                  "type": "boolean"
                },
                "creditAssessment": {
                  "type": "boolean"
                }
              },
              "required": [
                "customerId",
                "accountRead",
                "creditAssessment"
              ],
              "additionalProperties": false
            }
          },
          "assessments": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "id": {
                  "type": "string"
                },
                "customerId": {
                  "type": "string"
                },
                "customerName": {
                  "type": "string"
                },
                "scenario": {
                  "type": "string"
                },
                "createdAt": {
                  "type": "string"
                },
                "createdBy": {
                  "type": "string"
                },
                "permissionRestricted": {
                  "type": "boolean"
                },
                "result": {
                  "$ref": "#/components/schemas/CreditAssessmentResult"
                },
                "reviews": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/CreditReview"
                  }
                }
              },
              "required": [
                "id",
                "customerId",
                "customerName",
                "scenario",
                "createdAt",
                "createdBy",
                "permissionRestricted",
                "result",
                "reviews"
              ],
              "additionalProperties": false
            }
          },
          "scenarios": {
            "type": "array",
            "items": {
              "type": "string",
              "enum": [
                "ready",
                "thin_file",
                "stale",
                "refused",
                "high_commitments"
              ]
            }
          },
          "model": {
            "type": "object",
            "properties": {
              "name": {
                "type": "string"
              },
              "version": {
                "type": "string"
              },
              "status": {
                "type": "string"
              },
              "validation": {
                "type": "string"
              },
              "weights": {
                "type": "array",
                "items": {
                  "type": "object",
                  "properties": {
                    "label": {
                      "type": "string"
                    },
                    "maximum": {
                      "type": "integer"
                    }
                  },
                  "required": [
                    "label",
                    "maximum"
                  ],
                  "additionalProperties": false
                }
              }
            },
            "required": [
              "name",
              "version",
              "status",
              "validation",
              "weights"
            ],
            "additionalProperties": false
          },
          "gate": {
            "type": "object",
            "properties": {
              "id": {
                "type": "string",
                "const": "G-CREDIT"
              },
              "enabled": {
                "type": "boolean",
                "const": false
              },
              "requirements": {
                "type": "array",
                "items": {
                  "type": "string"
                }
              }
            },
            "required": [
              "id",
              "enabled",
              "requirements"
            ],
            "additionalProperties": false
          }
        },
        "required": [
          "mode",
          "liveEnabled",
          "canAssess",
          "canReview",
          "actor",
          "permissions",
          "assessments",
          "scenarios",
          "model",
          "gate"
        ],
        "additionalProperties": false,
        "description": "The Credit Desk: the current permissions of each applicant holding any (the applicants are the workspace's customers, listed once; one not listed here holds neither), assessments with their reviews, the illustrative rulecard and the closed credit gate."
      },
      "CashForecast": {
        "type": "object",
        "properties": {
          "tenantId": {
            "type": "string"
          },
          "legalEntityId": {
            "type": "string"
          },
          "currency": {
            "type": "string"
          },
          "asOf": {
            "type": "string"
          },
          "version": {
            "type": "string"
          },
          "inputHash": {
            "type": "string"
          },
          "status": {
            "type": "string",
            "enum": [
              "planning_estimate",
              "unknown_opening_balance"
            ]
          },
          "openingMinor": {
            "type": "integer"
          },
          "persistenceBaselineMinor": {
            "type": "integer"
          },
          "planningBufferMinor": {
            "type": "integer"
          },
          "scenarios": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "name": {
                  "type": "string",
                  "enum": [
                    "base",
                    "downside"
                  ]
                },
                "points": {
                  "type": "array",
                  "items": {
                    "type": "object",
                    "properties": {
                      "day": {
                        "type": "integer"
                      },
                      "date": {
                        "type": "string"
                      },
                      "inflowMinor": {
                        "type": "integer"
                      },
                      "outflowMinor": {
                        "type": "integer"
                      },
                      "closingMinor": {
                        "type": "integer"
                      },
                      "afterPlanningBufferMinor": {
                        "type": "integer"
                      },
                      "shortfallMinor": {
                        "type": "integer"
                      }
                    },
                    "required": [
                      "day",
                      "date",
                      "inflowMinor",
                      "outflowMinor",
                      "closingMinor",
                      "afterPlanningBufferMinor",
                      "shortfallMinor"
                    ],
                    "additionalProperties": false
                  }
                }
              },
              "required": [
                "name",
                "points"
              ],
              "additionalProperties": false
            }
          },
          "includedCommitmentIds": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "excludedCommitmentIds": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "warnings": {
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        },
        "required": [
          "tenantId",
          "legalEntityId",
          "currency",
          "asOf",
          "version",
          "inputHash",
          "status",
          "openingMinor",
          "persistenceBaselineMinor",
          "planningBufferMinor",
          "scenarios",
          "includedCommitmentIds",
          "excludedCommitmentIds",
          "warnings"
        ],
        "additionalProperties": false,
        "description": "Base and downside cash forecasts from approved commitments: a planning estimate, not an available balance."
      },
      "ErpManifest": {
        "type": "object",
        "properties": {
          "schema": {
            "type": "string",
            "const": "valo.erp.review-export.v1"
          },
          "companyId": {
            "type": "string"
          },
          "invoiceAllocations": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "invoiceId": {
                  "type": "string"
                },
                "invoiceVersion": {
                  "type": "string"
                },
                "amountMinor": {
                  "type": "integer"
                }
              },
              "required": [
                "invoiceId",
                "invoiceVersion",
                "amountMinor"
              ],
              "additionalProperties": false
            }
          },
          "creditNotes": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "id": {
                  "type": "string"
                },
                "invoiceId": {
                  "type": "string"
                },
                "amountMinor": {
                  "type": "integer"
                },
                "approved": {
                  "type": "boolean"
                },
                "version": {
                  "type": "string"
                }
              },
              "required": [
                "id",
                "invoiceId",
                "amountMinor",
                "approved",
                "version"
              ],
              "additionalProperties": false
            }
          },
          "grossMinor": {
            "type": "integer"
          },
          "netMinor": {
            "type": "integer"
          },
          "feeMinor": {
            "type": "integer"
          },
          "mappingVersion": {
            "type": "string"
          },
          "requestHash": {
            "type": "string"
          },
          "reviewer": {
            "type": "string"
          },
          "status": {
            "type": "string",
            "const": "not_posted"
          },
          "synthetic": {
            "type": "boolean",
            "const": true
          },
          "manifestHash": {
            "type": "string"
          }
        },
        "required": [
          "schema",
          "companyId",
          "invoiceAllocations",
          "grossMinor",
          "netMinor",
          "feeMinor",
          "mappingVersion",
          "requestHash",
          "reviewer",
          "status",
          "synthetic",
          "manifestHash"
        ],
        "additionalProperties": false,
        "description": "A reviewed accounting export: what an ERP would receive. The service never posts it."
      },
      "VatSchedule": {
        "type": "object",
        "properties": {
          "tenantId": {
            "type": "string"
          },
          "legalEntityId": {
            "type": "string"
          },
          "currency": {
            "type": "string"
          },
          "period": {
            "type": "string"
          },
          "configurationVersion": {
            "type": "string"
          },
          "outputVatMinor": {
            "type": "integer"
          },
          "eligibleInputVatMinor": {
            "type": "integer"
          },
          "blockedInputVatMinor": {
            "type": "integer"
          },
          "expectedClosingMinor": {
            "type": "integer"
          },
          "ledgerClosingMinor": {
            "type": "integer"
          },
          "varianceMinor": {
            "type": "integer"
          },
          "status": {
            "type": "string",
            "enum": [
              "review_required",
              "reconciled_for_review"
            ]
          },
          "filingStatus": {
            "type": "string",
            "const": "not_submitted"
          },
          "paymentStatus": {
            "type": "string",
            "const": "not_initiated"
          },
          "lines": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "invoiceId": {
                  "type": "string"
                },
                "kind": {
                  "type": "string",
                  "enum": [
                    "sales_invoice",
                    "sales_credit_note",
                    "purchase_invoice",
                    "purchase_credit_note"
                  ]
                },
                "netMinor": {
                  "type": "integer"
                },
                "vatMinor": {
                  "type": "integer"
                },
                "taxCode": {
                  "type": "string"
                },
                "paidMinor": {
                  "type": "integer"
                },
                "evidenceComplete": {
                  "type": "boolean"
                }
              },
              "required": [
                "invoiceId",
                "kind",
                "netMinor",
                "vatMinor",
                "taxCode",
                "paidMinor",
                "evidenceComplete"
              ],
              "additionalProperties": false
            }
          },
          "missingEvidence": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "excludedBankCreditsMinor": {
            "type": "integer"
          },
          "evidenceHash": {
            "type": "string"
          }
        },
        "required": [
          "tenantId",
          "legalEntityId",
          "currency",
          "period",
          "configurationVersion",
          "outputVatMinor",
          "eligibleInputVatMinor",
          "blockedInputVatMinor",
          "expectedClosingMinor",
          "ledgerClosingMinor",
          "varianceMinor",
          "status",
          "filingStatus",
          "paymentStatus",
          "lines",
          "missingEvidence",
          "excludedBankCreditsMinor",
          "evidenceHash"
        ],
        "additionalProperties": false,
        "description": "A VAT evidence review schedule reconciled to the ledger control: never a filed return or a payment."
      },
      "PayrollPlan": {
        "type": "object",
        "properties": {
          "kind": {
            "type": "string",
            "const": "payroll_funding_plan"
          },
          "scope": {
            "type": "object",
            "properties": {
              "tenantId": {
                "type": "string"
              },
              "legalEntityId": {
                "type": "string"
              },
              "currency": {
                "type": "string"
              }
            },
            "required": [
              "tenantId",
              "legalEntityId",
              "currency"
            ],
            "additionalProperties": false
          },
          "runId": {
            "type": "string"
          },
          "runVersion": {
            "type": "string"
          },
          "sourceHash": {
            "type": "string"
          },
          "maker": {
            "type": "string"
          },
          "sourceApprover": {
            "type": "string"
          },
          "sourceAccountId": {
            "type": "string"
          },
          "paymentDate": {
            "type": "string"
          },
          "asOf": {
            "type": "string"
          },
          "balanceAsOf": {
            "type": "string"
          },
          "reviewVersion": {
            "type": "integer"
          },
          "totalNetMinor": {
            "type": "integer"
          },
          "requiredMinor": {
            "type": "integer"
          },
          "availableMinor": {
            "type": [
              "integer",
              "null"
            ]
          },
          "shortfallMinor": {
            "type": [
              "integer",
              "null"
            ]
          },
          "commitmentsMinor": {
            "type": "integer"
          },
          "estimatedFeesMinor": {
            "type": "integer"
          },
          "bufferMinor": {
            "type": "integer"
          },
          "fundingStatus": {
            "type": "string",
            "enum": [
              "ready_for_review",
              "shortfall",
              "unknown"
            ]
          },
          "approvalStatus": {
            "type": "string",
            "enum": [
              "draft",
              "approved"
            ]
          },
          "items": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "id": {
                  "type": "string"
                },
                "employeeReference": {
                  "type": "string"
                },
                "beneficiaryReference": {
                  "type": "string"
                },
                "beneficiaryVersion": {
                  "type": "string"
                },
                "netMinor": {
                  "type": "integer"
                },
                "status": {
                  "type": "string",
                  "enum": [
                    "planned",
                    "exported",
                    "submitted",
                    "succeeded",
                    "failed",
                    "unknown",
                    "reversed"
                  ]
                },
                "idempotencyKey": {
                  "type": "string"
                },
                "evidenceReference": {
                  "type": "string"
                }
              },
              "required": [
                "id",
                "employeeReference",
                "beneficiaryReference",
                "beneficiaryVersion",
                "netMinor",
                "status",
                "idempotencyKey"
              ],
              "additionalProperties": false
            }
          },
          "frozenHash": {
            "type": "string"
          },
          "checker": {
            "type": "string"
          },
          "approvedHash": {
            "type": "string"
          },
          "evidenceAuthority": {
            "type": "object",
            "properties": {
              "maker": {
                "type": "string"
              },
              "checker": {
                "type": "string"
              },
              "identityHash": {
                "type": "string"
              }
            },
            "required": [
              "maker",
              "checker",
              "identityHash"
            ],
            "additionalProperties": false
          },
          "liveDispatchAllowed": {
            "type": "boolean",
            "const": false
          }
        },
        "required": [
          "kind",
          "scope",
          "runId",
          "runVersion",
          "sourceHash",
          "maker",
          "sourceApprover",
          "sourceAccountId",
          "paymentDate",
          "asOf",
          "balanceAsOf",
          "reviewVersion",
          "totalNetMinor",
          "requiredMinor",
          "availableMinor",
          "shortfallMinor",
          "commitmentsMinor",
          "estimatedFeesMinor",
          "bufferMinor",
          "fundingStatus",
          "approvalStatus",
          "items",
          "frozenHash",
          "liveDispatchAllowed"
        ],
        "additionalProperties": false,
        "description": "A funding plan for an approved net-pay run: maker, checker, funding state and each item's state. It pays nobody."
      },
      "PayrollManifest": {
        "type": "object",
        "properties": {
          "scope": {
            "type": "object",
            "properties": {
              "tenantId": {
                "type": "string"
              },
              "legalEntityId": {
                "type": "string"
              },
              "currency": {
                "type": "string"
              }
            },
            "required": [
              "tenantId",
              "legalEntityId",
              "currency"
            ],
            "additionalProperties": false
          },
          "runId": {
            "type": "string"
          },
          "runVersion": {
            "type": "string"
          },
          "sourceAccountId": {
            "type": "string"
          },
          "paymentDate": {
            "type": "string"
          },
          "checker": {
            "type": "string"
          },
          "approvedHash": {
            "type": "string"
          },
          "itemCount": {
            "type": "integer",
            "minimum": 0
          },
          "totalMinor": {
            "type": "integer"
          },
          "items": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "id": {
                  "type": "string"
                },
                "beneficiaryReference": {
                  "type": "string"
                },
                "beneficiaryVersion": {
                  "type": "string"
                },
                "netMinor": {
                  "type": "integer"
                },
                "idempotencyKey": {
                  "type": "string"
                }
              },
              "required": [
                "id",
                "beneficiaryReference",
                "beneficiaryVersion",
                "netMinor",
                "idempotencyKey"
              ],
              "additionalProperties": false
            }
          },
          "paymentStatus": {
            "type": "string",
            "const": "not_evidenced"
          },
          "manifestHash": {
            "type": "string"
          },
          "warning": {
            "type": "string"
          }
        },
        "required": [
          "scope",
          "runId",
          "runVersion",
          "sourceAccountId",
          "paymentDate",
          "checker",
          "approvedHash",
          "itemCount",
          "totalMinor",
          "items",
          "paymentStatus",
          "manifestHash",
          "warning"
        ],
        "additionalProperties": false,
        "description": "An approved payroll bank export: the unsent items and their totals. It does not reserve funds or prove payment."
      },
      "CashDesk": {
        "type": "object",
        "properties": {
          "initialised": {
            "type": "boolean"
          },
          "scope": {
            "type": "object",
            "properties": {
              "tenantId": {
                "type": "string"
              },
              "legalEntityId": {
                "type": "string"
              },
              "currency": {
                "type": "string"
              }
            },
            "required": [
              "tenantId",
              "legalEntityId",
              "currency"
            ],
            "additionalProperties": false
          },
          "name": {
            "type": "string"
          },
          "accounts": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "tenantId": {
                  "type": "string"
                },
                "legalEntityId": {
                  "type": "string"
                },
                "currency": {
                  "type": "string"
                },
                "id": {
                  "type": "string"
                },
                "name": {
                  "type": "string"
                },
                "source": {
                  "type": "string"
                },
                "sourceDefinition": {
                  "type": "string"
                },
                "authorised": {
                  "type": "boolean"
                },
                "bookedMinor": {
                  "type": "integer"
                },
                "availableMinor": {
                  "type": [
                    "integer",
                    "null"
                  ]
                },
                "pendingMinor": {
                  "type": [
                    "integer",
                    "null"
                  ]
                },
                "balanceAsOf": {
                  "type": "string"
                },
                "fetchedAt": {
                  "type": "string"
                },
                "coverageComplete": {
                  "type": "boolean"
                }
              },
              "required": [
                "tenantId",
                "legalEntityId",
                "currency",
                "id",
                "name",
                "source",
                "sourceDefinition",
                "authorised",
                "bookedMinor",
                "availableMinor",
                "pendingMinor",
                "balanceAsOf",
                "fetchedAt",
                "coverageComplete"
              ],
              "additionalProperties": false
            }
          },
          "positions": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "currency": {
                  "type": "string"
                },
                "bookedMinor": {
                  "type": "integer"
                },
                "availableMinor": {
                  "type": [
                    "integer",
                    "null"
                  ]
                },
                "pendingMinor": {
                  "type": [
                    "integer",
                    "null"
                  ]
                },
                "incomeMinor": {
                  "type": "integer"
                },
                "expenseMinor": {
                  "type": "integer"
                },
                "accountCount": {
                  "type": "integer",
                  "minimum": 0
                },
                "omittedAccountIds": {
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                },
                "oldestBalanceAsOf": {
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "latestFetchedAt": {
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "qualified": {
                  "type": "boolean"
                },
                "warnings": {
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                }
              },
              "required": [
                "currency",
                "bookedMinor",
                "availableMinor",
                "pendingMinor",
                "incomeMinor",
                "expenseMinor",
                "accountCount",
                "omittedAccountIds",
                "oldestBalanceAsOf",
                "latestFetchedAt",
                "qualified",
                "warnings"
              ],
              "additionalProperties": false
            }
          },
          "commitments": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "tenantId": {
                  "type": "string"
                },
                "legalEntityId": {
                  "type": "string"
                },
                "currency": {
                  "type": "string"
                },
                "id": {
                  "type": "string"
                },
                "label": {
                  "type": "string"
                },
                "direction": {
                  "type": "string",
                  "enum": [
                    "inflow",
                    "outflow"
                  ]
                },
                "amountMinor": {
                  "type": "integer"
                },
                "dueAt": {
                  "type": "string"
                },
                "knownAt": {
                  "type": "string"
                },
                "approved": {
                  "type": "boolean"
                },
                "source": {
                  "type": "string",
                  "enum": [
                    "invoice",
                    "bill",
                    "payroll",
                    "recurring_assumption"
                  ]
                },
                "version": {
                  "type": "string"
                }
              },
              "required": [
                "tenantId",
                "legalEntityId",
                "currency",
                "id",
                "label",
                "direction",
                "amountMinor",
                "dueAt",
                "knownAt",
                "approved",
                "source",
                "version"
              ],
              "additionalProperties": false
            }
          },
          "forecast": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/CashForecast"
              },
              {
                "type": "null"
              }
            ]
          },
          "erpDrafts": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "id": {
                  "type": "string"
                },
                "status": {
                  "type": "string"
                },
                "name": {
                  "type": "string"
                },
                "createdAt": {
                  "type": "string"
                },
                "draft": {
                  "type": "object",
                  "properties": {
                    "kind": {
                      "type": "string",
                      "const": "erp_receipt_draft"
                    },
                    "input": {
                      "type": "object",
                      "properties": {
                        "scope": {
                          "type": "object",
                          "properties": {
                            "tenantId": {
                              "type": "string"
                            },
                            "legalEntityId": {
                              "type": "string"
                            },
                            "currency": {
                              "type": "string"
                            }
                          },
                          "required": [
                            "tenantId",
                            "legalEntityId",
                            "currency"
                          ],
                          "additionalProperties": false
                        },
                        "maker": {
                          "type": "string"
                        },
                        "postingDate": {
                          "type": "string"
                        },
                        "canonicalReceiptId": {
                          "type": "string"
                        },
                        "bankReference": {
                          "type": "string"
                        },
                        "grossMinor": {
                          "type": "integer"
                        },
                        "feeMinor": {
                          "type": "integer"
                        },
                        "netMinor": {
                          "type": "integer"
                        },
                        "mapping": {
                          "type": "object",
                          "properties": {
                            "tenantId": {
                              "type": "string"
                            },
                            "legalEntityId": {
                              "type": "string"
                            },
                            "currency": {
                              "type": "string"
                            },
                            "companyId": {
                              "type": "string"
                            },
                            "provider": {
                              "type": "string",
                              "enum": [
                                "xero",
                                "odoo",
                                "export"
                              ]
                            },
                            "version": {
                              "type": "string"
                            },
                            "active": {
                              "type": "boolean"
                            },
                            "contactId": {
                              "type": "string"
                            },
                            "bankLedgerCode": {
                              "type": "string"
                            },
                            "revenueAccountCode": {
                              "type": "string"
                            },
                            "feeAccountCode": {
                              "type": "string"
                            },
                            "taxCode": {
                              "type": "string"
                            },
                            "financeApproved": {
                              "type": "boolean"
                            }
                          },
                          "required": [
                            "tenantId",
                            "legalEntityId",
                            "currency",
                            "companyId",
                            "provider",
                            "version",
                            "active",
                            "contactId",
                            "bankLedgerCode",
                            "revenueAccountCode",
                            "feeAccountCode",
                            "taxCode",
                            "financeApproved"
                          ],
                          "additionalProperties": false
                        },
                        "invoices": {
                          "type": "array",
                          "items": {
                            "type": "object",
                            "properties": {
                              "tenantId": {
                                "type": "string"
                              },
                              "legalEntityId": {
                                "type": "string"
                              },
                              "currency": {
                                "type": "string"
                              },
                              "id": {
                                "type": "string"
                              },
                              "companyId": {
                                "type": "string"
                              },
                              "contactId": {
                                "type": "string"
                              },
                              "version": {
                                "type": "string"
                              },
                              "outstandingMinor": {
                                "type": "integer"
                              },
                              "taxCode": {
                                "type": "string"
                              }
                            },
                            "required": [
                              "tenantId",
                              "legalEntityId",
                              "currency",
                              "id",
                              "companyId",
                              "contactId",
                              "version",
                              "outstandingMinor",
                              "taxCode"
                            ],
                            "additionalProperties": false
                          }
                        },
                        "allocations": {
                          "type": "array",
                          "items": {
                            "type": "object",
                            "properties": {
                              "invoiceId": {
                                "type": "string"
                              },
                              "invoiceVersion": {
                                "type": "string"
                              },
                              "amountMinor": {
                                "type": "integer"
                              }
                            },
                            "required": [
                              "invoiceId",
                              "invoiceVersion",
                              "amountMinor"
                            ],
                            "additionalProperties": false
                          }
                        },
                        "creditNotes": {
                          "type": "array",
                          "items": {
                            "type": "object",
                            "properties": {
                              "id": {
                                "type": "string"
                              },
                              "invoiceId": {
                                "type": "string"
                              },
                              "amountMinor": {
                                "type": "integer"
                              },
                              "approved": {
                                "type": "boolean"
                              },
                              "version": {
                                "type": "string"
                              }
                            },
                            "required": [
                              "id",
                              "invoiceId",
                              "amountMinor",
                              "approved",
                              "version"
                            ],
                            "additionalProperties": false
                          }
                        },
                        "source": {
                          "type": "string",
                          "enum": [
                            "bank_evidence",
                            "synthetic"
                          ]
                        },
                        "alreadyRecordedReceiptIds": {
                          "type": "array",
                          "items": {
                            "type": "string"
                          }
                        },
                        "closedThrough": {
                          "type": "string"
                        }
                      },
                      "required": [
                        "scope",
                        "maker",
                        "postingDate",
                        "canonicalReceiptId",
                        "bankReference",
                        "grossMinor",
                        "feeMinor",
                        "netMinor",
                        "mapping",
                        "invoices",
                        "allocations",
                        "source"
                      ],
                      "additionalProperties": false
                    },
                    "idempotencyKey": {
                      "type": "string"
                    },
                    "requestHash": {
                      "type": "string"
                    },
                    "status": {
                      "type": "string",
                      "enum": [
                        "proposed",
                        "blocked",
                        "already_recorded",
                        "reviewed"
                      ]
                    },
                    "reasons": {
                      "type": "array",
                      "items": {
                        "type": "string"
                      }
                    },
                    "residuals": {
                      "type": "array",
                      "items": {
                        "type": "object",
                        "properties": {
                          "invoiceId": {
                            "type": "string"
                          },
                          "beforeMinor": {
                            "type": "integer"
                          },
                          "paymentMinor": {
                            "type": "integer"
                          },
                          "creditNoteMinor": {
                            "type": "integer"
                          },
                          "afterMinor": {
                            "type": "integer"
                          }
                        },
                        "required": [
                          "invoiceId",
                          "beforeMinor",
                          "paymentMinor",
                          "creditNoteMinor",
                          "afterMinor"
                        ],
                        "additionalProperties": false
                      }
                    },
                    "review": {
                      "type": "object",
                      "properties": {
                        "reviewer": {
                          "type": "string"
                        },
                        "approvedHash": {
                          "type": "string"
                        }
                      },
                      "required": [
                        "reviewer",
                        "approvedHash"
                      ],
                      "additionalProperties": false
                    },
                    "liveDispatchAllowed": {
                      "type": "boolean",
                      "const": false
                    }
                  },
                  "required": [
                    "kind",
                    "input",
                    "idempotencyKey",
                    "requestHash",
                    "status",
                    "reasons",
                    "residuals",
                    "liveDispatchAllowed"
                  ],
                  "additionalProperties": false
                },
                "manifest": {
                  "$ref": "#/components/schemas/ErpManifest"
                }
              },
              "required": [
                "id",
                "status",
                "name",
                "createdAt",
                "draft"
              ],
              "additionalProperties": false
            }
          },
          "vat": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/VatSchedule"
              },
              {
                "type": "null"
              }
            ]
          },
          "vatExports": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "id": {
                  "type": "string"
                },
                "createdAt": {
                  "type": "string"
                },
                "schedule": {
                  "$ref": "#/components/schemas/VatSchedule"
                },
                "reviewer": {
                  "type": "string"
                }
              },
              "required": [
                "id",
                "createdAt",
                "schedule",
                "reviewer"
              ],
              "additionalProperties": false
            }
          },
          "payrollPlans": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "id": {
                  "type": "string"
                },
                "status": {
                  "type": "string"
                },
                "plan": {
                  "$ref": "#/components/schemas/PayrollPlan"
                },
                "summary": {
                  "type": "object",
                  "properties": {
                    "itemCount": {
                      "type": "integer",
                      "minimum": 0
                    },
                    "totalNetMinor": {
                      "type": "integer"
                    },
                    "counts": {
                      "type": "object",
                      "properties": {
                        "planned": {
                          "type": "integer",
                          "minimum": 0
                        },
                        "exported": {
                          "type": "integer",
                          "minimum": 0
                        },
                        "submitted": {
                          "type": "integer",
                          "minimum": 0
                        },
                        "succeeded": {
                          "type": "integer",
                          "minimum": 0
                        },
                        "failed": {
                          "type": "integer",
                          "minimum": 0
                        },
                        "unknown": {
                          "type": "integer",
                          "minimum": 0
                        },
                        "reversed": {
                          "type": "integer",
                          "minimum": 0
                        }
                      },
                      "required": [
                        "planned",
                        "exported",
                        "submitted",
                        "succeeded",
                        "failed",
                        "unknown",
                        "reversed"
                      ],
                      "additionalProperties": false
                    },
                    "status": {
                      "type": "string",
                      "enum": [
                        "completed",
                        "partially_completed",
                        "needs_reconciliation",
                        "submitted",
                        "exported_unpaid",
                        "planning"
                      ]
                    },
                    "liveDispatchAllowed": {
                      "type": "boolean",
                      "const": false
                    }
                  },
                  "required": [
                    "itemCount",
                    "totalNetMinor",
                    "counts",
                    "status",
                    "liveDispatchAllowed"
                  ],
                  "additionalProperties": false
                },
                "manifest": {
                  "$ref": "#/components/schemas/PayrollManifest"
                }
              },
              "required": [
                "id",
                "status",
                "plan",
                "summary"
              ],
              "additionalProperties": false
            }
          },
          "payrollReconciliation": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "id": {
                  "type": "string"
                },
                "runId": {
                  "type": "string"
                },
                "items": {
                  "type": "array",
                  "items": {
                    "type": "object",
                    "properties": {
                      "id": {
                        "type": "string"
                      },
                      "employeeReference": {
                        "type": "string"
                      },
                      "netMinor": {
                        "type": "integer"
                      },
                      "status": {
                        "type": "string",
                        "enum": [
                          "planned",
                          "exported",
                          "submitted",
                          "succeeded",
                          "failed",
                          "unknown",
                          "reversed"
                        ]
                      }
                    },
                    "required": [
                      "id",
                      "employeeReference",
                      "netMinor",
                      "status"
                    ],
                    "additionalProperties": false
                  }
                }
              },
              "required": [
                "id",
                "runId",
                "items"
              ],
              "additionalProperties": false
            }
          },
          "permissions": {
            "type": "object",
            "properties": {
              "read": {
                "type": "boolean"
              },
              "erp": {
                "type": "boolean"
              },
              "payroll": {
                "type": "boolean"
              }
            },
            "required": [
              "read",
              "erp",
              "payroll"
            ],
            "additionalProperties": false
          },
          "limitations": {
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        },
        "required": [
          "initialised",
          "scope",
          "name",
          "accounts",
          "positions",
          "commitments",
          "forecast",
          "erpDrafts",
          "vat",
          "vatExports",
          "payrollPlans",
          "payrollReconciliation",
          "permissions",
          "limitations"
        ],
        "additionalProperties": false,
        "description": "The Cash Desk: a separate sample SME's accounts, positions, commitments, forecast, accounting drafts, VAT schedules and payroll plans, as its permissions allow."
      },
      "ConnectedWorkspace": {
        "type": "object",
        "properties": {
          "mode": {
            "type": "string",
            "const": "synthetic"
          },
          "revision": {
            "type": "string"
          },
          "asOf": {
            "type": "string"
          },
          "role": {
            "type": "string"
          },
          "entity": {
            "type": "object",
            "properties": {
              "id": {
                "type": "string"
              },
              "name": {
                "type": "string"
              },
              "workspaceOwner": {
                "type": "string"
              }
            },
            "required": [
              "id",
              "name",
              "workspaceOwner"
            ],
            "additionalProperties": false
          },
          "customers": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "id": {
                  "type": "string"
                },
                "name": {
                  "type": "string"
                },
                "reference": {
                  "type": "string"
                }
              },
              "required": [
                "id",
                "name",
                "reference"
              ],
              "additionalProperties": false
            }
          },
          "consents": {
            "type": "array",
            "items": {
              "allOf": [
                {
                  "$ref": "#/components/schemas/ValopayRecord"
                },
                {
                  "type": "object",
                  "properties": {
                    "effectiveStatus": {
                      "type": "string",
                      "enum": [
                        "active",
                        "revoked",
                        "expired"
                      ]
                    }
                  },
                  "required": [
                    "effectiveStatus"
                  ]
                }
              ]
            }
          },
          "purposes": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "id": {
                  "type": "string",
                  "enum": [
                    "account_read",
                    "credit_assessment",
                    "merchant_account_read",
                    "erp_draft",
                    "payroll_prepare"
                  ]
                },
                "label": {
                  "type": "string"
                }
              },
              "required": [
                "id",
                "label"
              ],
              "additionalProperties": false
            }
          },
          "gates": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "id": {
                  "type": "string"
                },
                "name": {
                  "type": "string"
                },
                "requires": {
                  "type": "string"
                },
                "status": {
                  "type": "string",
                  "const": "not_enabled"
                },
                "liveEnabled": {
                  "type": "boolean",
                  "const": false
                }
              },
              "required": [
                "id",
                "name",
                "requires",
                "status",
                "liveEnabled"
              ],
              "additionalProperties": false
            }
          },
          "payments": {
            "type": "object",
            "properties": {
              "intents": {
                "type": "array",
                "items": {
                  "$ref": "#/components/schemas/ValopayRecord"
                }
              },
              "dues": {
                "type": "array",
                "items": {
                  "type": "object",
                  "properties": {
                    "id": {
                      "type": "string"
                    },
                    "name": {
                      "type": "string"
                    },
                    "reference": {
                      "type": "string"
                    },
                    "customerId": {
                      "type": "string"
                    },
                    "customerName": {
                      "type": "string"
                    },
                    "outstandingKobo": {
                      "type": "integer"
                    },
                    "blocked": {
                      "type": "boolean"
                    }
                  },
                  "required": [
                    "id",
                    "name",
                    "reference",
                    "customerId",
                    "customerName",
                    "outstandingKobo",
                    "blocked"
                  ],
                  "additionalProperties": false
                }
              }
            },
            "required": [
              "intents",
              "dues"
            ],
            "additionalProperties": false
          },
          "credit": {
            "$ref": "#/components/schemas/CreditDesk"
          },
          "cash": {
            "$ref": "#/components/schemas/CashDesk"
          }
        },
        "required": [
          "mode",
          "revision",
          "asOf",
          "role",
          "entity",
          "customers",
          "consents",
          "purposes",
          "gates",
          "payments",
          "credit",
          "cash"
        ],
        "additionalProperties": false,
        "description": "Synthetic connected workspace: granular consents with their effective state, bound sample payment intents, the Credit and Cash Desks and the live gates, every one closed. No read creates sample records."
      },
      "ConnectedActionInput": {
        "type": "object",
        "properties": {
          "action": {
            "type": "string",
            "minLength": 1,
            "maxLength": 80
          },
          "recordId": {
            "type": "string",
            "maxLength": 100
          },
          "reason": {
            "type": "string",
            "minLength": 8,
            "maxLength": 500
          },
          "data": {
            "type": "object",
            "additionalProperties": {},
            "default": {}
          },
          "expectedRevision": {
            "type": "string",
            "maxLength": 80
          }
        },
        "required": [
          "action",
          "reason",
          "expectedRevision"
        ],
        "additionalProperties": false,
        "description": "Action-specific data is validated by the server. Names use consent, payment, credit or cash prefixes. Every action requires the workspace's current revision and a reason: the revision changes with anything the workspace shows or its actions read (the lender and its settings, customers, the instalments it offers, a checkout names or a receipt was applied to and their attempts, connected records, and pay-by-bank receipts with their allocations), not with the lender's history (closes, the audit trail, exports, settled instalments or other payments). No input can enable live routes."
      },
      "CashActionOutcome": {
        "type": "object",
        "properties": {
          "message": {
            "type": "string"
          },
          "record": {
            "$ref": "#/components/schemas/ValopayRecord"
          },
          "data": {
            "type": "object",
            "properties": {
              "manifest": {
                "anyOf": [
                  {
                    "$ref": "#/components/schemas/ErpManifest"
                  },
                  {
                    "$ref": "#/components/schemas/VatSchedule"
                  },
                  {
                    "$ref": "#/components/schemas/PayrollManifest"
                  }
                ]
              },
              "synthetic": {
                "type": "boolean",
                "const": true
              },
              "externalInstructionPerformed": {
                "type": "boolean",
                "const": false
              }
            },
            "required": [
              "synthetic"
            ],
            "additionalProperties": false
          }
        },
        "required": [
          "message",
          "data"
        ],
        "additionalProperties": false,
        "description": "What a Cash Desk action did: its message, the record it saved or changed, and any export it prepared."
      },
      "ConnectedActionResult": {
        "type": "object",
        "properties": {
          "message": {
            "type": "string"
          },
          "record": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/ValopayRecord"
              },
              {
                "$ref": "#/components/schemas/CashActionOutcome"
              }
            ]
          },
          "mode": {
            "type": "string",
            "const": "synthetic"
          },
          "externalInstructionPerformed": {
            "type": "boolean",
            "const": false
          }
        },
        "required": [
          "message",
          "record",
          "mode",
          "externalInstructionPerformed"
        ],
        "additionalProperties": false,
        "description": "Committed sample operation, in the shape its action gives (connectedActionResultFor in lib/valopay-schema): a cash.* action answers its outcome with the Cash Desk record it saved or changed (absent only when the Cash Desk was already set up) and, for an export, the manifest it prepared; every other action answers the record it produced or changed, of the lender the request named: a consent for consent.*, a checkout for payment.*, an assessment for credit.assess and a review for credit.review. A receipt is evidence from the server simulator only."
      },
      "Message": {
        "type": "object",
        "properties": {
          "message": {
            "type": "string"
          }
        },
        "required": [
          "message"
        ],
        "additionalProperties": false,
        "description": "A confirmation in plain words; nothing else changed that the caller needs to read back."
      },
      "OperationView": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "label": {
            "type": "string"
          },
          "actor": {
            "type": "string"
          },
          "role": {
            "type": "string"
          },
          "status": {
            "type": "string",
            "enum": [
              "pending",
              "completed",
              "cancelled"
            ]
          },
          "createdAt": {
            "type": "string"
          },
          "updatedAt": {
            "type": "string"
          },
          "message": {
            "type": "string"
          },
          "recordId": {
            "type": [
              "string",
              "null"
            ]
          },
          "recordKind": {
            "type": [
              "string",
              "null"
            ]
          }
        },
        "required": [
          "id",
          "label",
          "actor",
          "role",
          "status",
          "createdAt",
          "updatedAt",
          "message",
          "recordId",
          "recordKind"
        ],
        "additionalProperties": false,
        "description": "One journal entry: what was asked, by whom, in which role, and whether the service confirmed it. Original request bodies stay private; a completed entry names the record it produced. A refused entry is cancelled and its message says why."
      },
      "OperationList": {
        "type": "object",
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/OperationView"
            },
            "maxItems": 25
          },
          "total": {
            "type": "integer",
            "minimum": 0
          },
          "offset": {
            "type": "integer",
            "minimum": 0
          }
        },
        "required": [
          "items",
          "total",
          "offset"
        ],
        "additionalProperties": false,
        "description": "The caller's journal for one lender, newest first, 25 rows a page."
      },
      "OperationReplayResult": {
        "type": "object",
        "additionalProperties": {},
        "description": "The original route's answer, recovered or re-run under the current validation and authorisation; its shape is that route's response."
      },
      "JourneyCounts": {
        "type": "object",
        "properties": {
          "customers": {
            "type": "integer",
            "minimum": 0
          },
          "batches": {
            "type": "integer",
            "minimum": 0
          },
          "receipts": {
            "type": "integer",
            "minimum": 0
          },
          "openCases": {
            "type": "integer",
            "minimum": 0
          },
          "unassignedCases": {
            "type": "integer",
            "minimum": 0
          },
          "closes": {
            "type": "integer",
            "minimum": 0
          },
          "exports": {
            "type": "integer",
            "minimum": 0
          }
        },
        "required": [
          "customers",
          "batches",
          "receipts",
          "openCases",
          "unassignedCases",
          "closes",
          "exports"
        ],
        "additionalProperties": false,
        "description": "Record counts that place the lender on the pilot journey: customers, committed batches, receipts, open and unassigned cases, closes and ready exports."
      },
      "PilotJourney": {
        "type": "object",
        "properties": {
          "lender": {
            "$ref": "#/components/schemas/Merchant"
          },
          "accessMode": {
            "type": "string",
            "enum": [
              "sandbox",
              "staff"
            ]
          },
          "actor": {
            "type": "string"
          },
          "syntheticOnly": {
            "type": "boolean",
            "const": true
          },
          "counts": {
            "$ref": "#/components/schemas/JourneyCounts"
          }
        },
        "required": [
          "lender",
          "accessMode",
          "actor",
          "syntheticOnly",
          "counts"
        ],
        "additionalProperties": false,
        "description": "The lender, the caller's access mode and the counts behind the journey view. Synthetic throughout."
      },
      "ImportBatchList": {
        "type": "object",
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ValopayRecord"
            },
            "maxItems": 25
          },
          "total": {
            "type": "integer",
            "minimum": 0
          },
          "offset": {
            "type": "integer",
            "minimum": 0
          }
        },
        "required": [
          "items",
          "total",
          "offset"
        ],
        "additionalProperties": false,
        "description": "Import batches newest first, 25 a page, with their source identity, quality totals and check counts but not their rows. A batch saved before check summaries were stored is listed without check counts while the key service cannot open its check."
      },
      "ImportBatchDetail": {
        "type": "object",
        "properties": {
          "batch": {
            "$ref": "#/components/schemas/ValopayRecord"
          },
          "revisions": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ValopayRecord"
            }
          }
        },
        "required": [
          "batch",
          "revisions"
        ],
        "additionalProperties": false,
        "description": "One batch with its source rows (import operator roles only) and every saved revision."
      },
      "BatchVersion": {
        "type": "object",
        "properties": {
          "expectedUpdatedAt": {
            "type": "string",
            "format": "date-time",
            "description": "An RFC 3339 date and time with Z or an offset, such as 2026-09-18T08:00:00+01:00. The service stores and compares the UTC instant."
          }
        },
        "required": [
          "expectedUpdatedAt"
        ],
        "additionalProperties": false,
        "description": "The batch version being committed; a stale version is refused (409)."
      },
      "ImportBatchInput": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "minLength": 1,
            "maxLength": 120
          },
          "kind": {
            "type": "string",
            "enum": [
              "customers",
              "mandates",
              "due-items",
              "attempts",
              "observations"
            ]
          },
          "source": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100
          },
          "sourceBatchId": {
            "type": "string",
            "minLength": 1,
            "maxLength": 120
          },
          "businessDate": {
            "type": "string",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
          },
          "sourceExpectationId": {
            "type": "string",
            "pattern": "^[a-f0-9]{64}$"
          },
          "csv": {
            "type": "string",
            "minLength": 1,
            "maxLength": 1500000
          },
          "mapping": {
            "type": "object",
            "additionalProperties": {
              "type": "string",
              "maxLength": 100
            },
            "default": {}
          },
          "amountUnit": {
            "type": "string",
            "enum": [
              "naira",
              "kobo"
            ]
          },
          "identityColumn": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100
          },
          "syntheticOnly": {
            "type": "boolean",
            "const": true
          },
          "expectedUpdatedAt": {
            "type": "string",
            "format": "date-time",
            "description": "An RFC 3339 date and time with Z or an offset, such as 2026-09-18T08:00:00+01:00. The service stores and compares the UTC instant."
          }
        },
        "required": [
          "name",
          "kind",
          "source",
          "sourceBatchId",
          "csv",
          "amountUnit",
          "identityColumn",
          "syntheticOnly"
        ],
        "additionalProperties": false,
        "description": "A synthetic source batch: source, source batch ID, record kind, mapping, identity column, amount unit and up to 500 CSV rows. syntheticOnly must be true; raw bank details are refused."
      },
      "Assignee": {
        "type": "object",
        "properties": {
          "actor": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "role": {
            "type": "string"
          }
        },
        "required": [
          "actor",
          "name",
          "role"
        ],
        "additionalProperties": false,
        "description": "A person who can own a case or review a close: demo roles in the sandbox, active staff with lender access on a staff host."
      },
      "EvidenceLink": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "reference": {
            "type": "string"
          },
          "kind": {
            "type": "string"
          }
        },
        "required": [
          "id",
          "name",
          "reference",
          "kind"
        ],
        "additionalProperties": false,
        "description": "A record the case can cite as evidence."
      },
      "CaseDetail": {
        "type": "object",
        "properties": {
          "record": {
            "$ref": "#/components/schemas/ValopayRecord"
          },
          "assignees": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Assignee"
            }
          },
          "events": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ValopayRecord"
            }
          },
          "evidence": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/EvidenceLink"
            }
          }
        },
        "required": [
          "record",
          "assignees",
          "events",
          "evidence"
        ],
        "additionalProperties": false,
        "description": "One exception with the people it can be handed to, its handover events and the records it can cite."
      },
      "CaseInput": {
        "type": "object",
        "properties": {
          "action": {
            "type": "string",
            "enum": [
              "claim",
              "handover",
              "update"
            ]
          },
          "expectedUpdatedAt": {
            "type": "string",
            "format": "date-time",
            "description": "An RFC 3339 date and time with Z or an offset, such as 2026-09-18T08:00:00+01:00. The service stores and compares the UTC instant."
          },
          "assignee": {
            "type": "string",
            "maxLength": 256
          },
          "note": {
            "type": "string",
            "minLength": 3,
            "maxLength": 2000
          },
          "nextAction": {
            "type": "string",
            "minLength": 3,
            "maxLength": 240
          },
          "nextActionAt": {
            "type": "string",
            "format": "date-time",
            "description": "An RFC 3339 date and time with Z or an offset, such as 2026-09-18T08:00:00+01:00. The service stores and compares the UTC instant."
          },
          "evidenceIds": {
            "type": "array",
            "items": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "maxItems": 20,
            "default": []
          }
        },
        "required": [
          "action",
          "expectedUpdatedAt",
          "note",
          "nextAction",
          "nextActionAt"
        ],
        "additionalProperties": false,
        "description": "A case handover or update: assignee, next action and its time, note and evidence, with the version being changed."
      },
      "PilotLenderInput": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "minLength": 2,
            "maxLength": 100
          },
          "segment": {
            "type": "string",
            "enum": [
              "Consumer lending",
              "Cooperative",
              "Asset finance",
              "Business finance"
            ]
          }
        },
        "required": [
          "name",
          "segment"
        ],
        "additionalProperties": false,
        "description": "A new synthetic lender: name and segment. A sandbox workspace holds at most five lenders, the two samples included."
      },
      "StaffMember": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "actor": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "role": {
            "type": "string",
            "enum": [
              "Admin",
              "Operations",
              "Finance",
              "Compliance reviewer",
              "Read-only"
            ]
          },
          "status": {
            "type": "string",
            "enum": [
              "active",
              "suspended",
              "revoked"
            ]
          },
          "expiresAt": {
            "type": "string"
          },
          "updatedAt": {
            "type": "string"
          }
        },
        "required": [
          "id",
          "actor",
          "name",
          "role",
          "status",
          "expiresAt",
          "updatedAt"
        ],
        "additionalProperties": false,
        "description": "A staff membership: its role, state, expiry and version."
      },
      "StaffAccess": {
        "type": "object",
        "properties": {
          "role": {
            "type": "string",
            "enum": [
              "Admin",
              "Operations",
              "Finance",
              "Compliance reviewer",
              "Read-only"
            ]
          },
          "status": {
            "type": "string",
            "enum": [
              "active",
              "suspended",
              "revoked"
            ]
          }
        },
        "required": [
          "role",
          "status"
        ],
        "additionalProperties": false,
        "description": "A membership's role and state, before or after a change."
      },
      "StaffChangeRequest": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "memberId": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "from": {
            "$ref": "#/components/schemas/StaffAccess"
          },
          "to": {
            "$ref": "#/components/schemas/StaffAccess"
          },
          "reason": {
            "type": "string"
          },
          "requestedBy": {
            "type": "string"
          },
          "requestedAt": {
            "type": "string"
          }
        },
        "required": [
          "id",
          "memberId",
          "name",
          "from",
          "to",
          "reason",
          "requestedBy",
          "requestedAt"
        ],
        "additionalProperties": false,
        "description": "A membership change that grants Admin, Finance or Compliance reviewer and waits for a second administrator: who asked, when and why. Approving it applies exactly this change; a later change to the membership leaves it out of date, and it is no longer listed."
      },
      "StaffMemberChange": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "actor": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "role": {
            "type": "string",
            "enum": [
              "Admin",
              "Operations",
              "Finance",
              "Compliance reviewer",
              "Read-only"
            ]
          },
          "status": {
            "type": "string",
            "enum": [
              "active",
              "suspended",
              "revoked"
            ]
          },
          "expiresAt": {
            "type": "string"
          },
          "updatedAt": {
            "type": "string"
          },
          "message": {
            "type": "string"
          },
          "pendingChange": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/StaffChangeRequest"
              },
              {
                "type": "null"
              }
            ]
          }
        },
        "required": [
          "id",
          "actor",
          "name",
          "role",
          "status",
          "expiresAt",
          "updatedAt",
          "message",
          "pendingChange"
        ],
        "additionalProperties": false,
        "description": "A membership change's answer: the membership as it now stands, what happened in plain words, and the waiting request when the change needs a second administrator (the membership is then unchanged)."
      },
      "StaffDirectoryMember": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "actor": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "role": {
            "type": "string",
            "enum": [
              "Admin",
              "Operations",
              "Finance",
              "Compliance reviewer",
              "Read-only"
            ]
          },
          "status": {
            "type": "string",
            "enum": [
              "active",
              "suspended",
              "revoked"
            ]
          },
          "expiresAt": {
            "type": [
              "string",
              "null"
            ]
          },
          "updatedAt": {
            "type": "string"
          },
          "lenderIds": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "allLenders": {
            "type": "boolean"
          }
        },
        "required": [
          "id",
          "actor",
          "name",
          "role",
          "status",
          "expiresAt",
          "updatedAt",
          "lenderIds",
          "allLenders"
        ],
        "additionalProperties": false,
        "description": "A membership in the team directory, with the lenders it may open; an administrator opens every lender and lists none. A viewer who is not an administrator sees only the colleagues who share a lender with them, only the lenders they share, and no one's expiry but their own (expiresAt null)."
      },
      "StaffInvitation": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "email": {
            "type": "string"
          },
          "role": {
            "type": "string",
            "enum": [
              "Admin",
              "Operations",
              "Finance",
              "Compliance reviewer",
              "Read-only"
            ]
          },
          "status": {
            "type": "string",
            "enum": [
              "pending",
              "accepted",
              "revoked"
            ]
          },
          "expiresAt": {
            "type": "string"
          },
          "invitedBy": {
            "type": "string"
          },
          "approval": {
            "type": "string",
            "enum": [
              "not_required",
              "awaiting",
              "approved"
            ]
          },
          "approvedBy": {
            "type": [
              "string",
              "null"
            ]
          }
        },
        "required": [
          "id",
          "email",
          "role",
          "status",
          "expiresAt",
          "invitedBy",
          "approval",
          "approvedBy"
        ],
        "additionalProperties": false,
        "description": "A pending, accepted or revoked invitation, who sent it and whether it waits for, or has, the second administrator's approval an Admin, Finance or Compliance reviewer invitation needs; the token is shown once, at creation."
      },
      "StaffEvent": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "actor": {
            "type": "string"
          },
          "action": {
            "type": "string"
          },
          "subject": {
            "type": "string"
          },
          "detail": {
            "$ref": "#/components/schemas/RecordData"
          },
          "createdAt": {
            "type": "string"
          }
        },
        "required": [
          "id",
          "actor",
          "action",
          "subject",
          "detail",
          "createdAt"
        ],
        "additionalProperties": false,
        "description": "One entry of the team's access history."
      },
      "StaffDirectory": {
        "type": "object",
        "properties": {
          "mode": {
            "type": "string",
            "enum": [
              "sandbox",
              "staff"
            ]
          },
          "actor": {
            "type": "string"
          },
          "members": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/StaffDirectoryMember"
            }
          },
          "lenders": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Merchant"
            }
          },
          "invitations": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/StaffInvitation"
            },
            "maxItems": 100
          },
          "changes": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/StaffChangeRequest"
            },
            "maxItems": 100
          },
          "events": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/StaffEvent"
            },
            "maxItems": 100
          },
          "message": {
            "type": "string"
          }
        },
        "required": [
          "mode",
          "actor",
          "members",
          "lenders",
          "invitations",
          "changes",
          "events",
          "message"
        ],
        "additionalProperties": false,
        "description": "The team as the caller may see it: members as StaffDirectoryMember describes; lenders, invitations, changes awaiting a second administrator and history for administrators. In the sandbox every list, lenders included, is empty and the message says why."
      },
      "InvitationInput": {
        "type": "object",
        "properties": {
          "email": {
            "type": "string",
            "format": "email",
            "maxLength": 254
          },
          "role": {
            "type": "string",
            "enum": [
              "Admin",
              "Operations",
              "Finance",
              "Compliance reviewer",
              "Read-only"
            ]
          }
        },
        "required": [
          "email",
          "role"
        ],
        "additionalProperties": false,
        "description": "An invitation: email address and pilot role."
      },
      "InvitationCreated": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "token": {
            "type": "string",
            "pattern": "^[a-f0-9]{64}$"
          },
          "approval": {
            "type": "string",
            "enum": [
              "not_required",
              "awaiting"
            ]
          },
          "message": {
            "type": "string"
          }
        },
        "required": [
          "id",
          "token",
          "approval",
          "message"
        ],
        "additionalProperties": false,
        "description": "The invitation, its one-time acceptance token and whether it waits for a second administrator's approval; no email is sent."
      },
      "AcceptInvitationInput": {
        "type": "object",
        "properties": {
          "token": {
            "type": "string",
            "pattern": "^[a-f0-9]{64}$"
          }
        },
        "required": [
          "token"
        ],
        "additionalProperties": false,
        "description": "The acceptance token from the invitation link."
      },
      "InvitationAccepted": {
        "type": "object",
        "properties": {
          "message": {
            "type": "string"
          },
          "role": {
            "type": "string",
            "enum": [
              "Admin",
              "Operations",
              "Finance",
              "Compliance reviewer",
              "Read-only"
            ]
          }
        },
        "required": [
          "message",
          "role"
        ],
        "additionalProperties": false,
        "description": "Confirmation of the new membership and its role."
      },
      "MembershipInput": {
        "type": "object",
        "properties": {
          "role": {
            "type": "string",
            "enum": [
              "Admin",
              "Operations",
              "Finance",
              "Compliance reviewer",
              "Read-only"
            ]
          },
          "status": {
            "type": "string",
            "enum": [
              "active",
              "suspended",
              "revoked"
            ]
          },
          "expectedUpdatedAt": {
            "type": "string",
            "format": "date-time",
            "description": "An RFC 3339 date and time with Z or an offset, such as 2026-09-18T08:00:00+01:00. The service stores and compares the UTC instant."
          },
          "reason": {
            "type": "string",
            "minLength": 3,
            "maxLength": 500
          }
        },
        "required": [
          "role",
          "status",
          "expectedUpdatedAt",
          "reason"
        ],
        "additionalProperties": false,
        "description": "A membership change: role, state, the version being changed and the reason."
      },
      "StaffLenderAccessInput": {
        "type": "object",
        "properties": {
          "expectedUpdatedAt": {
            "type": "string",
            "format": "date-time",
            "description": "An RFC 3339 date and time with Z or an offset, such as 2026-09-18T08:00:00+01:00. The service stores and compares the UTC instant."
          },
          "lenderIds": {
            "type": "array",
            "items": {
              "type": "string",
              "minLength": 1,
              "maxLength": 100
            },
            "maxItems": 250
          },
          "reason": {
            "type": "string",
            "minLength": 10,
            "maxLength": 1000
          }
        },
        "required": [
          "expectedUpdatedAt",
          "lenderIds",
          "reason"
        ],
        "additionalProperties": false,
        "description": "The lenders a non-administrator membership may open, with the version being changed and the reason."
      },
      "StaffLenderAccess": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "actor": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "role": {
            "type": "string",
            "enum": [
              "Admin",
              "Operations",
              "Finance",
              "Compliance reviewer",
              "Read-only"
            ]
          },
          "status": {
            "type": "string",
            "enum": [
              "active",
              "suspended",
              "revoked"
            ]
          },
          "expiresAt": {
            "type": "string"
          },
          "updatedAt": {
            "type": "string"
          },
          "lenderIds": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "allLenders": {
            "type": "boolean",
            "const": false
          },
          "message": {
            "type": "string"
          }
        },
        "required": [
          "id",
          "actor",
          "name",
          "role",
          "status",
          "expiresAt",
          "updatedAt",
          "lenderIds",
          "allLenders",
          "message"
        ],
        "additionalProperties": false,
        "description": "The membership with its saved lender access."
      },
      "ReadinessCheck": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "enum": [
              "identity",
              "mfa",
              "origin",
              "database",
              "encryption"
            ]
          },
          "name": {
            "type": "string"
          },
          "state": {
            "type": "string",
            "enum": [
              "verified_this_request",
              "configured",
              "configured_not_verified",
              "not_configured"
            ]
          },
          "detail": {
            "type": "string"
          }
        },
        "required": [
          "id",
          "name",
          "state",
          "detail"
        ],
        "additionalProperties": false,
        "description": "One readiness control (identity, MFA, origins, database isolation, encryption) with its state on this host and what it means."
      },
      "AccessReadiness": {
        "type": "object",
        "properties": {
          "syntheticOnly": {
            "type": "boolean",
            "const": true
          },
          "canCommission": {
            "type": "boolean"
          },
          "checkedAt": {
            "type": "string"
          },
          "checks": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ReadinessCheck"
            }
          }
        },
        "required": [
          "syntheticOnly",
          "canCommission",
          "checkedAt",
          "checks"
        ],
        "additionalProperties": false,
        "description": "The staff-access and encryption controls as this request observed them. Describes configuration; never reveals secrets."
      },
      "EncryptionVerification": {
        "type": "object",
        "properties": {
          "message": {
            "type": "string"
          },
          "checkedAt": {
            "type": "string"
          },
          "verified": {
            "type": "boolean",
            "const": true
          }
        },
        "required": [
          "message",
          "checkedAt",
          "verified"
        ],
        "additionalProperties": false,
        "description": "The result of sealing and opening a synthetic payload with the configured managed key."
      },
      "PayloadProtection": {
        "type": "object",
        "properties": {
          "message": {
            "type": "string"
          },
          "protectedCount": {
            "type": "integer",
            "minimum": 0
          },
          "mayHaveMore": {
            "type": "boolean"
          }
        },
        "required": [
          "message",
          "protectedCount",
          "mayHaveMore"
        ],
        "additionalProperties": false,
        "description": "How many stored payloads one bounded run protected, and whether another run is needed."
      },
      "ReverificationRequired": {
        "type": "object",
        "properties": {
          "clerk_error": {
            "type": "object",
            "properties": {
              "type": {
                "type": "string",
                "const": "forbidden"
              },
              "reason": {
                "type": "string",
                "const": "reverification-error"
              },
              "metadata": {
                "type": "object",
                "properties": {
                  "reverification": {
                    "type": "string"
                  }
                },
                "required": [
                  "reverification"
                ]
              }
            },
            "required": [
              "type",
              "reason",
              "metadata"
            ]
          }
        },
        "required": [
          "clerk_error"
        ],
        "description": "The identity provider's instruction to verify a second factor again; the console's sign-in component answers it."
      },
      "ProgressStep": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "href": {
            "type": "string",
            "pattern": "^/"
          },
          "state": {
            "type": "string",
            "enum": [
              "not_started",
              "in_progress",
              "awaiting_review",
              "completed",
              "blocked"
            ]
          },
          "evidence": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "missing": {
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        },
        "required": [
          "id",
          "name",
          "href",
          "state",
          "evidence",
          "missing"
        ],
        "additionalProperties": false,
        "description": "One pilot step with its state, the evidence behind that state and what is still missing."
      },
      "PilotAccess": {
        "type": "object",
        "properties": {
          "mode": {
            "type": "string",
            "enum": [
              "sandbox",
              "staff"
            ]
          },
          "state": {
            "type": "string",
            "enum": [
              "configured",
              "not_configured"
            ]
          },
          "message": {
            "type": "string"
          }
        },
        "required": [
          "mode",
          "state",
          "message"
        ],
        "additionalProperties": false,
        "description": "Whether real staff access is enabled on this host and what demo progress does not establish."
      },
      "PilotProgress": {
        "type": "object",
        "properties": {
          "lender": {
            "$ref": "#/components/schemas/Merchant"
          },
          "syntheticOnly": {
            "type": "boolean",
            "const": true
          },
          "access": {
            "$ref": "#/components/schemas/PilotAccess"
          },
          "steps": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ProgressStep"
            }
          }
        },
        "required": [
          "lender",
          "syntheticOnly",
          "access",
          "steps"
        ],
        "additionalProperties": false,
        "description": "The lender's progress through onboarding, ingestion, reconciliation, exceptions, close review and export, derived from its records."
      },
      "CloseReviewIssue": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "label": {
            "type": "string"
          },
          "detail": {
            "type": "string"
          },
          "unresolved": {
            "type": "boolean"
          }
        },
        "required": [
          "id",
          "label",
          "detail",
          "unresolved"
        ],
        "additionalProperties": false,
        "description": "A discrepancy or unresolved item the preparer must answer, and whether it remains open at the close."
      },
      "CloseReviewRecord": {
        "allOf": [
          {
            "$ref": "#/components/schemas/ValopayRecord"
          },
          {
            "type": "object",
            "properties": {
              "current": {
                "type": "boolean"
              }
            },
            "required": [
              "current"
            ]
          }
        ],
        "description": "A close review record with whether its snapshot still matches the close and its source evidence."
      },
      "CloseReviewEntry": {
        "type": "object",
        "properties": {
          "close": {
            "$ref": "#/components/schemas/ValopayRecord"
          },
          "issues": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CloseReviewIssue"
            }
          },
          "problem": {
            "type": [
              "string",
              "null"
            ]
          },
          "pendingFinancialCorrections": {
            "type": "integer",
            "minimum": 0
          },
          "reviews": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CloseReviewRecord"
            }
          }
        },
        "required": [
          "close",
          "issues",
          "problem",
          "pendingFinancialCorrections",
          "reviews"
        ],
        "additionalProperties": false,
        "description": "One close with the discrepancies a reviewer must answer, why it cannot be reviewed now (if so), pending financial corrections and its reviews."
      },
      "CloseReviewList": {
        "type": "object",
        "properties": {
          "closes": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CloseReviewEntry"
            },
            "maxItems": 25
          },
          "total": {
            "type": "integer",
            "minimum": 0
          },
          "actor": {
            "type": "string"
          },
          "reviewers": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Assignee"
            }
          },
          "accessMode": {
            "type": "string",
            "enum": [
              "sandbox",
              "staff"
            ]
          },
          "ownPrincipal": {
            "type": "string"
          }
        },
        "required": [
          "closes",
          "total",
          "actor",
          "reviewers",
          "accessMode",
          "ownPrincipal"
        ],
        "additionalProperties": false,
        "description": "The 25 newest closes with their reviews, the Finance reviewers available and who the caller is, so the console can enforce separation of duties."
      },
      "PrepareCloseReviewInput": {
        "type": "object",
        "properties": {
          "closeId": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100
          },
          "expectedUpdatedAt": {
            "type": "string",
            "format": "date-time",
            "description": "An RFC 3339 date and time with Z or an offset, such as 2026-09-18T08:00:00+01:00. The service stores and compares the UTC instant."
          },
          "reviewer": {
            "type": "string",
            "minLength": 1,
            "maxLength": 200
          },
          "preparationNote": {
            "type": "string",
            "minLength": 10,
            "maxLength": 3000
          },
          "discrepancyResponses": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "issueId": {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 100
                },
                "explanation": {
                  "type": "string",
                  "minLength": 10,
                  "maxLength": 3000
                }
              },
              "required": [
                "issueId",
                "explanation"
              ],
              "additionalProperties": false
            },
            "maxItems": 500
          },
          "unresolvedAcceptance": {
            "type": "string",
            "maxLength": 3000,
            "default": ""
          }
        },
        "required": [
          "closeId",
          "expectedUpdatedAt",
          "reviewer",
          "preparationNote",
          "discrepancyResponses"
        ],
        "additionalProperties": false,
        "description": "A close review preparation: the close and its version, an independent Finance reviewer, the preparation note and a response to every discrepancy."
      },
      "DecideCloseReviewInput": {
        "type": "object",
        "properties": {
          "expectedUpdatedAt": {
            "type": "string",
            "format": "date-time",
            "description": "An RFC 3339 date and time with Z or an offset, such as 2026-09-18T08:00:00+01:00. The service stores and compares the UTC instant."
          },
          "action": {
            "type": "string",
            "enum": [
              "approve",
              "return"
            ]
          },
          "note": {
            "type": "string",
            "minLength": 10,
            "maxLength": 3000
          },
          "sourceExceptions": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "issueId": {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 200
                },
                "reason": {
                  "type": "string",
                  "minLength": 10,
                  "maxLength": 3000
                },
                "evidence": {
                  "type": "string",
                  "minLength": 5,
                  "maxLength": 1000
                }
              },
              "required": [
                "issueId",
                "reason",
                "evidence"
              ],
              "additionalProperties": false
            },
            "maxItems": 500,
            "default": []
          }
        },
        "required": [
          "expectedUpdatedAt",
          "action",
          "note"
        ],
        "additionalProperties": false,
        "description": "A review decision: approve or reject with the version being decided, a note and an answer to every source exception."
      },
      "ImportCorrectionPreviewInput": {
        "type": "object",
        "properties": {
          "batchId": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100
          },
          "targetId": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100
          },
          "expectedUpdatedAt": {
            "type": "string",
            "format": "date-time",
            "description": "An RFC 3339 date and time with Z or an offset, such as 2026-09-18T08:00:00+01:00. The service stores and compares the UTC instant."
          },
          "changes": {
            "type": "object",
            "properties": {
              "name": {
                "type": "string",
                "minLength": 2,
                "maxLength": 160
              },
              "phoneMasked": {
                "type": "string",
                "maxLength": 40
              },
              "amountKobo": {
                "type": "integer",
                "minimum": 500000,
                "maximum": 9007199254740991
              },
              "dueDate": {
                "type": "string",
                "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
              }
            },
            "additionalProperties": false
          },
          "syntheticOnly": {
            "type": "boolean",
            "const": true
          }
        },
        "required": [
          "batchId",
          "targetId",
          "expectedUpdatedAt",
          "changes",
          "syntheticOnly"
        ],
        "additionalProperties": false,
        "description": "The batch, the imported record with its version, and the supported field changes to compare."
      },
      "ImportCorrectionPreview": {
        "type": "object",
        "properties": {
          "merchantId": {
            "type": "string"
          },
          "batchId": {
            "type": "string"
          },
          "targetId": {
            "type": "string"
          },
          "targetKind": {
            "type": "string"
          },
          "source": {
            "type": "string"
          },
          "rowId": {
            "type": "string"
          },
          "targetUpdatedAt": {
            "type": "string"
          },
          "financial": {
            "type": "boolean"
          },
          "previewDigest": {
            "type": "string"
          },
          "differences": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "field": {
                  "type": "string",
                  "enum": [
                    "name",
                    "phoneMasked",
                    "amountKobo",
                    "dueDate"
                  ]
                },
                "before": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "number"
                    },
                    {
                      "type": "null"
                    }
                  ]
                },
                "after": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "number"
                    }
                  ]
                }
              },
              "required": [
                "field",
                "before",
                "after"
              ],
              "additionalProperties": false
            }
          },
          "affected": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "id": {
                  "type": "string"
                },
                "kind": {
                  "type": "string"
                },
                "name": {
                  "type": "string"
                },
                "reference": {
                  "type": "string"
                },
                "status": {
                  "type": "string"
                },
                "updatedAt": {
                  "type": "string"
                }
              },
              "required": [
                "id",
                "kind",
                "name",
                "reference",
                "status",
                "updatedAt"
              ],
              "additionalProperties": false
            }
          },
          "blockers": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "consequence": {
            "type": "string"
          }
        },
        "required": [
          "merchantId",
          "batchId",
          "targetId",
          "targetKind",
          "source",
          "rowId",
          "targetUpdatedAt",
          "financial",
          "previewDigest",
          "differences",
          "affected",
          "blockers",
          "consequence"
        ],
        "additionalProperties": false,
        "description": "The before/after comparison, the records the change touches, any blockers and the digest a proposal must quote."
      },
      "ImportCorrectionProposalInput": {
        "type": "object",
        "properties": {
          "batchId": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100
          },
          "targetId": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100
          },
          "expectedUpdatedAt": {
            "type": "string",
            "format": "date-time",
            "description": "An RFC 3339 date and time with Z or an offset, such as 2026-09-18T08:00:00+01:00. The service stores and compares the UTC instant."
          },
          "changes": {
            "type": "object",
            "properties": {
              "name": {
                "type": "string",
                "minLength": 2,
                "maxLength": 160
              },
              "phoneMasked": {
                "type": "string",
                "maxLength": 40
              },
              "amountKobo": {
                "type": "integer",
                "minimum": 500000,
                "maximum": 9007199254740991
              },
              "dueDate": {
                "type": "string",
                "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
              }
            },
            "additionalProperties": false
          },
          "syntheticOnly": {
            "type": "boolean",
            "const": true
          },
          "previewDigest": {
            "type": "string",
            "pattern": "^[a-f0-9]{64}$"
          },
          "reviewer": {
            "type": "string",
            "minLength": 1,
            "maxLength": 180
          },
          "reason": {
            "type": "string",
            "minLength": 10,
            "maxLength": 1000
          },
          "evidence": {
            "type": "string",
            "minLength": 5,
            "maxLength": 1000
          }
        },
        "required": [
          "batchId",
          "targetId",
          "expectedUpdatedAt",
          "changes",
          "syntheticOnly",
          "previewDigest",
          "reviewer",
          "reason",
          "evidence"
        ],
        "additionalProperties": false,
        "description": "A proposal quoting the preview digest, naming an independent Finance reviewer, with the reason and evidence."
      },
      "ImportCorrectionDecisionInput": {
        "type": "object",
        "properties": {
          "proposalDigest": {
            "type": "string",
            "pattern": "^[a-f0-9]{64}$"
          },
          "action": {
            "type": "string",
            "enum": [
              "approve",
              "reject",
              "withdraw"
            ]
          },
          "reason": {
            "type": "string",
            "minLength": 10,
            "maxLength": 1000
          }
        },
        "required": [
          "proposalDigest",
          "action",
          "reason"
        ],
        "additionalProperties": false,
        "description": "Approve, reject or withdraw, quoting the proposal digest, with a reason."
      },
      "ImportCorrectionView": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "merchantId": {
            "type": "string"
          },
          "createdAt": {
            "type": "string"
          },
          "proposedBy": {
            "type": "string"
          },
          "proposedPrincipal": {
            "type": "string"
          },
          "reviewer": {
            "type": "string"
          },
          "reason": {
            "type": "string"
          },
          "evidence": {
            "type": "string"
          },
          "proposalDigest": {
            "type": "string"
          },
          "status": {
            "type": "string",
            "enum": [
              "awaiting_review",
              "approved",
              "rejected",
              "withdrawn"
            ]
          },
          "current": {
            "type": "boolean"
          },
          "preview": {
            "$ref": "#/components/schemas/ImportCorrectionPreview"
          },
          "decision": {
            "type": [
              "object",
              "null"
            ],
            "properties": {
              "id": {
                "type": "string"
              },
              "action": {
                "type": "string",
                "enum": [
                  "approve",
                  "reject",
                  "withdraw"
                ]
              },
              "actor": {
                "type": "string"
              },
              "principalId": {
                "type": "string"
              },
              "reason": {
                "type": "string"
              },
              "at": {
                "type": "string"
              }
            },
            "required": [
              "id",
              "action",
              "actor",
              "principalId",
              "reason",
              "at"
            ]
          }
        },
        "required": [
          "id",
          "merchantId",
          "createdAt",
          "proposedBy",
          "proposedPrincipal",
          "reviewer",
          "reason",
          "evidence",
          "proposalDigest",
          "status",
          "current",
          "preview",
          "decision"
        ],
        "additionalProperties": false,
        "description": "A proposal with its preview, its decision if any, and whether the comparison is still current."
      },
      "ImportCorrectionList": {
        "type": "object",
        "properties": {
          "batchId": {
            "type": "string"
          },
          "actor": {
            "type": "string"
          },
          "ownPrincipal": {
            "type": "string"
          },
          "role": {
            "type": "string"
          },
          "targets": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "id": {
                  "type": "string"
                },
                "kind": {
                  "type": "string"
                },
                "name": {
                  "type": "string"
                },
                "reference": {
                  "type": "string"
                },
                "updatedAt": {
                  "type": "string"
                },
                "rowId": {
                  "type": "string"
                },
                "amountKobo": {
                  "type": "number"
                },
                "dueDate": {
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "phoneMasked": {
                  "type": "string"
                },
                "supported": {
                  "type": "boolean"
                },
                "status": {
                  "type": "string"
                }
              },
              "required": [
                "id",
                "kind",
                "name",
                "reference",
                "updatedAt",
                "rowId",
                "amountKobo",
                "dueDate",
                "phoneMasked",
                "supported",
                "status"
              ],
              "additionalProperties": false
            }
          },
          "proposals": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ImportCorrectionView"
            }
          },
          "reviewers": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "actor": {
                  "type": "string"
                },
                "name": {
                  "type": "string"
                },
                "role": {
                  "type": "string"
                }
              },
              "required": [
                "actor",
                "name",
                "role"
              ]
            }
          },
          "syntheticOnly": {
            "type": "boolean",
            "const": true
          }
        },
        "required": [
          "batchId",
          "actor",
          "ownPrincipal",
          "role",
          "targets",
          "proposals",
          "reviewers",
          "syntheticOnly"
        ],
        "additionalProperties": false,
        "description": "The batch's imported records, its proposals and the Finance reviewers available."
      },
      "SourceProfileInput": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "minLength": 2,
            "maxLength": 120
          },
          "source": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100
          },
          "kind": {
            "type": "string",
            "enum": [
              "customers",
              "mandates",
              "due-items",
              "attempts",
              "observations"
            ]
          },
          "mapping": {
            "type": "object",
            "additionalProperties": {
              "type": "string",
              "maxLength": 100
            },
            "default": {}
          },
          "identityColumn": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100
          },
          "amountUnit": {
            "type": "string",
            "enum": [
              "naira",
              "kobo"
            ]
          },
          "firstExpectedAt": {
            "type": "string",
            "format": "date-time",
            "description": "An RFC 3339 date and time with Z or an offset, such as 2026-09-18T08:00:00+01:00. The service stores and compares the UTC instant."
          },
          "cadenceHours": {
            "type": "integer",
            "minimum": 1,
            "maximum": 8760
          },
          "graceMinutes": {
            "type": "integer",
            "minimum": 0,
            "maximum": 10080
          },
          "expectedRows": {
            "type": [
              "integer",
              "null"
            ],
            "minimum": 0,
            "maximum": 500,
            "default": null
          },
          "expectedAmountKobo": {
            "type": [
              "integer",
              "null"
            ],
            "minimum": 0,
            "maximum": 9007199254740991,
            "default": null
          },
          "status": {
            "type": "string",
            "enum": [
              "active",
              "paused"
            ],
            "default": "active"
          },
          "syntheticOnly": {
            "type": "boolean",
            "const": true
          },
          "expectedUpdatedAt": {
            "type": "string",
            "format": "date-time",
            "description": "An RFC 3339 date and time with Z or an offset, such as 2026-09-18T08:00:00+01:00. The service stores and compares the UTC instant."
          }
        },
        "required": [
          "name",
          "source",
          "kind",
          "identityColumn",
          "amountUnit",
          "firstExpectedAt",
          "cadenceHours",
          "graceMinutes",
          "syntheticOnly"
        ],
        "additionalProperties": false,
        "description": "A reusable synthetic source contract: mapping, identity column, amount unit, first expected delivery, cadence, grace and expected totals."
      },
      "SourceManifestInput": {
        "type": "object",
        "properties": {
          "businessDate": {
            "type": "string",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
          },
          "files": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "source": {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 100
                },
                "sourceBatchId": {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 120
                },
                "kind": {
                  "type": "string",
                  "enum": [
                    "customers",
                    "mandates",
                    "due-items",
                    "attempts",
                    "observations"
                  ]
                },
                "expectedRows": {
                  "type": "integer",
                  "minimum": 0,
                  "maximum": 500
                },
                "expectedAmountKobo": {
                  "type": "integer",
                  "minimum": 0,
                  "maximum": 9007199254740991
                }
              },
              "required": [
                "source",
                "sourceBatchId",
                "kind",
                "expectedRows",
                "expectedAmountKobo"
              ],
              "additionalProperties": false
            },
            "maxItems": 100
          },
          "noFilesExpected": {
            "type": "boolean"
          },
          "reason": {
            "type": "string",
            "minLength": 10,
            "maxLength": 3000
          },
          "evidence": {
            "type": "string",
            "minLength": 5,
            "maxLength": 1000
          },
          "previousManifestId": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100
          },
          "expectedUpdatedAt": {
            "type": "string",
            "format": "date-time",
            "description": "An RFC 3339 date and time with Z or an offset, such as 2026-09-18T08:00:00+01:00. The service stores and compares the UTC instant."
          },
          "syntheticOnly": {
            "type": "boolean",
            "const": true
          }
        },
        "required": [
          "businessDate",
          "files",
          "noFilesExpected",
          "reason",
          "evidence",
          "syntheticOnly"
        ],
        "additionalProperties": false,
        "description": "The files and control totals expected for one WAT business date, or an explicit no-file declaration, with reason and evidence; a revision names the declaration it replaces."
      },
      "SourceCompleteness": {
        "type": "object",
        "properties": {
          "businessDate": {
            "type": "string",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
          },
          "manifest": {
            "type": [
              "object",
              "null"
            ],
            "properties": {
              "id": {
                "type": "string"
              },
              "updatedAt": {
                "type": "string",
                "format": "date-time"
              },
              "data": {
                "type": "object",
                "additionalProperties": {}
              }
            },
            "required": [
              "id",
              "updatedAt",
              "data"
            ]
          },
          "files": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "source": {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 100
                },
                "sourceBatchId": {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 120
                },
                "kind": {
                  "type": "string",
                  "enum": [
                    "customers",
                    "mandates",
                    "due-items",
                    "attempts",
                    "observations"
                  ]
                },
                "expectedRows": {
                  "type": "integer",
                  "minimum": 0,
                  "maximum": 500
                },
                "expectedAmountKobo": {
                  "type": "integer",
                  "minimum": 0,
                  "maximum": 9007199254740991
                },
                "id": {
                  "type": "string"
                },
                "batchId": {
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "batchStatus": {
                  "type": "string"
                },
                "businessDate": {
                  "type": [
                    "string",
                    "null"
                  ],
                  "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
                },
                "receivedRows": {
                  "type": [
                    "integer",
                    "null"
                  ],
                  "minimum": 0,
                  "maximum": 9007199254740991
                },
                "receivedAmountKobo": {
                  "type": [
                    "integer",
                    "null"
                  ],
                  "minimum": 0,
                  "maximum": 9007199254740991
                },
                "status": {
                  "type": "string",
                  "enum": [
                    "complete",
                    "incomplete"
                  ]
                },
                "problems": {
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                }
              },
              "required": [
                "source",
                "sourceBatchId",
                "kind",
                "expectedRows",
                "expectedAmountKobo",
                "id",
                "batchId",
                "batchStatus",
                "businessDate",
                "receivedRows",
                "receivedAmountKobo",
                "status",
                "problems"
              ],
              "additionalProperties": false
            }
          },
          "activeProfiles": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "id": {
                  "type": "string"
                },
                "source": {
                  "type": "string"
                },
                "kind": {
                  "type": "string"
                }
              },
              "required": [
                "id",
                "source",
                "kind"
              ]
            }
          },
          "undeclared": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "id": {
                  "type": "string"
                },
                "name": {
                  "type": "string"
                },
                "status": {
                  "type": "string"
                },
                "source": {
                  "type": "string"
                },
                "sourceBatchId": {
                  "type": "string"
                },
                "kind": {
                  "type": "string"
                }
              },
              "required": [
                "id",
                "name",
                "status",
                "source",
                "sourceBatchId",
                "kind"
              ]
            }
          },
          "status": {
            "type": "string",
            "enum": [
              "complete",
              "incomplete"
            ]
          },
          "issues": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "id": {
                  "type": "string"
                },
                "label": {
                  "type": "string"
                },
                "detail": {
                  "type": "string"
                }
              },
              "required": [
                "id",
                "label",
                "detail"
              ]
            }
          },
          "basisDigest": {
            "type": "string",
            "pattern": "^[a-f0-9]{64}$"
          },
          "expectedFiles": {
            "type": "integer",
            "minimum": 0,
            "maximum": 9007199254740991
          },
          "completeFiles": {
            "type": "integer",
            "minimum": 0,
            "maximum": 9007199254740991
          }
        },
        "required": [
          "businessDate",
          "manifest",
          "files",
          "activeProfiles",
          "undeclared",
          "status",
          "issues",
          "basisDigest",
          "expectedFiles",
          "completeFiles"
        ],
        "description": "Whether the declared source files for a business date arrived complete, with each file's state, the profiles that expect a delivery by that date, undeclared batches and the issues Finance must answer."
      },
      "SourceBatchQuality": {
        "type": "object",
        "properties": {
          "profileId": {
            "type": [
              "string",
              "null"
            ]
          },
          "profileVersion": {
            "type": [
              "string",
              "null"
            ]
          },
          "sourceRows": {
            "type": "integer",
            "minimum": 0,
            "maximum": 9007199254740991
          },
          "sourceAmountKobo": {
            "type": [
              "integer",
              "null"
            ],
            "minimum": 0,
            "maximum": 9007199254740991
          },
          "importedRows": {
            "type": "integer",
            "minimum": 0,
            "maximum": 9007199254740991
          },
          "importedAmountKobo": {
            "type": [
              "integer",
              "null"
            ],
            "minimum": 0,
            "maximum": 9007199254740991
          },
          "duplicateRows": {
            "type": "integer",
            "minimum": 0,
            "maximum": 9007199254740991
          },
          "conflictRows": {
            "type": "integer",
            "minimum": 0,
            "maximum": 9007199254740991
          },
          "invalidRows": {
            "type": "integer",
            "minimum": 0,
            "maximum": 9007199254740991
          },
          "status": {
            "type": "string",
            "enum": [
              "checked",
              "needs_review",
              "unavailable"
            ]
          },
          "issues": {
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        },
        "required": [
          "profileId",
          "profileVersion",
          "sourceRows",
          "sourceAmountKobo",
          "importedRows",
          "importedAmountKobo",
          "duplicateRows",
          "conflictRows",
          "invalidRows",
          "status",
          "issues"
        ],
        "description": "The original committed totals and checks of a source batch."
      },
      "PaystackFixtureInput": {
        "type": "object",
        "properties": {
          "scenario": {
            "type": "string",
            "enum": [
              "payment",
              "duplicate",
              "amount_mismatch",
              "out_of_order",
              "tampered"
            ]
          },
          "syntheticOnly": {
            "type": "boolean",
            "const": true
          }
        },
        "required": [
          "scenario",
          "syntheticOnly"
        ],
        "additionalProperties": false,
        "description": "A recorded Paystack test scenario to deliver to the inbox."
      },
      "ProviderReplayInput": {
        "type": "object",
        "properties": {
          "expectedUpdatedAt": {
            "type": "string",
            "format": "date-time",
            "description": "An RFC 3339 date and time with Z or an offset, such as 2026-09-18T08:00:00+01:00. The service stores and compares the UTC instant."
          },
          "reason": {
            "type": "string",
            "minLength": 3,
            "maxLength": 500
          }
        },
        "required": [
          "expectedUpdatedAt",
          "reason"
        ],
        "additionalProperties": false,
        "description": "A replay of a stored provider event, with its version and the reason."
      },
      "SourceDelivery": {
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "enum": [
              "paused",
              "late",
              "on_schedule",
              "awaiting_first_delivery"
            ]
          },
          "missedDeliveries": {
            "type": "integer",
            "minimum": 0,
            "maximum": 9007199254740991
          },
          "nextExpectedAt": {
            "type": "string"
          },
          "lastCommittedAt": {
            "type": [
              "string",
              "null"
            ]
          },
          "lastBatchId": {
            "type": [
              "string",
              "null"
            ]
          }
        },
        "required": [
          "status",
          "missedDeliveries",
          "nextExpectedAt",
          "lastCommittedAt",
          "lastBatchId"
        ],
        "additionalProperties": false,
        "description": "Where a profile stands against its cadence: missed deliveries, the next expected time and the last committed batch."
      },
      "SourceProfile": {
        "allOf": [
          {
            "$ref": "#/components/schemas/ValopayRecord"
          },
          {
            "type": "object",
            "properties": {
              "delivery": {
                "$ref": "#/components/schemas/SourceDelivery"
              }
            },
            "required": [
              "delivery"
            ]
          }
        ],
        "description": "A source profile record with its delivery state."
      },
      "SourceBatchSummary": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "source": {
            "type": "string"
          },
          "sourceBatchId": {
            "type": "string"
          },
          "kind": {
            "type": "string"
          },
          "status": {
            "type": "string"
          },
          "createdAt": {
            "type": "string"
          },
          "quality": {
            "$ref": "#/components/schemas/SourceBatchQuality"
          }
        },
        "required": [
          "id",
          "name",
          "source",
          "sourceBatchId",
          "kind",
          "status",
          "createdAt",
          "quality"
        ],
        "additionalProperties": false,
        "description": "A batch as the sources page lists it, with its original quality totals."
      },
      "SourceSummary": {
        "type": "object",
        "properties": {
          "lateSources": {
            "type": "integer",
            "minimum": 0,
            "maximum": 9007199254740991
          },
          "duplicateRows": {
            "type": "integer",
            "minimum": 0,
            "maximum": 9007199254740991
          },
          "conflictRows": {
            "type": "integer",
            "minimum": 0,
            "maximum": 9007199254740991
          },
          "batchesNeedingReview": {
            "type": "integer",
            "minimum": 0,
            "maximum": 9007199254740991
          }
        },
        "required": [
          "lateSources",
          "duplicateRows",
          "conflictRows",
          "batchesNeedingReview"
        ],
        "additionalProperties": false,
        "description": "Counts that need attention: late sources, duplicate and conflicting rows, batches needing review."
      },
      "ProviderEvent": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "status": {
            "type": "string"
          },
          "reference": {
            "type": "string"
          },
          "amountKobo": {
            "type": "integer",
            "minimum": 0,
            "maximum": 9007199254740991
          },
          "createdAt": {
            "type": "string"
          },
          "updatedAt": {
            "type": "string"
          },
          "mode": {
            "type": "string",
            "enum": [
              "fixture",
              "test"
            ]
          },
          "message": {
            "type": "string"
          },
          "deliveryCount": {
            "type": "integer",
            "minimum": 0,
            "maximum": 9007199254740991
          },
          "replayCount": {
            "type": "integer",
            "minimum": 0,
            "maximum": 9007199254740991
          },
          "financialRecordsCreated": {
            "type": "integer",
            "const": 0
          }
        },
        "required": [
          "id",
          "name",
          "status",
          "reference",
          "amountKobo",
          "createdAt",
          "updatedAt",
          "mode",
          "message",
          "deliveryCount",
          "replayCount",
          "financialRecordsCreated"
        ],
        "additionalProperties": false,
        "description": "A stored provider event: fixture or test mode, how often it was delivered and replayed, and the guarantee that it created no financial record."
      },
      "PaystackInbox": {
        "type": "object",
        "properties": {
          "mode": {
            "type": "string",
            "const": "test_only"
          },
          "externalConnectionVerified": {
            "type": "boolean",
            "const": false
          },
          "canRunFixtures": {
            "type": "boolean"
          },
          "state": {
            "type": "string",
            "const": "configuration_required"
          },
          "message": {
            "type": "string"
          },
          "events": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ProviderEvent"
            },
            "maxItems": 50
          },
          "total": {
            "type": "integer",
            "minimum": 0,
            "maximum": 9007199254740991
          },
          "quarantined": {
            "type": "integer",
            "minimum": 0,
            "maximum": 9007199254740991
          },
          "duplicates": {
            "type": "integer",
            "minimum": 0,
            "maximum": 9007199254740991
          }
        },
        "required": [
          "mode",
          "externalConnectionVerified",
          "canRunFixtures",
          "state",
          "message",
          "events",
          "total",
          "quarantined",
          "duplicates"
        ],
        "additionalProperties": false,
        "description": "The read-only Paystack test inbox: its fixed test-only state, the stored events and how many were quarantined or duplicated."
      },
      "SourcesView": {
        "type": "object",
        "properties": {
          "completeness": {
            "$ref": "#/components/schemas/SourceCompleteness"
          },
          "profiles": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/SourceProfile"
            }
          },
          "batches": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/SourceBatchSummary"
            }
          },
          "summary": {
            "$ref": "#/components/schemas/SourceSummary"
          },
          "paystack": {
            "$ref": "#/components/schemas/PaystackInbox"
          }
        },
        "required": [
          "completeness",
          "profiles",
          "batches",
          "summary",
          "paystack"
        ],
        "additionalProperties": false,
        "description": "Everything the sources page shows for one lender and business date."
      },
      "PaystackFixtureResult": {
        "type": "object",
        "properties": {
          "accepted": {
            "type": "boolean"
          },
          "duplicate": {
            "type": "boolean"
          },
          "event": {
            "$ref": "#/components/schemas/ProviderEvent"
          }
        },
        "required": [
          "accepted",
          "duplicate",
          "event"
        ],
        "additionalProperties": false,
        "description": "Whether the fixture was accepted or recognised as a duplicate, and the stored event."
      },
      "PaystackTestEvent": {
        "type": "object",
        "properties": {
          "event": {
            "type": "string"
          },
          "data": {
            "type": "object",
            "additionalProperties": {},
            "description": "The event's payload as Paystack sent it."
          }
        },
        "required": [
          "event",
          "data"
        ],
        "description": "A Paystack test event exactly as Paystack signed it. The signature covers these bytes, so the body is authenticated before it is parsed. charge.success and the two direct-debit authorisation events are recorded; any other signed event is acknowledged and recorded as ignored."
      },
      "PaystackDeliveryReceipt": {
        "type": "object",
        "properties": {
          "accepted": {
            "type": "boolean",
            "const": true
          },
          "duplicate": {
            "type": "boolean"
          }
        },
        "required": [
          "accepted",
          "duplicate"
        ],
        "description": "The acknowledgement Paystack receives: the signed event is saved in the mapped lender's inbox, or recognised as a repeat delivery of one already saved."
      },
      "PersonalWorkView": {
        "type": "object",
        "properties": {
          "merchantId": {
            "type": "string",
            "minLength": 1,
            "maxLength": 300
          },
          "lenderName": {
            "type": "string"
          },
          "actor": {
            "type": "string",
            "minLength": 1,
            "maxLength": 300
          },
          "role": {
            "type": "string"
          },
          "asOf": {
            "type": "string",
            "format": "date-time"
          },
          "syntheticOnly": {
            "type": "boolean",
            "const": true
          },
          "canViewTeam": {
            "type": "boolean"
          },
          "canWork": {
            "type": "boolean"
          },
          "scope": {
            "type": "string",
            "enum": [
              "mine",
              "team"
            ]
          },
          "filter": {
            "type": "string",
            "enum": [
              "all",
              "overdue",
              "handover",
              "review",
              "unread"
            ],
            "default": "all"
          },
          "items": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "id": {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 300
                },
                "eventId": {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 300
                },
                "sourceId": {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 300
                },
                "sourceVersion": {
                  "type": "string",
                  "format": "date-time"
                },
                "sourceDigest": {
                  "type": "string",
                  "pattern": "^[a-f0-9]{64}$"
                },
                "type": {
                  "type": "string",
                  "enum": [
                    "case",
                    "handover",
                    "review"
                  ]
                },
                "title": {
                  "type": "string"
                },
                "nextAction": {
                  "type": "string"
                },
                "assignee": {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 300
                },
                "assigneeName": {
                  "type": "string"
                },
                "dueAt": {
                  "type": [
                    "string",
                    "null"
                  ],
                  "format": "date-time"
                },
                "overdue": {
                  "type": "boolean"
                },
                "escalated": {
                  "type": "boolean"
                },
                "escalationReason": {
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "reviewCurrent": {
                  "type": [
                    "boolean",
                    "null"
                  ]
                },
                "href": {
                  "type": "string",
                  "pattern": "^/"
                },
                "readAt": {
                  "type": [
                    "string",
                    "null"
                  ],
                  "format": "date-time"
                },
                "canAcknowledge": {
                  "type": "boolean"
                },
                "assignmentEventId": {
                  "type": [
                    "string",
                    "null"
                  ],
                  "minLength": 1,
                  "maxLength": 300
                },
                "notice": {
                  "type": [
                    "string",
                    "null"
                  ]
                }
              },
              "required": [
                "id",
                "eventId",
                "sourceId",
                "sourceVersion",
                "sourceDigest",
                "type",
                "title",
                "nextAction",
                "assignee",
                "assigneeName",
                "dueAt",
                "overdue",
                "escalated",
                "escalationReason",
                "reviewCurrent",
                "href",
                "readAt",
                "canAcknowledge",
                "assignmentEventId",
                "notice"
              ],
              "additionalProperties": false
            },
            "maxItems": 50
          },
          "total": {
            "type": "integer",
            "minimum": 0
          },
          "offset": {
            "type": "integer",
            "minimum": 0
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 50
          },
          "counts": {
            "type": "object",
            "properties": {
              "all": {
                "type": "integer",
                "minimum": 0
              },
              "overdue": {
                "type": "integer",
                "minimum": 0
              },
              "handover": {
                "type": "integer",
                "minimum": 0
              },
              "review": {
                "type": "integer",
                "minimum": 0
              },
              "unread": {
                "type": "integer",
                "minimum": 0
              },
              "escalated": {
                "type": "integer",
                "minimum": 0
              }
            },
            "required": [
              "all",
              "overdue",
              "handover",
              "review",
              "unread",
              "escalated"
            ],
            "additionalProperties": false
          },
          "workload": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "actor": {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 300
                },
                "name": {
                  "type": "string"
                },
                "total": {
                  "type": "integer",
                  "minimum": 0
                },
                "overdue": {
                  "type": "integer",
                  "minimum": 0
                },
                "handovers": {
                  "type": "integer",
                  "minimum": 0
                },
                "reviews": {
                  "type": "integer",
                  "minimum": 0
                },
                "escalated": {
                  "type": "integer",
                  "minimum": 0
                }
              },
              "required": [
                "actor",
                "name",
                "total",
                "overdue",
                "handovers",
                "reviews",
                "escalated"
              ],
              "additionalProperties": false
            },
            "maxItems": 100
          },
          "workloadTotal": {
            "type": "integer",
            "minimum": 0
          },
          "history": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "id": {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 300
                },
                "action": {
                  "type": "string",
                  "enum": [
                    "read",
                    "acknowledge"
                  ]
                },
                "sourceId": {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 300
                },
                "summary": {
                  "type": "string"
                },
                "at": {
                  "type": "string",
                  "format": "date-time"
                },
                "href": {
                  "type": "string",
                  "pattern": "^/"
                }
              },
              "required": [
                "id",
                "action",
                "sourceId",
                "summary",
                "at",
                "href"
              ],
              "additionalProperties": false
            },
            "maxItems": 10
          },
          "escalationRule": {
            "type": "string"
          }
        },
        "required": [
          "merchantId",
          "lenderName",
          "actor",
          "role",
          "asOf",
          "syntheticOnly",
          "canViewTeam",
          "canWork",
          "scope",
          "items",
          "total",
          "offset",
          "limit",
          "counts",
          "workload",
          "workloadTotal",
          "history",
          "escalationRule"
        ],
        "additionalProperties": false,
        "description": "The caller's (or, for administrators, the team's) cases, handovers, reviews and notifications, paged and counted."
      },
      "WorkReceiptInput": {
        "type": "object",
        "properties": {
          "sourceId": {
            "type": "string",
            "minLength": 1,
            "maxLength": 300
          },
          "eventId": {
            "type": "string",
            "minLength": 1,
            "maxLength": 300
          },
          "expectedUpdatedAt": {
            "type": "string",
            "format": "date-time",
            "description": "An RFC 3339 date and time with Z or an offset, such as 2026-09-18T08:00:00+01:00. The service stores and compares the UTC instant."
          },
          "expectedDigest": {
            "type": "string",
            "pattern": "^[a-f0-9]{64}$"
          }
        },
        "required": [
          "sourceId",
          "eventId",
          "expectedUpdatedAt",
          "expectedDigest"
        ],
        "additionalProperties": false,
        "description": "The item being acknowledged, with its version."
      },
      "WorkReceipt": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "minLength": 1,
            "maxLength": 300
          },
          "merchantId": {
            "type": "string",
            "minLength": 1,
            "maxLength": 300
          },
          "action": {
            "type": "string",
            "enum": [
              "read",
              "acknowledge"
            ]
          },
          "sourceId": {
            "type": "string",
            "minLength": 1,
            "maxLength": 300
          },
          "eventId": {
            "type": "string",
            "minLength": 1,
            "maxLength": 300
          },
          "actor": {
            "type": "string",
            "minLength": 1,
            "maxLength": 300
          },
          "at": {
            "type": "string",
            "format": "date-time"
          },
          "duplicate": {
            "type": "boolean"
          },
          "syntheticOnly": {
            "type": "boolean",
            "const": true
          },
          "financialStatusChanged": {
            "type": "boolean",
            "const": false
          }
        },
        "required": [
          "id",
          "merchantId",
          "action",
          "sourceId",
          "eventId",
          "actor",
          "at",
          "duplicate",
          "syntheticOnly",
          "financialStatusChanged"
        ],
        "additionalProperties": false,
        "description": "The recorded acknowledgement: who, what, and when."
      },
      "LifecycleView": {
        "type": "object",
        "properties": {
          "merchantId": {
            "type": "string",
            "minLength": 1,
            "maxLength": 200
          },
          "lenderName": {
            "type": "string"
          },
          "actor": {
            "type": "string",
            "minLength": 1,
            "maxLength": 200
          },
          "asOf": {
            "type": "string",
            "format": "date-time"
          },
          "policy": {
            "type": "object",
            "properties": {
              "rawCsvDays": {
                "type": [
                  "integer",
                  "null"
                ],
                "minimum": 1,
                "maximum": 3650
              },
              "journalPayloadDays": {
                "type": [
                  "integer",
                  "null"
                ],
                "minimum": 1,
                "maximum": 3650
              },
              "exportFileDays": {
                "type": [
                  "integer",
                  "null"
                ],
                "minimum": 1,
                "maximum": 3650
              },
              "auditTrail": {
                "type": "string",
                "const": "retain"
              }
            },
            "required": [
              "rawCsvDays",
              "journalPayloadDays",
              "exportFileDays",
              "auditTrail"
            ],
            "additionalProperties": false
          },
          "minimumDays": {
            "type": "object",
            "properties": {
              "rawCsvDays": {
                "type": "integer",
                "minimum": 1
              },
              "journalPayloadDays": {
                "type": "integer",
                "minimum": 1
              },
              "exportFileDays": {
                "type": "integer",
                "minimum": 1
              }
            },
            "required": [
              "rawCsvDays",
              "journalPayloadDays",
              "exportFileDays"
            ],
            "additionalProperties": false
          },
          "secondApprover": {
            "type": "boolean"
          },
          "policyRevision": {
            "type": "string",
            "pattern": "^[a-f0-9]{64}$"
          },
          "holdRevision": {
            "type": "string",
            "pattern": "^[a-f0-9]{64}$"
          },
          "eligibleCount": {
            "type": "integer",
            "minimum": 0
          },
          "evidenceTotal": {
            "type": "integer",
            "minimum": 0
          },
          "targets": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "kind": {
                  "type": "string",
                  "enum": [
                    "raw_csv",
                    "journal_payload",
                    "export_file"
                  ]
                },
                "merchantId": {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 200
                },
                "sourceId": {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 200
                },
                "version": {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 200
                },
                "createdAt": {
                  "type": "string",
                  "format": "date-time"
                },
                "label": {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 200
                },
                "digest": {
                  "type": "string",
                  "pattern": "^[a-f0-9]{64}$"
                },
                "status": {
                  "type": "string",
                  "enum": [
                    "committed",
                    "completed",
                    "cancelled",
                    "ready",
                    "failed"
                  ]
                },
                "held": {
                  "type": "boolean"
                },
                "evidence": {
                  "type": "array",
                  "items": {
                    "type": "object",
                    "properties": {
                      "reason": {
                        "type": "string",
                        "enum": [
                          "open_case",
                          "approved_close_review"
                        ]
                      },
                      "recordId": {
                        "type": "string",
                        "minLength": 1,
                        "maxLength": 200
                      }
                    },
                    "required": [
                      "reason",
                      "recordId"
                    ],
                    "additionalProperties": false
                  },
                  "maxItems": 10
                }
              },
              "required": [
                "kind",
                "merchantId",
                "sourceId",
                "version",
                "createdAt",
                "label",
                "digest",
                "status",
                "held",
                "evidence"
              ],
              "additionalProperties": false
            },
            "maxItems": 100
          },
          "targetTotal": {
            "type": "integer",
            "minimum": 0
          },
          "targetOffset": {
            "type": "integer",
            "minimum": 0
          },
          "holds": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "kind": {
                  "type": "string",
                  "enum": [
                    "raw_csv",
                    "journal_payload",
                    "export_file"
                  ]
                },
                "sourceId": {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 200
                },
                "reason": {
                  "type": "string"
                },
                "actor": {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 200
                },
                "at": {
                  "type": "string",
                  "format": "date-time"
                }
              },
              "required": [
                "kind",
                "sourceId",
                "reason",
                "actor",
                "at"
              ],
              "additionalProperties": false
            },
            "maxItems": 100
          },
          "holdTotal": {
            "type": "integer",
            "minimum": 0
          },
          "runs": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "id": {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 200
                },
                "merchantId": {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 200
                },
                "status": {
                  "type": "string",
                  "enum": [
                    "preview",
                    "approved",
                    "running",
                    "completed",
                    "attention"
                  ]
                },
                "updatedAt": {
                  "type": "string",
                  "format": "date-time"
                },
                "createdAt": {
                  "type": "string",
                  "format": "date-time"
                },
                "expiresAt": {
                  "type": "string",
                  "format": "date-time"
                },
                "previewDigest": {
                  "type": "string",
                  "pattern": "^[a-f0-9]{64}$"
                },
                "policyRevision": {
                  "type": "string",
                  "pattern": "^[a-f0-9]{64}$"
                },
                "candidates": {
                  "type": "array",
                  "items": {
                    "type": "object",
                    "properties": {
                      "kind": {
                        "type": "string",
                        "enum": [
                          "raw_csv",
                          "journal_payload",
                          "export_file"
                        ]
                      },
                      "merchantId": {
                        "type": "string",
                        "minLength": 1,
                        "maxLength": 200
                      },
                      "sourceId": {
                        "type": "string",
                        "minLength": 1,
                        "maxLength": 200
                      },
                      "version": {
                        "type": "string",
                        "minLength": 1,
                        "maxLength": 200
                      },
                      "createdAt": {
                        "type": "string",
                        "format": "date-time"
                      },
                      "label": {
                        "type": "string",
                        "minLength": 1,
                        "maxLength": 200
                      },
                      "digest": {
                        "type": "string",
                        "pattern": "^[a-f0-9]{64}$"
                      },
                      "status": {
                        "type": "string",
                        "enum": [
                          "committed",
                          "completed",
                          "cancelled",
                          "ready",
                          "failed"
                        ]
                      }
                    },
                    "required": [
                      "kind",
                      "merchantId",
                      "sourceId",
                      "version",
                      "createdAt",
                      "label",
                      "digest",
                      "status"
                    ],
                    "additionalProperties": false
                  },
                  "maxItems": 100
                },
                "candidateCount": {
                  "type": "integer",
                  "minimum": 0
                },
                "moreEligible": {
                  "type": "integer",
                  "minimum": 0
                },
                "preparedBy": {
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "approvedBy": {
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "approvedAt": {
                  "type": [
                    "string",
                    "null"
                  ],
                  "format": "date-time"
                },
                "receipts": {
                  "type": "array",
                  "items": {
                    "type": "object",
                    "properties": {
                      "id": {
                        "type": "string",
                        "minLength": 1,
                        "maxLength": 200
                      },
                      "kind": {
                        "type": "string",
                        "enum": [
                          "raw_csv",
                          "journal_payload",
                          "export_file"
                        ]
                      },
                      "sourceId": {
                        "type": "string",
                        "minLength": 1,
                        "maxLength": 200
                      },
                      "status": {
                        "type": "string",
                        "enum": [
                          "deleted",
                          "already_absent",
                          "blocked",
                          "failed"
                        ]
                      },
                      "at": {
                        "type": "string",
                        "format": "date-time"
                      },
                      "detail": {
                        "type": "string"
                      },
                      "actor": {
                        "type": "string",
                        "minLength": 1,
                        "maxLength": 200
                      }
                    },
                    "required": [
                      "id",
                      "kind",
                      "sourceId",
                      "status",
                      "at",
                      "detail",
                      "actor"
                    ],
                    "additionalProperties": false
                  },
                  "maxItems": 100
                },
                "successful": {
                  "type": "integer",
                  "minimum": 0
                },
                "remaining": {
                  "type": "integer",
                  "minimum": 0
                },
                "auditRetained": {
                  "type": "boolean",
                  "const": true
                },
                "financialRecordsRetained": {
                  "type": "boolean",
                  "const": true
                },
                "syntheticOnly": {
                  "type": "boolean",
                  "const": true
                }
              },
              "required": [
                "id",
                "merchantId",
                "status",
                "updatedAt",
                "createdAt",
                "expiresAt",
                "previewDigest",
                "policyRevision",
                "candidates",
                "candidateCount",
                "moreEligible",
                "approvedBy",
                "approvedAt",
                "receipts",
                "successful",
                "remaining",
                "auditRetained",
                "financialRecordsRetained",
                "syntheticOnly"
              ],
              "additionalProperties": false
            },
            "maxItems": 10
          },
          "auditRetained": {
            "type": "boolean",
            "const": true
          },
          "financialRecordsRetained": {
            "type": "boolean",
            "const": true
          },
          "syntheticOnly": {
            "type": "boolean",
            "const": true
          }
        },
        "required": [
          "merchantId",
          "lenderName",
          "actor",
          "asOf",
          "policy",
          "policyRevision",
          "holdRevision",
          "eligibleCount",
          "evidenceTotal",
          "targets",
          "targetTotal",
          "targetOffset",
          "holds",
          "holdTotal",
          "runs",
          "auditRetained",
          "financialRecordsRetained",
          "syntheticOnly"
        ],
        "additionalProperties": false,
        "description": "The lender's retention policy, the shortest periods it may set (minimumDays) and whether a second administrator approves runs (secondApprover), holds, bounded inventory of what the policy would touch, and saved retention runs."
      },
      "LifecycleRunView": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "minLength": 1,
            "maxLength": 200
          },
          "merchantId": {
            "type": "string",
            "minLength": 1,
            "maxLength": 200
          },
          "status": {
            "type": "string",
            "enum": [
              "preview",
              "approved",
              "running",
              "completed",
              "attention"
            ]
          },
          "updatedAt": {
            "type": "string",
            "format": "date-time"
          },
          "createdAt": {
            "type": "string",
            "format": "date-time"
          },
          "expiresAt": {
            "type": "string",
            "format": "date-time"
          },
          "previewDigest": {
            "type": "string",
            "pattern": "^[a-f0-9]{64}$"
          },
          "policyRevision": {
            "type": "string",
            "pattern": "^[a-f0-9]{64}$"
          },
          "candidates": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "kind": {
                  "type": "string",
                  "enum": [
                    "raw_csv",
                    "journal_payload",
                    "export_file"
                  ]
                },
                "merchantId": {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 200
                },
                "sourceId": {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 200
                },
                "version": {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 200
                },
                "createdAt": {
                  "type": "string",
                  "format": "date-time"
                },
                "label": {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 200
                },
                "digest": {
                  "type": "string",
                  "pattern": "^[a-f0-9]{64}$"
                },
                "status": {
                  "type": "string",
                  "enum": [
                    "committed",
                    "completed",
                    "cancelled",
                    "ready",
                    "failed"
                  ]
                }
              },
              "required": [
                "kind",
                "merchantId",
                "sourceId",
                "version",
                "createdAt",
                "label",
                "digest",
                "status"
              ],
              "additionalProperties": false
            },
            "maxItems": 100
          },
          "candidateCount": {
            "type": "integer",
            "minimum": 0
          },
          "moreEligible": {
            "type": "integer",
            "minimum": 0
          },
          "preparedBy": {
            "type": [
              "string",
              "null"
            ]
          },
          "approvedBy": {
            "type": [
              "string",
              "null"
            ]
          },
          "approvedAt": {
            "type": [
              "string",
              "null"
            ],
            "format": "date-time"
          },
          "receipts": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "id": {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 200
                },
                "kind": {
                  "type": "string",
                  "enum": [
                    "raw_csv",
                    "journal_payload",
                    "export_file"
                  ]
                },
                "sourceId": {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 200
                },
                "status": {
                  "type": "string",
                  "enum": [
                    "deleted",
                    "already_absent",
                    "blocked",
                    "failed"
                  ]
                },
                "at": {
                  "type": "string",
                  "format": "date-time"
                },
                "detail": {
                  "type": "string"
                },
                "actor": {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 200
                }
              },
              "required": [
                "id",
                "kind",
                "sourceId",
                "status",
                "at",
                "detail",
                "actor"
              ],
              "additionalProperties": false
            },
            "maxItems": 100
          },
          "successful": {
            "type": "integer",
            "minimum": 0
          },
          "remaining": {
            "type": "integer",
            "minimum": 0
          },
          "auditRetained": {
            "type": "boolean",
            "const": true
          },
          "financialRecordsRetained": {
            "type": "boolean",
            "const": true
          },
          "syntheticOnly": {
            "type": "boolean",
            "const": true
          }
        },
        "required": [
          "id",
          "merchantId",
          "status",
          "updatedAt",
          "createdAt",
          "expiresAt",
          "previewDigest",
          "policyRevision",
          "candidates",
          "candidateCount",
          "moreEligible",
          "approvedBy",
          "approvedAt",
          "receipts",
          "successful",
          "remaining",
          "auditRetained",
          "financialRecordsRetained",
          "syntheticOnly"
        ],
        "additionalProperties": false,
        "description": "One retention run: its reviewed manifest, who prepared it, approval state and per-item receipts."
      },
      "RetentionPolicyInput": {
        "type": "object",
        "properties": {
          "policy": {
            "type": "object",
            "properties": {
              "rawCsvDays": {
                "type": [
                  "integer",
                  "null"
                ],
                "minimum": 1,
                "maximum": 3650
              },
              "journalPayloadDays": {
                "type": [
                  "integer",
                  "null"
                ],
                "minimum": 1,
                "maximum": 3650
              },
              "exportFileDays": {
                "type": [
                  "integer",
                  "null"
                ],
                "minimum": 1,
                "maximum": 3650
              },
              "auditTrail": {
                "type": "string",
                "const": "retain"
              }
            },
            "required": [
              "rawCsvDays",
              "journalPayloadDays",
              "exportFileDays",
              "auditTrail"
            ],
            "additionalProperties": false
          },
          "expectedRevision": {
            "type": "string",
            "pattern": "^[a-f0-9]{64}$"
          },
          "reason": {
            "type": "string",
            "minLength": 10,
            "maxLength": 500
          }
        },
        "required": [
          "policy",
          "expectedRevision",
          "reason"
        ],
        "additionalProperties": false,
        "description": "The retention periods per kind, with the version being changed and the reason."
      },
      "RetentionHoldInput": {
        "type": "object",
        "properties": {
          "kind": {
            "type": "string",
            "enum": [
              "raw_csv",
              "journal_payload",
              "export_file"
            ]
          },
          "sourceId": {
            "type": "string",
            "minLength": 1,
            "maxLength": 200
          },
          "held": {
            "type": "boolean"
          },
          "expectedHoldRevision": {
            "type": "string",
            "pattern": "^[a-f0-9]{64}$"
          },
          "reason": {
            "type": "string",
            "minLength": 10,
            "maxLength": 500
          }
        },
        "required": [
          "kind",
          "sourceId",
          "held",
          "expectedHoldRevision",
          "reason"
        ],
        "additionalProperties": false,
        "description": "A hold on one item, or its release, with the reason."
      },
      "LifecyclePreviewInput": {
        "type": "object",
        "properties": {
          "expectedPolicyRevision": {
            "type": "string",
            "pattern": "^[a-f0-9]{64}$"
          }
        },
        "required": [
          "expectedPolicyRevision"
        ],
        "additionalProperties": false,
        "description": "Which kinds to preview and the reason for the run."
      },
      "LifecycleApproveInput": {
        "type": "object",
        "properties": {
          "expectedUpdatedAt": {
            "type": "string",
            "format": "date-time",
            "description": "An RFC 3339 date and time with Z or an offset, such as 2026-09-18T08:00:00+01:00. The service stores and compares the UTC instant."
          },
          "previewDigest": {
            "type": "string",
            "pattern": "^[a-f0-9]{64}$"
          },
          "reason": {
            "type": "string",
            "minLength": 10,
            "maxLength": 500
          }
        },
        "required": [
          "expectedUpdatedAt",
          "previewDigest",
          "reason"
        ],
        "additionalProperties": false,
        "description": "Approval of a previewed run, quoting its manifest digest."
      },
      "LifecycleExecuteInput": {
        "type": "object",
        "properties": {
          "previewDigest": {
            "type": "string",
            "pattern": "^[a-f0-9]{64}$"
          }
        },
        "required": [
          "previewDigest"
        ],
        "additionalProperties": false,
        "description": "Execution of an approved run, quoting its manifest digest, in bounded batches."
      }
    },
    "headers": {
      "X-Valopay-Operation": {
        "description": "The id of this request's entry in the operations journal: GET /v1/operations lists it, and POST /v1/operations/{id}/retry and /cancel take it. Sent on every answer, success, refusal or failure, to a request whose Idempotency-Key the journal records, once the entry exists; absent without a key, when the key itself is refused, and for a demo role switch, which the journal does not record.",
        "schema": {
          "type": "string"
        }
      }
    },
    "securitySchemes": {
      "sandboxCookie": {
        "type": "apiKey",
        "in": "cookie",
        "name": "__Host-valopay_sandbox",
        "description": "The anonymous sandbox's cookie: a 32-byte random token in hexadecimal that the service sets on the first visit (GET /v1/workspace) and renews on every answer, HttpOnly, SameSite=Lax and lasting 30 days from the last visit. Behind TLS it is __Host-valopay_sandbox (Secure, Path=/, no Domain); on a plain-HTTP local run it is valopay_sandbox. A missing or malformed token starts a new sandbox; two different tokens are refused (400). The sandbox is found by the token's digest, never by the token, which is never stored or logged. A staff host identifies a signed-in session instead."
      }
    }
  }
}